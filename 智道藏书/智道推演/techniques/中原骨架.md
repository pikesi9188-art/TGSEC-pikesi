---
name: cn-frameworks
description: 大爱仙尊·假设作业长文（cn-frameworks）。先专卡后读本文。
---

> 作业先走 MAP.md 对应专卡。本文件是长文速查，不替代专卡。

# CN Frameworks（国产框架漏洞速查目录）

> 仅在根路由选择本目录后读取。每个条目都是待验证的检查假设；执行前用当前一手证据确认版本/适用性，不虚构命中。
> **分工**：本目录 = **框架 → 已知 CVE/未授权端点**（正向速查）；`realworld-patterns.md` = 黑产站**业务逻辑共性**（支付回调/IDOR/会话导出/弱口令链）。同一框架两边都看——本目录给"这个框架历史上有哪些洞"，realworld 给"这类业务站怎么打逻辑"。
> **说明**：本目录为公开漏洞事实的提炼改写，非原文拷贝。CVE 编号仅作检索锚点，实战以一手指纹+版本确认为准。

**触发**：指纹识别到国产框架（RuoYi 若依 / 通达OA / 泛微 e-cology / 用友 NC·U8 / 致远 / 蓝凌 / 万户 / DedeCMS·Discuz·ThinkPHP 等国产 CMS / 发卡系统）。

## 领域决策直觉

1. **国产框架"最快洞"三选一优先级**：未授权端点（单个 servlet/jsp 免鉴权 RCE/上传/文件读）> 默认口令（`admin/admin`、`admin/123456`、产品名口令）> 历史 CVE。先探未授权面，命中即终局，别一上来就翻 CVE 列表。
2. **未授权 servlet 是 OA 的命门**：致远 `wpsAssistServlet`/`htmlofficeservlet`、用友 `BshServlet`、泛微 `XmlRpcServlet`/Dubbo debug——这些是**单请求免认证**入口，价值远高于需登录的 SQLi。
3. **通达OA 的定式是 `delete.php` SQLi**：几乎每个模块都有一个 `delete.php`，未过滤 ID 参数直入 SQL。命中一个，同站其他 `delete.php` 大概率同构（专家直觉 #1：同一过滤逻辑被多页面复用）。
4. **版本决定打法**：先探版本（500 页横幅 / `ver.txt` / 登录页构建日期 / FOFA title），再按"版本-漏洞速查"选路径，不盲目全喷。
5. **移动端/PDA 子路径鉴权更弱**：`/pda/`、`/mobile/`、`/m/`、`/app-api/` 常比主站弱——注入和越权集中在这里。
6. **国产 CMS 通病**：`/install/` 未删（变量覆盖/反序列化 getshell）+ 模板编辑器后台 RCE + 默认口令 + `.bak` 备份泄露。
7. **发卡/OA 拿到 webshell 或 SQLi 后先翻配置**：数据库连接串、支付商户密钥、云存储 AK/SK、内网地址——这些比数据本身更能拓线（转 `realworld-patterns.md` 支付线 / `cloud-cn.md` 云线）。

## 指纹速查表（先认框架再选路径）

| 框架 | FOFA / 特征 | 特征路径 / 响应 |
|------|-------------|----------------|
| RuoYi 若依 | `body="ruoyi"`、`app="若依"` | `/prod-api/` 返回"欢迎使用RuoYi"、`/druid/`、`/swagger-ui.html`、`/captchaImage` |
| 通达OA | `app="Tongda-OA"` | `/logincheck.php`、`/general/`、`/ispirit/` |
| 泛微 e-cology | `app="泛微-协同办公OA"` | `/weaver/`、`/ecology/`、`/E-mobile/` |
| 泛微 e-office | `body="e-office"` | `/eoffice/`、`OfficeServer.php` |
| 用友 NC/U8 | `app="用友-NC"`、`title="Yonyou NC"` | `/nc/`、`/u8sl/`、`/servlet/~ic/`、`/service/` |
| 致远 Seeyon | `app="致远互联-OA"` | `/seeyon/`、`seeyon/main.do`、`seeyon/login.jsp` |
| 蓝凌 Landray | `app="蓝凌OA"` | `/sys/`、`/landray/`、`/ekp/` |
| 万户 Wanhu | `app="万户OA"` | ezoffice/`/defaultroot/` |
| DedeCMS | 页脚"织梦"、`/data/admin/ver.txt` | `/dede/`、`/plus/`、`/include/` |
| Discuz | `/source/discuz_version.php`、页脚 | `/admin.php`、`/uc_server/`、`/wap/index.php` |
| ThinkPHP | 500 页横幅版本、`?s=` 路由 | `/index.php?s=`、`/runtime/log/` |
| Typecho | meta generator | `/admin/`、`/install.php` |
| 发卡 dujiaoka | `body="dujiaoka"`、`app="独角数卡"` | `/pay/*/notify_url`、`/admin`(dcat) |
| 发卡 acg-faka | `lizhipay/acg-faka` | `/user/api/*`、`callback.Epay` |

---

## RuoYi（若依，Spring Boot + Shiro）

> Java 系政企/后台高占有率框架。最快洞：**Shiro 默认密钥反序列化（免认证 RCE）** → 默认口令 → Druid 未授权 → 自定义 `/prod-api/**` 未鉴权端点。

**指纹**：`curl -s https://target/prod-api/` 返回"欢迎使用RuoYi后台管理框架"；`Set-Cookie: rememberMe=...`（Shiro）；`/captchaImage`（含 UUID）。

### 最快漏洞点

| # | 漏洞 | 端点/条件 | 认证 | CVE |
|---|------|----------|------|-----|
| 1 | Shiro rememberMe 反序列化 RCE | AES-CBC 密钥硬编码 `kPH+bIxk5D2deZiIxcaaaA==`（≤4.6.0 / Shiro<1.7） | ❌ 免认证 | CVE-2016-4437 |
| 2 | 默认口令 | `admin/admin123`、`ry/123456` | — | — |
| 3 | Druid 监控未授权 | `/druid/index.html`（<4.7.0 常未配认证） | ❌/弱 | — |
| 4 | 自定义 `/prod-api/**` 未鉴权 | 业务 Controller 漏注册 SecurityConfig → 配置读写 | ❌ | — |
| 5 | 文件上传 → RCE | `/common/upload` | ✅ 后台 | CVE-2022-32065 |
| 6 | 代码生成 SQLi/模板注入 | `/tool/gen/createTable` | ✅ | CVE-2022-48114 / CVE-2024-46076 / CVE-2025-0734 |
| 7 | 定时任务 RCE | `/monitor/job` 调 `ryTask` 执行方法 | ✅ 后台 | — |
| 8 | 权限提升 | 改 postID/menuId/jobID/role 越权 | ✅ 低权 | CVE-2024-57438 / CVE-2025-28400~28403 |
| 9 | 信息泄露 | `/swagger-ui.html`、`/doc.html`、`/v2/api-docs`、`/actuator` | 视配置 | — |

### 已验证入口 / 检测要点

- **Shiro 检测（假设→验证）**：`curl -v https://target -H "Cookie: rememberMe=1"` → 响应含 `Set-Cookie: rememberMe=deleteMe` = 用 Shiro。密钥命中判定：用已知默认 key 加密探针 payload，服务端不返回 `deleteMe` = 密钥有效。**v4.6.1+ 已随机化密钥**，但很多存量部署仍用默认 key。利用走标准 Shiro 反序列化工具链（CommonsBeanutils 等 gadget），不在此展开武器化。
- **未授权配置端点（实战高命中）**：RuoYi 自定义业务 Controller 常漏加认证拦截 → 任意 `/prod-api/<业务>/**` 可能免鉴权读写。典型危害：读到含全部支付/汇率密钥的配置对象。详见 `realworld-patterns.md` §四 RuoYi 系与支付线。
- **Druid**：`/druid/index.html` 未授权 → SQL 执行历史、数据源连接串（内网 MySQL）、Session 信息。默认口令 `ruoyi/123456` 也常见（见 realworld §4.3）。
- **前端泄露**：RuoYi-Vue 前端 JS（`jsencrypt.*.js`）可能内嵌 RSA 公私钥 → 解密 remember-me / 请求体。

### 检测 / 防御

- 检测：`/druid/`、`/swagger-ui.html`、`/actuator` 公网可达即高危；Shiro `deleteMe` 响应 + 默认 key 命中。
- 防御（P0）：改默认口令、禁用 `admin`；升级 Shiro 并随机化 rememberMe 密钥；SecurityConfig 为所有 `/prod-api/**` 显式加认证拦截；关闭 Swagger/Druid 公网暴露；验证码升级滑块/reCAPTCHA。

**版本-路径速查**：≤4.5 → Shiro RCE→默认口令→Druid→上传；4.6~4.7.3 → 默认口令→gen SQLi→上传；4.7.4~4.8 → gen SQLi→权限提升→信息泄露。

---

## 通达OA（Tongda，PHP）

> 定式：**`delete.php` 系列 SQL 注入**（~50+ CVE 同一模式）+ 文件上传 RCE + 默认口令。

**指纹**：登录页 `/logincheck.php`、路径 `/general/`、`/ispirit/`、`/pda/`；FOFA `app="Tongda-OA"`。影响面 2017~v11.10。

### 最快漏洞点

| 漏洞 | 模式 | 代表 CVE |
|------|------|---------|
| SQLi（主力，遍布各模块 `delete.php`） | 未过滤 GET/POST 参数直入 SQL | CVE-2024-4903(`/general/meeting/manage/delete.php` M_ID_STR)、CVE-2024-25320(`/affair/delete.php`)、CVE-2024-1251(`/general/email/outbox/delete.php`)、CVE-2024-1252、CVE-2024-0938、CVE-2023-7180/7021~7023 等 |
| SQLi（PDA 子系统，鉴权更弱） | `/pda/**` 参数注入 | CVE-2024-10617(`/pda/workflow/check_seal.php` ID)、CVE-2024-10656~10731 系列 |
| 文件上传 → RCE | 上传绕过 → webshell（<v11.5/v11.6） | 公开 `admintony/TongdaRCE` 链 |
| 路径遍历 / 信息泄露 | `/inc/package_static_resources.php` | CVE-2024-10599 |

### 已验证入口 / 检测要点

- **SQLi 假设**：任取一个 `delete.php`（如 `/general/meeting/manage/delete.php?M_ID_STR=1`）→ 报错/布尔/时间差确认注入 → dump 管理员哈希。**同站横向**：命中一个 `delete.php` 后，其余模块 `delete.php` 大概率同构，逐个试（sqlmap 批量或手动）。
- **RCE 链**：SQLi 拿凭据登录 → 后台文件上传绕过 → webshell；或直接走公开 TongdaRCE 上传链（版本匹配时）。
- 默认口令：`admin/admin`、`admin/123456`、`admin/tongda`。

### 检测 / 防御

- 检测：WAF/日志监控 `delete.php` 的异常 ID 参数（含引号/`sleep`/`updatexml`）；`/pda/**` 未认证访问。
- 防御：升级到最新版；所有 `delete.php`/PDA 端点参数化查询 + 类型校验；上传目录禁执行；改默认口令。

---

## 泛微 Weaver（e-cology / e-office，Java）

> 核心突破口：**未授权 servlet**——`XmlRpcServlet`（免认证文件读）、Dubbo debug（免认证 RCE）、e-office `OfficeServer.php`（免认证上传）。

**指纹**：`/weaver/`、`/ecology/`、`/E-mobile/`（e-cology）；`/eoffice/`、`OfficeServer.php`（e-office）；FOFA `app="泛微-协同办公OA"`。

### e-cology 最快漏洞点

| 漏洞 | 端点 | 认证 | CVE |
|------|------|------|-----|
| Dubbo debug RCE | `/papi/esearch/data/devops/dubboApi/debug/method` | ❌ 免认证 | CVE-2026-22679（v10.0 <20260312） |
| XmlRpcServlet 任意文件读 | `/weaver/org.apache.xmlrpc.webserver.XmlRpcServlet` | ❌ 免认证 | CVE-2022-50992（<10.52） |
| FrameworkShellController RCE | 构造脚本请求 | 视配置 | CVE-2023-51892（v10.0.2310.01） |
| getdata.jsp SQLi | `getdata.jsp`（sql 参数直入） | 视配置 | CVE-2025-34038（E-cology 8.0） |
| 竞态文件上传 → RCE | race 上传 | ❌ | CVE-2024-48069 / CVE-2024-48070 |
| 任意文件读（POST handler） | `filelFileDownloadForOutDoc.class` | — | CVE-2023-3793 |
| 源码泄露 | `/cloudstore/ecode/setup/ecology_dev.zip` | ❌ | CVE-2024-7704 |
| XXE / CRLF | `RequestInfoByXml` / `ViewRequestForwardSPA.jsp`(isintervenor) | — | CVE-2023-2806 / CVE-2019-10272 |

### e-office 最快漏洞点

| 漏洞 | 端点 | 认证 | CVE |
|------|------|------|-----|
| 任意文件上传 | `OfficeServer.php`（multipart POST） | ❌ 免认证 | CVE-2022-50993（<10.0_20221201） |
| 文件上传 | `/inc/jquery/uploadify/uploadify.php`(Filedata) | ✅ | CVE-2023-2648（9.5） |
| 文件上传 | `App/Ajax/ajax.php?action=mobile_upload_save` | 视配置 | CVE-2023-2523 |
| 配置泄露 | `/building/backmgr/.../jx2_config.ini`、`/E-mobile/App/System/File/downfile.php`(url) | — | CVE-2023-2766 / CVE-2023-2765 |

### 已验证入口 / 检测要点

- **优先探未授权面**：`XmlRpcServlet` 免认证读配置文件 → 抽数据库凭据/API key/服务口令；e-office `OfficeServer.php` 免认证上传直接 webshell。这两个是"单请求终局"，先打。
- **RCE 升级链**：XmlRpcServlet 读配置 → getdata.jsp SQLi dump 凭据 → Dubbo debug（CVE-2026-22679）或 FrameworkShellController（CVE-2023-51892）RCE，或竞态上传 webshell。
- 默认口令：e-cology `sysadmin/1`；e-office `admin/admin`、`admin/123456`、`admin/ecology`。

### 检测 / 防御

- 检测：`XmlRpcServlet`、`OfficeServer.php`、`/papi/.../dubboApi/debug/` 公网可达即高危；监控这些路径的异常 POST。
- 防御：升级到 vendor 最新补丁（多为免认证严重洞，优先级 P0）；WAF 拦截上述 servlet 未授权访问；关闭 Dubbo debug/XML-RPC 对外；`ecology_dev.zip` 类源码包移出 web 根。

---

## 用友 Yonyou（NC / U8 Cloud / PLM，Java）

> 单一最危：**BeanShell 测试 servlet 免认证 = 裸奔 Java 解释器 = trivial RCE**。

**指纹**：`/nc/`、`/u8sl/`、`/portal/`、`/servlet/~ic/`、`/service/`；FOFA `app="用友-NC"` / `title="Yonyou NC"`。

### 最快漏洞点

| 漏洞 | 端点 | 认证 | CVE |
|------|------|------|-----|
| BeanShell 代码注入 RCE | `/servlet/~ic/bsh.servlet.BshServlet` | ❌ 免认证 | CVE-2025-34039（NC ≤6.5） |
| 任意文件读 | `PrintTemplateFileServlet` | — | CVE-2023-4748（≤20230807） |
| U8 Cloud SQLi | `nc/pubitf/erm/mobile/appservice/AppServletService.class`(usercode) | — | CVE-2025-14185（U8 Cloud 5.0~5.1sp） |
| 网关头操纵 | `/service/NCloudGatewayServlet` | — | CVE-2025-12344 |
| PLM 命令注入 | 全版本 | — | CVE-2021-41744 |
| Open Redirect / XSS | `/help/top.jsp`、`/menu.jsp`(flag)、`/login.jsp`(redirect) | — | CVE-2025-2709~2712 |

### 已验证入口 / 检测要点

- **BeanShell（先打，命中即终局）**：`/servlet/~ic/bsh.servlet.BshServlet` 免认证暴露 = 未授权 Java 代码执行（等价于裸奔的 `Runtime.exec`）。存量 NC 部署极常见。假设→验证：GET 该路径若返回 BeanShell 测试页 = 可用；执行探针命令确认（回显即 RCE）。
- **BeanShell 打补丁后备选**：`PrintTemplateFileServlet` 文件读 → 抽配置库凭据 → 登录；或 U8 Cloud 移动端 `usercode` SQLi。
- 默认口令：`admin/admin`、`admin/1`、`system/system`、`nc/nc`、`yonyou/yonyou`。

### 检测 / 防御

- 检测：`/servlet/~ic/bsh.servlet.BshServlet` 公网可达 = 立即高危处置；监控该路径任意访问。
- 防御：删除/禁用 BeanShell 测试 servlet（P0）；升级到 vendor 补丁；`PrintTemplateFileServlet` 加路径白名单；移动端接口参数化。

---

## 致远 Seeyon（A8/A6/M3，Java）

> 主力入口：**未授权文件上传/写入 servlet** → JSP webshell。`wpsAssistServlet` 与 `htmlofficeservlet` 是野外最常打的两个。

**指纹**：`/seeyon/`、`seeyon/main.do`、`seeyon/login.jsp`；FOFA `app="致远互联-OA"`（title 常暴露 `V8.0SP2` 等版本）。

### 最快漏洞点

| 漏洞 | 端点 | 认证 | CVE |
|------|------|------|-----|
| 路径遍历上传 → RCE | `/seeyon/wpsAssistServlet`（realFileType/fileId 路径穿越写 JSP） | ❌ 免认证 | CVE-2025-34040（v5.0~v8.0SP2） |
| 任意文件写入 | `/seeyon/htmlofficeservlet` | ❌ 免认证 | CVE-2019-25714 |
| 反序列化 | `ThirdMenuController`(oursNetService.getData) | — | CVE-2025-5140（≤8.1SP2） |
| RCE | postData function | — | CVE-2025-4531（8.1SP2） |
| Session 操纵 | `thirdpartyController.do`(enc) | — | CVE-2021-4461（≤7.0SP1） |
| 路径遍历 / SSRF / XSS | `M3CoreController` Download、`ssoproxy.jsp`、`main.do`(topValue) | — | CVE-2025-4529 / 4000 / 56451 |

### 已验证入口 / 检测要点

- **wpsAssistServlet（首选）**：`realFileType` 用 `../../../../ApacheJetspeed/webapps/ROOT/xxx.jsp` 路径穿越，multipart 上传 JSP 到 webroot → 访问即 webshell。免认证，v5.0~v8.0SP2 通杀（EDB-52490）。
- **htmlofficeservlet（备选）**：另一个免认证任意文件写入 webroot 的入口；与上者构成致远两大文件写向量。
- 打补丁系统：`ThirdMenuController` 反序列化（CVE-2025-5140）作为后手。
- 默认口令：`admin/123456`、`system/system`、`seeyon/123456`。

### 检测 / 防御

- 检测：`wpsAssistServlet`、`htmlofficeservlet` 公网可达即高危；监控 webroot 下新出现的 `.jsp`；上传参数含 `../` 路径穿越。
- 防御：升级 vendor 补丁（P0，两大免认证写入洞）；WAF 拦截这两个 servlet 未授权访问；webroot 目录禁 JSP 写入/执行。

---

## 蓝凌 Landray / 万户 Wanhu（Java / 混合）

**蓝凌指纹**：`/sys/`、`/landray/`、`/ekp/`；FOFA `app="蓝凌OA"`。

| 漏洞 | 端点 | 认证 | CVE |
|------|------|------|-----|
| HQL 注入（可查任意 Hibernate 实体） | uid POST 参数 | ❌ 免认证 | CVE-2024-58352 |
| 任意文件读 | `/sys/ui/extend/varkind/custom.jsp` | — | CVE-2022-34924 |

- **HQL 注入要点**：不同于原始 SQL，HQL 注入可访问所有映射实体类 → 直接掏用户凭据/系统配置/业务数据。免认证，危害大。
- 蓝凌历史上还有 `/data/sys-common/datajson.js` 类反序列化、`admin.do` 命令执行等（CNVD/CNNVD 收录多于 CVE），指纹命中后按版本查最新 POC。
- 默认口令：`admin/admin`、`sysadmin/123456`、`admin/landray`。

**万户指纹**：ezoffice、`/defaultroot/`；FOFA `app="万户OA"`。CVE.org 收录为 0，但 CNVD/CNNVD 常报 SQLi / 文件上传 / 默认口令类。默认口令 `admin/admin`、`admin/123456`、`admin/wanhu`。指纹命中后走通用 OA 打法（未授权 servlet 探测 + `delete.php` 类 SQLi + 上传）。

### 检测 / 防御（蓝凌/万户）

- 检测：`custom.jsp`、`datajson.js`、`/defaultroot/` 未授权访问；HQL 注入的异常 uid 参数。
- 防御：升级补丁；未授权端点加认证；HQL/SQL 参数化；改默认口令。

---

## 金蝶 Kingdee / 帆软 FineReport（方法论占位，暂无结构化 CVE 目录）

> ⚠️ 本目录暂未收录这两家的结构化 CVE 目录。此处**只给攻击面方法论**，不列具体 CVE 编号以免虚构；实战需用一手指纹 + 公开漏洞库（CNVD/CNNVD/NVD）核实版本对应 POC。

- **金蝶（EAS / Cloud 云星空 / K3）**：指纹 `/kingdee/`、`/K3Cloud/`、云星空登录页。常见公开漏洞**类**：RPC/序列化接口未授权（`ScpSupRpcServic` 类）、文件上传、目录遍历/文件读、默认口令。方法论：探未授权 RPC/servlet → 文件读抽配置 → 反序列化/上传 RCE。
- **帆软 FineReport / FineBI**：指纹 `/webroot/decision`、`/ReportServer`、报表设计器。常见公开漏洞**类**：远程设计/通道反序列化 RCE、未授权信息泄露（`ReportServer?op=fr_config`）、任意文件读、权限绕过（v9/v10 历史）。方法论：探 `/ReportServer` 未授权配置/文件读 → 反序列化 RCE；先确认版本再选公开 POC。
- 二者均适用通用国产框架直觉：未授权端点优先 → 默认口令 → 版本对应 CVE。

---

## 国产 CMS 速查（PHP 系）

> 通病：`/install/` 未删（变量覆盖/反序列化 getshell）+ 后台模板编辑 RCE + `.bak` 备份泄露 + 默认口令 `admin/admin`·`admin/123456`。指纹命中后按下表选最快点。

| CMS | 指纹 | 最快漏洞点（未授权/CVE 优先） | 备注 |
|-----|------|------------------------------|------|
| **DedeCMS 织梦** | `/dede/`、`/data/admin/ver.txt`、页脚 | `/install/index.php(.bak)` 变量覆盖 RFI getshell(CVE-2015-4553)；`plus/feedback_js.php` SQLi;`select_soft_post.php` 会话伪造+上传;`file_manage_control.php` 命令执行 | 后台默认 `admin/admin`；厂商加固变体见 realworld §12.1 |
| **EmpireCMS 帝国** | `/e/admin/`、`/e/install/` | `e/install/index.php` 未删 RCE；Guestbook CLIENT-IP/XFF 头 SQLi;`..%2F` 目录穿越上传;`ReplaceListVars` 模板 eval | 配置 `/e/class/config.php` |
| **PHPCMS** | `/api.php?op=phpcms`、`/caches/` | 1.1.7 多处 class RFI;`data.php`/`search_ajax.php`/`yp/job.php` SQLi;`attachment.class.php`(9.6.0) 上传;`type.php` 代码注入 | — |
| **Discuz!** | `/source/discuz_version.php`、`/uc_server/` | `wap/index.php` RCE(6.x/7.x, CVE-2008-6958, creditsformula eval);Discuz!ML 3.2-3.4 语言包 RCE;`member.php` 任意改密(需 uid);X5.0 认证绕过+LFI(2026) | 插件层 SQLi（Crazy Star/JiangHu） |
| **ThinkPHP** | 500 页横幅版本、`?s=` 路由 | <5.0.23 `invokefunction` RCE;5.0.23 POST `_method=__construct`;<6.0.14 lang+pearcmd LFI→RCE(CVE-2022-47945);6.x 反序列化(CVE-2024-44902/48112, flysystem-cached-adapter) | 反序列化是 6.x 主向量;`/runtime/log/` 可能泄凭据 |
| **FastAdmin**（TP 系） | `/index.php/admin/`、`filter=`/`op=` 参数 | `Ajax.php`(table 参数) SQLi + 未授权上传;auth 绕过;继承 ThinkPHP 反序列化 | **filter/op 注入绕多租户**详见 realworld §2.1 |
| **Typecho** | meta generator、`/install.php` | `install.php` 反序列化 RCE(CVE-2018-18753, `__typecho_config` cookie);1.2.1 上传;存储 XSS 偷 admin cookie | 配置 `/config.inc.php` |

**CMS 打法定式**：指纹+版本 → 探 `/install/` 是否未删（DedeCMS/EmpireCMS/Typecho 的历史 getshell 全靠这个）→ 版本对应注入/RCE → 后台默认口令 → 后台模板/插件编辑器写 PHP。ThinkPHP 特例：先看 500 页横幅定版本，<5.0.23 直接 `invokefunction`，6.x 转反序列化。

---

## 发卡系统（dujiaoka / acg-faka / 云尚 / 风铃）

> 本域**业务逻辑打法**（支付回调伪造/查单拖卡密/价格篡改）已在 `realworld-patterns.md` §一、§九、§十八 详述——那是首选，命中率最高。本节只补**框架/依赖层的已知 CVE**，与业务逻辑互补。

**dujiaoka 独角数卡**（Laravel 6 + dcat-admin，≤2.0.4 已归档不修）：

- 无自身 CVE，但**继承 dcat-admin 全部漏洞**（admin 面板即 dcat）：
  - CVE-2026-11621 未授权/低权文件上传 `/admin/dcat-api/editor-md/upload`（editormd-image-file）→ 上传绕过可 RCE
  - CVE-2025-65656 文件包含 `Extend/VersionManager.php`
  - CVE-2024-54775 / CVE-2025-0709 / CVE-2024-29644 / CVE-2023-33736 存储/反射 XSS（menu/roles/登录框/URL 参数）
- 通用弱点：dcat-admin 默认 `admin/admin` 无爆破防护；`.env` `APP_DEBUG=true` 泄露 `APP_KEY`/DB 查询；`ADMIN_HTTPS=false` 明文传凭据；Laravel 6 EOL 依赖链未修。
- 业务线（首选）：全系支付回调签名可逆 → 零元购；`search-order-by-email`/`detail-order-sn` 订单 IDOR。见 realworld §1.1。

**acg-faka 异次元**（PHP 8 自研 + Eloquent）：

- CVE-2023-43971 反射 XSS（`Index.php` encode 参数，1.1.7）
- 默认 demo：`demo@demo.com/123456`；session cookie `ACG-SHOP`；`/admin`→登录；`kernel/Install/Lock` 删除可重装。
- 业务线（首选）：持商户 key 伪造 Epay 回调零元购；`/user/api/index/query` 未鉴权拖卡密；`/app/Pay/*/runtime.log` 泄商户 key。见 realworld §一、§九。

**云尚 / 风铃发卡**：无 CVE，小众。风铃 USDT 直付 → 交易确认可伪造/低确认数双花/竞态；云尚"无加密版 1.5.7"源码公开可静态审计（搜硬编码凭据/`eval`/`system`/SQLi）。业务打法走 realworld 发卡类共性表。

### 检测 / 防御（发卡）

- 检测：`/admin/dcat-api/editor-md/upload` 未授权上传；`APP_DEBUG` 错误页；`/app/Pay/*/runtime.log` 可读；回调端点 GET 触发 + 只验签。
- 防御：升级 dcat-admin；关闭 `APP_DEBUG`；回调强随机密钥 ≥32 字符 + 校验金额/来源 IP + 订单状态机不可回退（详见 realworld 支付回调防御）；日志目录禁 web 访问。

---

## 跨框架收尾（拿到入口之后）

1. **未授权 servlet/jsp 命中 = 优先于一切**：致远/用友/泛微的免认证入口是"单请求终局"，比任何需登录的洞都值钱。
2. **SQLi/webshell 后先翻配置**：DB 连接串、支付商户密钥、云 AK/SK、内网地址——比业务数据更能拓线。转 `realworld-patterns.md`（支付/业务链）、`cloud-cn.md`（国内云）、`database.md`（DB 提权横向）。
3. **企业内多 OA 共存**：外网打下一个 OA（多为致远/泛微）→ webshell 内网扫其他 OA/ERP（用友 NC 的 BeanShell、通达 SQLi）→ 逐框架横向。转 `ad.md`（仅明确授权内网任务）。
4. **历史入侵留痕是信号**：webroot 里已有的陌生 JSP/PHP webshell、后台里 id 异常的 admin 账号 = 前人接管痕迹，也是可复用/可爆破的目标。
5. **证据闭环**：每个 confirmed 洞回溯 observation（指纹/未授权响应）→ reproduction（最小 PoC 请求）→ impact（读到的凭据/执行的命令回显）。Engagement 模式写入 `case/ledger.jsonl`。

---

*本目录为公开漏洞事实的提炼改写。CVE 为检索锚点，实战以一手指纹+版本确认为准。业务逻辑打法见 `realworld-patterns.md`。*
