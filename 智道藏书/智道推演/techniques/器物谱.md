---
name: product
description: 大爱仙尊·假设作业长文（product）。先专卡后读本文。
---

> 作业先走 MAP.md 对应专卡。本文件是长文速查，不替代专卡。

# 产品接管（ArgoCD / Harbor / Portainer / RabbitMQ）

> 仅在根路由选择本目录后读取。每个条目都是**具体 CVE 驱动的接管手法**，命中指纹后直接按步操作。

**触发**: 发现 ArgoCD/Harbor/Portainer/RabbitMQ 等产品暴露，目标中间件/云原生产品。

---

## ArgoCD

- **指纹**: `/api/v1/version` 返回 version 信息
- **CVE-2024-31989**（Critical 9.1）: Redis（6379 默认无认证）缓存投毒 → 恶意 repo/Application → **K8s 集群接管**；未授权 `/api/v1/applications` `/api/v1/clusters` 枚举
- **CVE-2022-29165**: SSO redirect_uri 绕过
- **CVE-2024-40638**: Webhook SSRF
- **接管**: 拿到 ArgoCD admin token → K8s 集群完全控制 → K8s 横向

## Harbor

- **指纹**: `/api/v2.0/systeminfo`；默认凭据 `admin/Harbor12345`（大量部署未改）
- **未授权镜像目录**: `/v2/_catalog` 列全部镜像
- **镜像分析**: pull + dive/skopeo 逐层分析提取 `.env`/`~/.ssh/id_rsa`/`~/.aws/credentials`
- **供应链投毒**: push 权限 → 后门 Dockerfile 注入（保持 ENTRYPOINT 不变 + LD_PRELOAD）
- **Webhook 外传**: 创建监听 PUSH_ARTIFACT 事件发到攻击者服务器
- **复制策略窃取**: 把全部镜像复制到攻击者 Registry
- **Robot Account 持久化**: `duration:-1` 永久系统级 Token
- **Trivy 漏洞报告窃取**: 从扫描结果找真实漏洞
- **升级**: 镜像凭据 → 云账号横向

## Portainer

- **指纹**: `/api/status` 返回 Portainer 状态
- **CVE-2024-29296**: 用户枚举时间差（无需认证）
- **CVE-2018-12678**: Request Splitting SSRF → Docker API RCE（≤1.17）
- **已认证后**: JWT → `/api/endpoints/{id}/docker/containers/create` 特权容器 + 挂载宿主根目录 → chroot 逃逸 RCE
- **升级**: 容器逃逸 → 宿主机 root

## RabbitMQ

- **指纹**: 5672（AMQP）/ 15672（Management API）；默认 `guest/guest`（`loopback_users = none` 时可远程）
- **Management API 利用**: 批量 dump 队列消息（`requeue:true` 不删原消息）→ 创建 administrator 后门用户 → Shovel/Federation 实时消息转发到攻击机
- **Erlang Cookie RCE**（最高危）: 读 `/var/lib/rabbitmq/.erlang.cookie` → `erl -sname attacker -setcookie STOLEN -remsh rabbit@TARGET` → `os:cmd("id")`
- **消息注入**: 篡改队列消息干扰业务逻辑（如改支付订单/越权操作）
- **升级**: 消息中的凭证 → 横向；Erlang shell → 服务器控制