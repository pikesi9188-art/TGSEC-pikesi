---
name: cloud
description: 大爱仙尊·假设作业长文（cloud）。先专卡后读本文。
---

> 作业先走 Skill `群瓮` / `cloud-metadata-harvesting`。本文件是长文速查，不替代专卡。

# Cloud Pentest

> 仅在根路由选择本目录后读取。每个条目都是待验证的检查假设；执行前用当前一手证据确认适用性。

**触发**: 目标运行在 AWS/Azure/GCP/阿里云或 K8s 环境。

## 领域决策直觉

1. 元数据服务（169.254.169.254）是云环境的第一个检查点——有访问就有角色凭证
2. IAM 权限枚举比传统端口扫描更有价值——知道"能做什么"比知道"开着什么"更直接
3. 容器逃逸不等于集群控制：先看 Pod 的 service account 权限再决定是否逃逸

---

## 云资产发现

### IMDS 元数据探测
- **信号**: 目标在 AWS/Azure/GCP 上运行（EC2/VM/GCE）
- **假设**: IMDS 可访问且返回临时凭证
- **验证**: `curl http://169.254.169.254/latest/meta-data/iam/security-credentials/` (AWS) / `curl -H "Metadata:true" http://169.254.169.254/metadata/identity/oauth2/token` (Azure) / `curl "http://metadata.google.internal/computeMetadata/v1/instance/service-accounts/default/token"` (GCP)
- **证实**: 返回有效的 IAM 角色临时凭证
- **升级**: 凭证 → IAM 权限枚举 → 权限提升路径

### IAM 权限枚举
- **信号**: 拥有云 API 凭证（Access Key/Token/Service Account Key）
- **假设**: 当前身份可能有过度的、非预期或可链接的权限
- **验证**: AWS——`enumerate-iam` 全权限暴力枚举 / `pmapper` 图论分析权限链 / `cloudsplaining` 识别过度权限。Azure——`az ad signed-in-user show` + 资源提供商枚举。GCP——`gcloud projects get-iam-policy` + `gcloud services list` + `gcloud auth application-default print-access-token`
- **证实**: 发现低权限身份可链式提升到管理员权限
- **升级**: 权限提升 → 跨账户访问 → 数据窃取

### 云存储枚举
- **信号**: 发现任何域名或 JS 中引用云存储 URL
- **假设**: 存在公开或弱权限的存储桶/容器
- **验证**: AWS S3 `aws s3 ls s3://bucket-name --no-sign-request` → Azure Blob `https://account.blob.core.windows.net/container` → GCP Storage `gsutil ls gs://bucket-name` → 阿里云 OSS `https://bucket.oss-<region>.aliyuncs.com`
- **证实**: 可未认证读取/写入存储对象
- **升级**: 存储内容分析 → 敏感信息提取 → 凭据发现 → 横向移动

---

## IMDS / 云身份 低影响验证纪律

> 上面的 IMDS/IAM 链拿的是凭据；这一节管**怎么验证到"控制缺失"而不过度触碰**——SSRF→IMDS 类发现最容易被"扫描器说能打"误报，也最容易在验证时越界（碰真凭据/跨租户/真实写入）。与全局证据链一致，此处给云特化的检查顺序与停止点。

**最低影响检查顺序**（从只读往上升，每步够用即停，不跳级）：
1. **只读配置**：metadata endpoint 协议版本/hop-limit/是否可改；实例·节点·工作负载附加身份及资源权限；PodSpec/ServiceAccount/automount/`hostNetwork`/特权/挂载；OIDC issuer·subject·audience·信任条件；SDK provider 顺序与静态凭据回退。
2. **静态分析**：跟踪用户可控 URL → 实际网络调用的解析/DNS/逐跳重定向；确认调用方**不能**任意设置 metadata/Host/Authorization/转发类请求头；确认日志/追踪/调试**不记录** Token 或完整凭据响应；确认 RS 仍执行 IAM/RBAC（不只信工作负载身份）。
3. **本地 mock**：只返回无害 marker 的本地 HTTP stub 验证 URL/重定向控制；mock credential provider 验证 SDK 选择顺序；合成声明验证 issuer/subject/audience 策略匹配；合成事件验证"配置→身份→API→出口"关联。
4. **隔离环境**：只用无敏感资源/无写权限的测试身份；验证无身份入口的主体连接被拒、错配 issuer/subject/audience 被拒；只确认**最终主体类别**，不输出 Token/签名材料/凭据响应；可写动作一律改草稿/临时/mock。

**观察→假设→证据**（每个检查先填，禁止把扫描器结论/单次错误/单次延迟/单请求/缺日志直接写成"已利用"）：观察（已读到的配置/策略/代码/事件）→ 假设（可能的身份路径/资源影响）→ 下一步最低影响检查 → 支持证据 / 否定证据 / 证据不足项。

**强制停止**（命中即保存非敏感证据并停，不靠扩权/切身份/找旁路继续）：即将访问凭据路径或展示真实秘密 / 即将访问其他租户·范围外账号·非测试资源 / 即将产生真实写入·发送·删除·改权限·持久化 / 测试身份意外获得超预期权限 / 结果无法归因（再做只会扩大范围）/ 已取得证明控制有效或缺失的最小证据。

**结论门**（同时满足才可写"已验证控制缺失"，否则写"风险假设"或"证据不足"）：目标与前置在授权范围内 · 结果可重复可归因 · 至少一个明确控制点未按预期工作 · 非纯扫描器推断或日志缺失 · 证据不依赖真实秘密或生产副作用。

**证据脱敏**：记录中不得出现真实 Token/Authorization Header/私钥/云密钥/完整 metadata 凭据响应/业务数据；每个发现记：授权范围、前置与已观察事实、待验假设、实际最小检查、时间·主体·资源·配置版本、支持/否定/不足判定、首个失效控制点、最高可能影响（不夸大）、已触发的停止条件、根因修复位置与复测 oracle。

（国内云 `100.100.100.200`/`169.254.0.23`/openstack 路径同一套纪律，见 `cloud-cn.md`。）

---

## 容器逃逸

### Copy Fail CVE-2026-31431（无特权逃逸）
- **信号**: 在容器中运行，无特权/无 capability，内核版本在受影响范围
- **假设**: Linux 内核 page-cache CoW 路径竞争条件可实现无特权容器逃逸
- **验证**: 三阶段——(1) AF_ALG + splice() 竞态污染内核 page cache 中的只读文件 (2) OverlayFS 共享层跨容器传播污染 (3) 特权 DaemonSet（kube-proxy 等）执行污染二进制 → 节点级代码执行。全程零权限/零 capability/磁盘取证免疫
- **证实**: 在宿主机节点上执行命令
- **升级**: 节点控制 → K8s 集群控制 → 云元数据访问

### 容器逃逸 11 种路径
- **信号**: 在容器内，需评估逃逸可能性
- **假设**: Docker socket/特权模式/挂载/cgroup 等配置提供逃逸路径
- **验证**: CDK 自动化评估——Docker socket 挂载 → Docker API → CVE-2019-5736 runC（/proc/self/exe 覆盖）→ CVE-2020-15257 containerd-shim → CVE-2022-0492 cgroup release_agent → procfs/设备挂载 → Copy-Fail CVE-2026 → LXCFS → Ptrace → CAP_DAC_READ_SEARCH 宿主机文件读取 → Cgroup 设备权限重写
- **证实**: 至少一种路径可用
- **升级**: 宿主机访问 → K8s 凭据窃取 → 集群控制

### badPods 五维风险分级
- **信号**: Pod 配置存在 privileged/hostPID/hostNetwork/hostIPC/hostPath 组合
- **假设**: 五维组合的风险等级直接对应攻击面
- **验证**: privileged + hostPID → nsenter -t 1 宿主机逃逸。hostPath kubelet 凭据 → /etc/kubernetes/admin.conf 窃取。hostNetwork → etcd/kubelet 流量嗅探
- **证实**: 成功获取宿主机 shell 或 kubelet 凭据
- **升级**: K8s 集群管理权限 → 所有 namespace 访问

---

## K8s 攻击链

### K8s 凭据与权限发现
- **信号**: 在 Pod 中获得 shell
- **假设**: ServiceAccount Token 挂载点可访问，权限可能超出预期
- **验证**: `cat /var/run/secrets/kubernetes.io/serviceaccount/token` → `kubectl auth can-i --list` → KubeHound 图论分析 SA/Role/ClusterRole/Secret 关系 → 寻找过度授权的 SA
- **证实**: Token 有超出当前 namespace 的权限
- **升级**: 跨 namespace 访问 → Secrets 窃取 → 集群管理

### Admission Webhook 后门
- **信号**: 已有 cluster-admin 或 MutatingWebhookConfiguration 创建权限
- **假设**: 可注入 Admission Webhook 使所有新建 Pod 自动携带后门
- **验证**: 创建 MutatingWebhookConfiguration → 规则匹配所有 Pod 创建 → 注入 Sidecar 容器（hostPath 挂载 + 特权）→ 所有新建 Pod 自动携带后门
- **证实**: 新创建的 Pod 自动包含后门容器
- **升级**: 集群级持久化 → 隐蔽长期访问

### etcd 直接访问 → RBAC 篡改
- **信号**: etcd 端口 2379 未授权或可凭据访问
- **假设**: 直接修改 etcd 中 RBAC 数据可完全绕过 API Server 审计
- **验证**: etcd 未授权访问 → etcdctl 直接读写 /registry/rbac/ → 修改 cluster-admin ClusterRoleBinding 添加攻击者 SA → API Server 完全无审计记录
- **证实**: SA 获得 cluster-admin 且 kubectl 审计日志为零
- **升级**: 集群完全控制 + 审计隐身

### Shadow API Server 持久化
- **信号**: 需要长期隐蔽的 K8s 集群控制
- **假设**: 可在集群内 MITM kube-api 流量
- **验证**: MITM ClusterIP 实现 Shadow API Server → 拦截并修改 kube-api 请求/响应 → 隐身代理
- **证实**: 正常 kubectl 流量被透明劫持且不触发告警
- **升级**: 长期集群控制 → 所有操作完全隐蔽

---

## 云持久化与后门

### AWS IAM 信任策略后门
- **信号**: 已获 AWS 高权限访问
- **假设**: 可创建或修改 IAM Role 信任策略，嵌入攻击者 AWS 账户 ARN
- **验证**: 创建合法 Role → 嵌入攻击者 AWS 账户 ARN 的 Trust Policy → 附加 AdministratorAccess → 创建 Access Key 导出 → 或修改现有 Role 信任策略注入
- **证实**: 从攻击者账户成功 sts:AssumeRole 获取管理员权限
- **升级**: 跨账户访问 → 持续数据窃取

### sts:GetFederationToken 密钥删除后存活
- **信号**: 使用 Access Key 访问。管理员可能轮换/删除 Key
- **假设**: GetFederationToken 生成的 Token 独立于原始 Key 生命周期
- **验证**: 在 Access Key 有效期内调用 GetFederationToken → 生成独立 Token 最长 36 小时 → 管理员删除/轮换原 Access Key 后 Token 仍有效 → 用存活 Token 建立新持久化入口
- **证实**: 原 Key 被删除后，Token 仍可执行 AWS API 调用
- **升级**: 应急持久化 → 创建新 IAM 用户或 Role 后门

### Lambda Serverless 后门
- **信号**: 目标使用 AWS Lambda
- **假设**: Lambda Extension 作为独立进程可持久化后门且不修改函数代码
- **验证**: 恶意 Extension 注入（独立进程，不修改函数代码）→ Lambda 运行时 API 劫持 → EventBridge 规则劫持 → Function URL 后门 AuthType NONE 直接 HTTPS 调用
- **证实**: Lambda 冷启动后 Extension 仍执行恶意逻辑
- **升级**: Serverless 持久化 → 事件驱动数据窃取

### Azure AD Connect 密码提取
- **信号**: 目标使用 Azure AD Connect 同步本地 AD 到 Azure
- **假设**: AAD Connect 服务器存储同步凭据
- **验证**: 从 AAD Connect 服务器提取同步凭据 → Kerberos 票据转向云资源 → Azure 资源访问 → 云环境持久化后门创建
- **证实**: 成功用同步凭据访问 Azure 资源
- **升级**: Azure 订阅控制 → 所有云资源访问

---

## 2026 云提权与绕过（2026 增量）

### AWS IAM 7 条提权链
- **信号**: 拿到 AWS 密钥但权限有限
- **验证**:
  1. `iam:CreatePolicyVersion` 覆盖（创建带 `*:*` 的新版本策略 → 自身提权）
  2. `iam:PassRole` + `lambda:CreateFunction`（传递 AdminRole → Lambda 执行 → 提权）
  3. `iam:PassRole` + `ec2:RunInstances`（带 Instance Profile 启动 → 元数据拿高权角色）
  4. `sts:AssumeRole` 链式、`iam:AttachUserPolicy`、`iam:CreateAccessKey`、`iam:UpdateLoginProfile`
- **验证流程**: `enumerate-iam` 全权限枚举 → 定位可提权权限 → 每条链给出完整 bash 命令（权限发现→利用→验证）
- **证实**: 权限提升到管理员
- **升级**: 跨账户访问 → 数据窃取

### IMDSv2 绕过
- **信号**: 应用 SSRF 存在但目标仅启 IMDSv2（需 PUT 令牌）
- **验证**: Axios CVE-2026-40175（header injection 使 GET 模拟 PUT）→ IPv6 `[fd00:ec2::254]` 绕过 → DNS rebinding 绕过
- **证实**: 绕过 IMDSv2 拿到临时凭证
- **升级**: IAM 角色接管

### CloudTrail/GuardDuty 绕过
- **信号**: 需隐身操作（避免日志记录）
- **验证**: `aws cloudtrail delete-trail` / `stop-logging`；`guardduty stop-monitoring`；创建可信 IP 集；使用 VPC 端点绕过 DNS 分析
- **证实**: 操作不留审计日志
- **升级**: 隐蔽持久化

### Azure Managed Identity 令牌窃取 + Key Vault 操纵
- **信号**: 在 Azure VM/App Service/Function 中
- **验证**: `curl -H "Metadata:true" 169.254.169.254/metadata/identity/oauth2/token` → 枚举订阅/资源 → VM 扩展命令执行；`az keyvault set-policy --object-id $ATTACKER_OBJECT_ID --secret-permissions get list` 给攻击者加 Key Vault 权限
- **证实**: 拿到 Azure AD 令牌 / 读取 Key Vault 机密
- **升级**: 云资源横向

### 云原生资产发现（Aurelian + CloudFox）
- **验证**: `aurelian public-resources --all --out aurelian_public.json` + `aurelian iam-analyze --neo4j` → Cypher 查询 privesc 路径；覆盖 AWS(16模块)/Azure(5)/GCP(4)，含 CloudTrail 隐身份查询
- **升级**: 多云资产全景 → 找最弱路径

---

## 语言/系统沙箱逃逸

> 与"容器逃逸"互补——目标是 Python/Lua 沙箱、seccomp 过滤器、chroot 而非容器。

### Python 沙箱逃逸（Pyjail）
- **信号**: 在线判题/受限 Python，`exec`/`eval` 存在但 builtins 被阉割
- **验证**: 核心链 `().__class__.__bases__[0].__subclasses__()` → 找 `os._wrap_close.__init__.__globals__['system']('sh')` → 关键字过滤用 `getattr`+`chr()` 拼接 → 括号过滤用 `@exec` 装饰器 → 引号过滤 `bytes([111,115]).decode()` → AST 级过滤：`ast.Call` 被禁用 f-string 或 `__init_subclass__` 类定义侧信道 → 15 维判断清单（builtins/eval/keyword/underscore/dot/parentheses/quote 各自限制）
- **证实**: 执行任意 Python/系统命令
- **升级**: shell → 服务器控制

### Seccomp 绕过
- **信号**: `prctl(PR_SET_SECCOMP)`/BPF 过滤 syscall
- **验证**: **架构混淆**（64 位进程 `int 0x80` 调 32 位 syscall，execve=11 vs 59）→ ORW 链（open/read/write 三段）→ `sendfile(1,3,NULL,0x100)` 替代 read+write → io_uring 绕过（内核 5.1-5.11，≥5.12 已修复）→ ptrace fork 注入 → `openat(AT_FDCWD,"flag",O_RDONLY)` 当 open 被禁
- **证实**: 在 seccomp 限制下读取 flag/执行命令

### chroot 逃逸
- **验证**: 双 chroot（`chroot . /bin/sh` 后再 chroot 到更上层，配合 `mkdir foo; chroot foo; cd ..`）→ 需 root 或 CAP_SYS_CHROOT；有文件描述符指向 chroot 外目录时直接跳回
- **升级**: 脱离 chroot 边界
