---
name: telegram
description: 大爱仙尊·假设作业长文（telegram）。先专卡后读本文。
---

> 作业先走 Skill `太白云生·云台`；TMA/白标再交接家族卡。本文件是长文速查，不替代专卡。

# Telegram / TG 生态

> 仅在根路由选择本目录后读取。每个条目都是待验证的检查假设；执行前用当前一手证据确认适用性。

**触发**: 目标为 TG Bot / Mini App (TMA) / 云控后台 / 接码平台 / 群管系统 / 使用 TG OAuth 登录的 Web 服务 / 有 bot_token 的页面。

## 领域决策直觉

1. **bot_token 泄露是最高频突破口**——前端 JS 常硬编码，拿到即完全控制 bot（`getMe`/`setWebhook`/`sendMessage`）
2. **initData 验证边界是 TMA 后端唯一认证**——很多开发者只校验 hash 存在不校验有效性，或直接信任前端传的 user_id
3. TG 云控/接码/群管后台通常有独立 Web 面板，但 token/bot 关联数据是关键横财
4. 支付类（Stars/充值卡密）测并发与回调伪造（见 web.md 竞态/业务逻辑）
5. 800 万+月活的信任模型 = 用户信任 TG 内的任何 Mini App——钓鱼面极广

---

## Mini App (TMA) initData 伪造

### initData 验证绕过
- **信号**: 目标 TMA 后端用 initData 认证，前端传 `initData`/`initDataUnsafe`
- **假设**: 后端未校验 HMAC 签名、未校验 auth_date 过期、或只信 `initDataUnsafe`（名字即警告：unsafe，仅供 UI）
- **验证**:
  1. 拦截 TMA 请求 → 看后端是否信任 `initDataUnsafe.user.id`（跳过签名）→ 直接改 user_id 越权
  2. 拿到 bot_token（前端 JS 硬编码/泄露）→ 用 HMAC-SHA256 算法伪造 initData：`secret_key = HMAC_SHA256("WebAppData", bot_token)`，`hash = HMAC_SHA256(secret_key, sorted_params_str)` → 可伪造任意 user_id + is_premium=true
  3. 检查 auth_date 过期校验：重放旧 initData 或用当前时间重新签名
- **证实**: 以任意 user_id 身份访问后端 API / 提权为 admin
- **升级**: 越权 → 全用户数据 → 管理员接管
- **工具**: `InitDataForger`（已知 bot_token 时 forge/replay）

### WebView XSS（CVE-2024-33905）
- **信号**: TMA 使用 `web_app_open_link` 或 postMessage 通信
- **假设**: 可注入 postMessage 数据 → XSS 窃取 TMA 会话/token
- **验证**: 注入恶意 postMessage → 窃取 tgWebAppData / 会话 cookie → 1 次点击劫持会话
- **证实**: 在 TMA WebView 中执行 JS
- **升级**: 会话劫持 → 账户接管

### tgWebAppThemeParams 注入 / 平台伪造
- **信号**: TMA 前端读取 `tgWebAppThemeParams` 或检测 `tgWebAppPlatform`
- **假设**: 注入恶意 theme 参数触发 XSS 或 CSS 注入；伪造平台值绕过平台限制（伪装成 ios/android 解锁隐藏功能）
- **验证**: 改 `tgWebAppPlatform=android` 触发仅移动端逻辑 → 注入 theme JSON 含 `<img onerror>` 测试
- **证实**: 平台限制被绕过 / 注入执行

---

## Bot Webhook & Token

### bot_token 泄露 → 完全控制
- **信号**: 页面/JS/配置含 `\d{8,10}:[A-Za-z0-9_-]{35}` 模式
- **验证**: `curl https://api.telegram.org/bot<TOKEN>/getMe` → 返回 `{"ok":true,...}` 即有效 → `getWebhookInfo` 泄露服务器 URL + pending_update_count → `setWebhook` 改到自己的服务器接管更新 → 发任意消息/操作
- **证实**: getMe 返回 ok:true
- **升级**: 完全控制 bot → 读用户消息（若 bot 有读取权限）→ 伪装运营者
- **关联**: 从 epay 页面/交易平台源码/前端 bundle 扫 token：`grep -rE "[0-9]{8,10}:[A-Za-z0-9_-]{35}"`

### Webhook 请求伪造（CVE-2026-25474）
- **信号**: 目标 bot 服务器 webhook 无 secret token 校验
- **假设**: 攻击者可伪造 TG Update 发往 webhook → 触发未授权 bot 操作
- **验证**: 构造伪造 Update JSON POST 到 webhook URL → 无校验则 bot 执行攻击者控制的命令/操作
- **证实**: bot 响应了伪造 update（如发送消息/改配置）
- **升级**: 未授权操作 → 诈骗/资金转移
- **三层防护检查**: setWebhook 是否带 secret_token → 收到 update 是否校验 X-Telegram-Bot-Api-Secret-Token → 是否校验 update 内容合理性

### SSRF via setWebhook
- **信号**: 拿到 bot_token，目标服务器接受任意 webhook URL
- **假设**: Telegram 服务器会 POST 到 webhook URL → 可用于探测内网（响应不回显，但 timing/status code 可端口扫描）
- **验证**: `setWebhook -u http://internal-service.local:8080/admin` → 观察响应差异（accepts non-HTTPS/internal URL?）
- **证实**: 目标接受非 HTTPS/内网 webhook URL
- **升级**: 内网探测 → 盲 SSRF

---

## 支付 / 诈骗链

### Telegram Stars 退款欺诈
- **信号**: TMA 卖 Stars 礼品/虚拟商品
- **假设**: buy → 转赠 → 退款 Stars → 两者都保留
- **验证**: 购买 → 转移 gift → 申请退款 → 检查是否 Stars 已退且 gift 仍在
- **证实**: 退款后 gift 仍可用
- **升级**: 免费获取付费功能（~$100K+ 真实损失）

### 充值卡密 / 代理支付回调
- **信号**: TMA 接卡密充值或代理支付
- **假设**: 支付回调签名可伪造（见 web.md 业务逻辑/签名条目）
- **验证**: 重放回调 / 改金额 / 伪造签名 → 卡密免费到账
- **证实**: 未支付获得卡密/余额
- **升级**: 免费充值 → 资金损失

---

## TON 区块链（如有）

### TON Connect 2.0 四攻击向量
- **信号**: TMA 集成 TON Connect 钱包
- **假设**: 克隆 dApp / 剪贴板篡改 / deep link 注入 / 持久会话可窃取授权
- **验证**: 伪造克隆 dApp 诱导授权 → 篡改地址 clipboard → 恶意 deep link 覆盖请求 → 持久 session 复用
- **证实**: 拿到用户钱包授权/签名
- **升级**: 资产盗取

### TON Bridge 跨链（代码-hash 绕过）
- **信号**: TON 桥/TAC Bridge 类跨链
- **假设**: 智能合约代码-hash 验证可绕过
- **验证**: 构造 payload 绕过 code-hash 检查 → 假交易跨链（TAC Bridge $2.85M 真实攻击）
- **证实**: 跨链转移伪造资产
- **升级**: 资金损失

### TONResolver RAT（TON 合约作 C2）
- **信号**: 恶意样本 TON 智能合约通信
- **假设**: TON 合约地址不可变 → 用作持久 C2 服务器（成本极低不可封禁）
- **验证**: 分析样本找 TON 合约地址 → 合约读指令/写数据
- **证实**: C2 指令下发正常
- **升级**: 对抗性 C2 基础设施识别

---

## MTProto 协议层

### auth_key_id 明文去匿名化（GNMX-01）
- **信号**: 分析 MTProto 流量 / 云控基础设施
- **假设**: auth_key_id 作为明文 ID 可关联同一用户多次会话
- **验证**: 抓包看 MTProto 头 auth_key_id 是否明文 → 跨会话关联
- **证实**: 不同时间/设备流量可关联到同一 auth_key
- **升级**: 用户去匿名化 / 会话追踪

### 消息重排序攻击
- **信号**: MTProto 加密流中间人位置
- **假设**: 加密但无 MAC 的帧可被重排序（encrypt-and-MAC 缺陷）
- **验证**: 重放/重排序捕获的消息帧 → 观察接收端行为
- **证实**: 消息顺序被篡改影响状态
- **升级**: 状态机操纵 → 业务逻辑绕过

---

## TG 生态横财速查（对你目标类型通用）

| 目标类型 | 高价值检查 |
|---------|-----------|
| 云控后台 | bot_token/api_id/api_hash 硬编码 → getMe 验证 → 全号控制 |
| 接码平台 | 订单 IDOR、验证码泄露、admin 面板、短信通道凭据 |
| 群管系统 | 群组权限 IDOR、广告任务越权、接入码泄露 |
| TG OAuth 登录 | OAuth redirect_uri 校验、code 交换、state 缺失 |
| 号商/批发 | 卡密 IDOR、库存导出、支付回调伪造 |
| TMA 游戏/抽奖 | Stars 双花、中奖概率操纵、好友邀请竞态 |
