---
name: auth-modern
description: 大爱仙尊·假设作业长文（auth-modern）。先专卡后读本文。
---

> 作业先走 MAP.md 对应专卡。本文件是长文速查，不替代专卡。

# 现代认证与传输安全（Passkey/WebAuthn + TLS 1.3 0-RTT + OAuth2/OIDC）

> 补全工具箱三个空白：**无密码认证(Passkey/WebAuthn)依赖方审计**、**TLS 1.3 0-RTT 重放**、**OAuth2/OIDC 授权码流与令牌重放(RFC 9700)**。三者共性：「配置项存在 ≠ 漏洞成立」——必须打通到业务副作用/认证决定才算数。授权敏感，默认只做代码审计/配置分析/loopback 验证，禁生产重放。

## 何时读这个文件
- 目标登录出现 `navigator.credentials`、`publicKey`、`webauthn`、`/webauthn/register|login`、FIDO2、指纹/faceID 登录 → **Passkey 审计**（§一）
- 目标启用 TLS 1.3 且返回 `Early-Data` 头、Nginx `ssl_early_data on`、CDN/QUIC 0-RTT、`425 Too Early` → **0-RTT 重放**（§二）
- 目标用第三方登录/SSO（`/oauth/authorize`、`/login/oauth`、`code`/`state`/`redirect_uri`、OIDC `id_token`）且需**审实现**而非仅打 redirect_uri → **OAuth2/OIDC 审计**（§三；打 redirect_uri 攻击面先看 `web.md`）

---

## 一、Passkey / WebAuthn 依赖方(RP)审计

**核心认知**：WebAuthn 的安全不来自「浏览器弹出了生物识别窗口」，而来自依赖方**服务端**完整验签。浏览器 API 成功只表示客户端拿到一个响应，最终认证决定必须由服务端密码学验证做出——前端布尔值/设备名/UI 文案都不能替代。这就是攻击面所在：**服务端漏验某一步 = 绕过。**

**边界澄清（别打错方向）**：
- WebAuthn 不是授权模型——登录成功后仍要逐请求做服务端授权（IDOR 照样打，见 realworld-patterns A1）
- passkey 不修复 XSS/会话固定/Cookie 配置/终端失陷——这些仍是独立攻击面
- 本地生物识别只是「激活认证器」，服务器从没拿到生物模板
- ceremony 抗钓鱼 ≠ 短信/客服/邮件**恢复路径**抗钓鱼 → **恢复流程常是最软肋**

### 1.1 服务端必须验证的 10 步（缺任一即漏洞）

注册(`webauthn.create`)与认证(`webauthn.get`)服务端都要过这些，审计时逐条问「代码里有吗」：

1. `clientDataJSON.type` 结构正确（create/get 不能混）
2. **challenge 精确匹配**本次服务端保存值，未过期/未消费/上下文一致
3. **origin 精确匹配**允许列表——不做子串/后缀/正则宽匹配 ← 高频缺陷
4. authenticator data 里 **`rpIdHash == SHA256(预期RP ID)`**
5. UP 已置位；策略要求多因素时 **UV 也必须置位**（`userVerification:preferred` 不保证每次返回 UV ← 常见误配）
6. 公钥算法在本次 `pubKeyCredParams` 允许列表内
7. 扩展输出符合请求与策略
8. attestation 按策略验证（允许 none 时也要完成格式+authData 校验）
9. **credential ID 归属校验**：认证时证明 credential ID 属于当前账号；用户名无关流程必须用 `userHandle` 找账号并验证归属一致——**不能因为签名有效就把签名者映射到请求里的任意账号** ← 账号接管点
10. 签名用该 credential ID 对应**已存公钥**验证，覆盖 `authenticatorData || SHA256(clientDataJSON)`

### 1.2 攻击者视角的高价值缺陷点

| 缺陷 | 表现 | 后果 |
|------|------|------|
| **challenge 可重放** | 同一 challenge 未一次性消费/未绑 ceremony 类型/多实例无共享原子消费 | 抓包重放断言登录 |
| **origin 宽匹配** | 服务端用 `endsWith`/正则/子串比 origin | 钓鱼子域/同后缀域伪造断言 |
| **RP ID 作用域过宽** | rpId 设为顶级域，子域可发起 | 跨子域凭据滥用 |
| **credential 归属不校验** | 认证只验签名有效，不验 credential↔账号绑定 | 用自己的 passkey 签名 + 改 userHandle/账号 → 登入他人 |
| **UV 未强制** | 业务当多因素用但 `userVerification` 非 required | 仅 UP(在场)即通过，降级为单因素 |
| **signCount 处理错误** | 把 0/回退当硬失败或完全不查 | 误伤或漏检克隆 |
| **新增/删除凭据无再认证** | 仅凭旧 Cookie 就能加新 passkey | 会话劫持后加自己的凭据永久接管 |
| **恢复路径弱** | 恢复回退到短信/邮件/客服 | 绕过整个抗钓鱼链 ← 优先打这里 |

### 1.3 最小回归集（审计验证用）
- type/challenge/origin/RP ID 任一错配 → 必须失败且状态不变
- 同一响应第二次提交 → 必须失败（重放防护）
- credential ID 与账号/`userHandle` 归属错配 → 必须失败
- UV 必需时只有 UP 的响应 → 必须失败
- 签名/公钥/算法错配 → 失败
- 注册并发 + challenge 并发 → 只能成功一次
- 新增/删除凭据仅有旧会话无再认证 → 必须失败

### 1.4 生命周期 / 恢复 / 隐私（策略层审计清单，补 §1.2 攻击视角）

§1.2 打的是验签绕过；这一层查的是**治理缺陷**——常是账号接管的真实落点（尤其恢复路径）。

- **RP 配置硬化**：RP ID / 允许 origin **来自服务端固定配置**，不接受请求参数、`Host` 头或未受信代理头覆盖 ← Host 头注入改 RP ID 是宽匹配之外的第二条路；COSE 算法策略由服务端定，不由客户端 `pubKeyCredParams` 决定。
- **恢复流程硬化（最软肋，优先审）**：恢复不依赖安全问题、不仅凭当前已劫持会话；恢复码随机+哈希存储+限速+短时/离线+**用后失效**；恢复成功后**重新评估旧凭据与现有会话**（不能只加新 passkey 就放行）；高价值账号加延迟/人工复核/职责分离；同步型 passkey 的 provider 账号恢复纳入威胁模型。
- **凭据生命周期治理**：删除/替换/恢复接口执行 CSRF + 服务端授权 + 再认证；同一 credential ID 不能并发绑多账号；失窃后能立即撤销具体 credential ID；删除**最后一个**强认证器有额外确认；新增/删除/恢复/撤销走**独立渠道通知**用户；用户可查看凭据名称/创建时间/最近使用并安全撤销。
- **日志与隐私**：日志**不保存**私钥、生物样本、完整 assertion、完整 challenge 或无用途设备指纹；attestation/AAGUID/BE·BS/使用历史有用途说明+访问控制+保留期限。
- **审计结论三分法**：**阻断项**（缺失即认证绕过/跨账号绑定/可重放，上线前必修）/ **策略项**（取决于业务保证级别，但须有明确决策与测试）/ **风险信号**（不单独定性，进风险评分/告警）。

---

## 二、TLS 1.3 0-RTT (Early Data) 重放

**要证明/否定的完整风险路径**（只发现配置项不算漏洞）：

```
0-RTT 已启用 → 请求可进入 Early Data → 反重放/回退边界失效 → 同一业务意图产生重复副作用
```

**四条基线事实**（先立住，避免误报）：
1. TLS 1.3 当前根规范 RFC 8446/9846；0-RTT **不保证**跨连接不可重放（协议本身就允许重放，防护在应用层）
2. TLS 层直接复制 与 客户端重试造成的业务重复，是两类不同问题
3. HTTP 的 Safe/Idempotent 语义、`Early-Data: 1` 头、`425 Too Early` 都**不能替代**业务副作用审计
4. 「名义 GET」可能在实现里写库/发消息/改权限——方法名不代表副作用

**数据流（每条边都要问失败时如何处理）**：
```
恢复凭据 → TLS/QUIC 接受决定 → 代理风险标记 → HTTP 路由策略 → 应用幂等 → 副作用
```
失败行为分四种：拒绝 / 返回 425 / 等待完整握手 / **错误放行**（← 漏洞）。

### 2.1 五个优先攻击面

| 面 | 失败条件（漏洞信号） | 最低证据 |
|----|---------------------|---------|
| **A. 启用状态/默认值** | 运营方以为关了但 downstream 开着；上层代理覆盖了 TLS 库的 AntiReplay；up/down stream 配置混淆 | 最终生效配置+组件版本+TLS库版本+连接方向 |
| **B. 路由语义** | 仅按方法名允许 Early Data；名义 GET 却消费令牌/写库/发消息/改权限；PUT/DELETE 因「幂等」被当 Safe | 路由到副作用代码的调用链或合成计数器 |
| **C. 票据/反重放状态** | 只共享 ticket key 不共享**已用状态**；external PSK/session cache 绕开内建保护；cache TTL/票据有效期/接受窗口不一致 | 状态模式+原子消费位置+作用域+故障行为 |
| **D. 多节点/重启/故障切换** | 同一恢复凭据在多节点各接受一次；新实例把空缓存当全新鲜；replay store 不可用时继续接受 | 合成标签在不同实例的接受日志+启动代次 |
| **E. Early-Data 传播** | 代理不打 `Early-Data:1`；后端不据此对非幂等请求返回 425；风险标记在链路中途丢失 | 各跳请求头透传情况 |

### 2.2 授权与安全边界（重要）
- 目标是**证明或否定风险路径**，不是收集真实票据或做公网重放工具
- 禁区：生产、副作用路由、真实 session ticket/PSK/Cookie、用户流量、第三方、DoS
- 允许时只做：loopback/容器单次恢复验证、合成对象、配置/源码/日志字段分析
- 范围不完整 → 降级为纯文档/源码/配置/日志分析

### 2.3 快速指纹
- 响应头 `Early-Data`、状态码 `425 Too Early`
- Nginx `ssl_early_data on;`、Cloudflare/CDN 0-RTT 开关、`GET /` 带 early data 扩展的 ClientHello
- QUIC/HTTP3 目标默认更可能启用（见 web.md 协议降级绕 WAF 一节，两者联动）

---

## 三、OAuth 2.0 / OIDC 授权码流与令牌重放（RFC 9700 审计）

> 据 RFC 9700/7636/9207/9449/9126 + OIDC Core。**与 web.md 分工**：`web.md` OAuth 条目打**攻击面**（`redirect_uri` 绕过矩阵/open redirect 中转/邮箱预关联接管）；本节做**流与令牌的防护/代码审计模型**（PKCE 降级、Mix-Up、audience、刷新轮换、DPoP、ID Token 校验）。两边互补，打 OAuth 先看 web.md 的 redirect_uri 矩阵，审 OAuth 实现看这里。

**核心认知**：授权码流的安全本质 = 把一次浏览器授权交易**绑定**到正确的客户端实例、用户代理、授权服务器(AS)、资源服务器(RS)，并让任何泄露的 code/token 被另一方重放时失效。「签名有效」「令牌看起来对」都不是终点。

**令牌边界（别混用，混用即漏洞）**：Authorization Code=一次性兑换凭据；Access Token=「能访问什么」（给 RS）；ID Token=「AS 认证了谁」（给客户端，OIDC）；Refresh Token=续期。**Access Token ≠ 登录证明**（不能拿它建登录会话）；**ID Token ≠ API Bearer**（不能拿它调业务 API）。

### 3.1 审计核对清单（信号→缺陷→后果）

| 控制点 | 缺陷信号（代码审计红旗） | 后果 |
|--------|------------------------|------|
| **流选型** | 用 Implicit（token 进授权响应）/ ROPC（收用户密码） | 令牌注入/泄露/凭据暴露面扩大 |
| **PKCE 绑定** | 无 PKCE、用 `plain`、**授权请求无 challenge 但令牌请求带 verifier 却放行**（降级） | 截获的 code 被注入/兑换 |
| **state/nonce/PKCE 职责混用** | 用一个参数机械替代三者；`state`/`nonce` 固定或低熵或并发共用 | CSRF / ID Token 重放 / 交易错绑 |
| **redirect_uri 匹配** | `contains`/`startsWith`/正则通配/只比域名（非**精确字符串**匹配） | 授权码/令牌被劫持到攻击者端点（详见 web.md 绕过矩阵） |
| **发行者绑定(Mix-Up)** | 多 AS 客户端不校验 RFC 9207 `iss`、由回调参数选 token endpoint | code 被发往攻击者 AS 的令牌端点 |
| **audience 限制** | RS 只验签名，不验 `aud`/scope/业务对象权限 | 令牌被错误 RS 收集后转用 |
| **令牌重放价值** | 纯 Bearer、无 DPoP/mTLS 发送者约束、令牌长寿命 | 泄露即可被任意持有者重放 |
| **刷新令牌** | 公共客户端刷新令牌无轮换、轮换后不追踪令牌族(family) | 泄露的刷新令牌无限续期，重用不可检测/止损 |
| **307 跳转** | 处理含凭据的 POST 后用 307（保留方法+体）跳客户端 | 凭据被转发到客户端 |
| **OIDC ID Token** | 不验 `iss` 精确/`aud` 含本客户端/`exp`/`nonce` 一次性/UserInfo `sub` 一致 | 身份伪造/令牌错用 |

### 3.2 最小验证集（低影响，仅授权/自有/隔离环境；达到预期拒绝证据即停）

- `redirect_uri` 的 scheme/host/path/query 任一非允许差异 → 必须拒绝（不得前缀/后缀/通配放行）。
- 同一授权码第二次兑换 → 必须失败；用错误 `code_verifier` 兑换 → `invalid_grant`。
- 授权请求无 challenge、令牌请求带 verifier → 必须拒绝（防 PKCE 降级）。
- `state`/`nonce` 不匹配/已用/过期 → 终止且**不得先建会话**。
- 多发行者客户端收到错误/缺失/冲突 `iss` → 终止，code 不发往任何令牌端点。
- RS 收到错误 `aud` 或超 scope 的令牌 → 拒绝。
- 刷新令牌轮换后重放旧值 → 触发令牌族链级撤销（不是只返回错误）。
- DPoP proof 的签名/`htm`/`htu`/时间窗/`jti`/`ath`/密钥绑定任一不符 → 拒绝。
- 原生应用不得用内嵌 WebView 代替外部用户代理；公共原生客户端必须 PKCE。

### 3.3 检测与遥测

- **日志**（脱敏）：交易 ID 不可逆摘要、client_id、issuer、失败阶段、错误类别、重放判定、策略版本；**禁记**完整 code/token/verifier/nonce/client_secret/DPoP 私钥。
- **高价值告警**：同一 code 或刷新令牌摘要重复使用；redirect/issuer/audience/PKCE method/DPoP 校验异常突增；同一授权链短时来自不兼容客户端实例；token endpoint 出现「有 verifier 但原交易无 challenge」；RS 大量 wrong-audience 令牌。
- **常见误报**：用户刷新回调页导致的重复兑换（仍拒第二次，按交易 ID 降噪）；移动端 loopback 回调端口变化是 RFC 8252 例外（非任意 redirect）；可信代理重写 scheme/host 造成 issuer/redirect 异常 → 修代理边界，不是放宽匹配。
- **不完整修复反例**：只加 `state` 不校 redirect/issuer/PKCE；有 client_secret 就关 PKCE；轮换刷新令牌但不追踪令牌族；上 DPoP 后取消 HTTPS/audience/XSS 防护。

---

## 关联
- Passkey 登录成功后的授权仍要打 → [realworld-patterns 附录A A1 IDOR / B 认证会话]
- OAuth `redirect_uri` 绕过矩阵 / open redirect 站内中转 / 邮箱预关联接管（攻击面）→ `web.md` OAuth 2.0 / SSO 攻击
- 0-RTT 与 HTTP/3 QUIC 绕 WAF/IP 白名单联动 → `web.md` 协议降级
- 恢复路径的短信/邮件软肋 → `social-engineering.md`
