---
name: crypto-attacks
description: 大爱仙尊·假设作业长文（crypto-attacks）。先专卡后读本文。
---

> 作业先走 Skill `宝黄天·器匣` / `crypto_decode.py`。JWT 利用仍走 `jwt-bypass-pentest`。

# Crypto Attacks（密码学攻击）

> 仅在根路由选择本目录后读取，且仅限明确授权的 CTF/密码学任务。每个条目都是待验证的检查假设。

**触发**: 目标含加密数据/签名/Cookie 密文/CTF 密码题，或需分析 RSA/对称加密/哈希/古典密码。

## 领域决策直觉

1. **先看信息再选攻击**——不盲目跑工具：知道 n 多大？几个 n？e 大不大？有无 oracle？决定用哪条链（见各决策树）
2. 工具链：`factordb.com` / yafu / msieve（分解）→ `RsaCtfTool` / SageMath / sympy（代数）→ `hashpump` / fastcoll（哈希）→ CyberChef（编码）
3. Web 场景的对称密码误用见 web.md「Web 密码学误用」条目；本篇聚焦数学层攻击

---

## RSA 攻击

### 决策树（按已知信息选）
- **n < 512 位**: 直接分解 factordb.com → yafu → msieve
- **多个 n**: Batch GCD 找共享素数（共因子 → 一次分解全部）
- **e 小**:
  - e=3 单密文小消息 → 立方根攻击（`RsaCtfTool --attack cube_root`）
  - e=3 多密文不同 n → **Håstad 广播攻击**（中国剩余定理）
  - 两条相关消息 → **Franklin-Reiter** 相关消息攻击
  - 部分明文已知 → **Coppersmith** stereotyped message
- **e 极大** → d 小 → **Wiener**（连分数）→ 不行再 **Boneh-Durfee**（格，d < N^0.292）
- **同 n 两个不同 e** → 共模攻击（Bezout 系数）
- **已知部分 p 位** → Coppersmith partial key；**p-1 B-smooth** → Pollard p-1；**p≈q** → Fermat 分解
- **有 oracle**:
  - 奇偶 oracle（LSB）→ **LSB oracle** 逐位恢复明文
  - PKCS#1 v1.5 填充有效性 oracle → **Bleichenbacher**
  - OAEP oracle → **Manger's attack**
- **RSA-CRT 故障攻击**: 相同消息两次签名（一次注入错误）→ gcd 恢复 p/q
- **工具**: `RsaCtfTool --publickey key.pub --uncipherfile flag.enc`（自动选攻击）/ `owiener`（Wiener）/ `sympy` / SageMath `small_roots()`（Coppersmith）

---

## 对称密码攻击

### 快速攻击选择
| 现象 | 弱点 | 攻击 |
|------|------|------|
| 相同明文 → 相同密文（块对齐） | ECB | cut-and-paste / byte-at-a-time |
| 填充错误可区分 | CBC | Padding Oracle（免密钥解密） |
| 改密文影响下一块 | CBC 无完整性 | 比特翻转 |
| 密钥复用 + XOR/流密码 | Two-time pad | 密文 XOR 得明文 |
| 可预测 PRNG | LCG/LFSR | 状态恢复（Berlekamp-Massey） |
| 双重加密 | 2DES 类 | 中间相遇（Meet-in-the-middle） |

### 各攻击要点
- **CBC Padding Oracle**: 服务端对填充错误给出可区分响应 → 逐字节解出任意密文（工具 padbuster，见 web.md Web 密码学误用）
- **CBC 比特翻转**: 篡改第 N 块密文 → 翻转第 N+1 块明文（role=0→1）
- **ECB cut-and-paste**: 相同明文块 → 相同密文块，切块重排伪造数据
- **ECB byte-at-a-time**: 逐字节推密文恢复未知明文
- **流密码密钥复用**: `c1 XOR c2 = m1 XOR m2` → 已知明文推其他

---

## 格密码攻击（Lattice）

### 快速应用
| 问题 | 格技术 | 关键参数 |
|------|--------|---------|
| RSA 小根 | Coppersmith（LLL on polynomial lattice） | 根界 X < N^(1/e) |
| RSA 小 d | Boneh-Durfee（多元 Coppersmith） | d < N^0.292 |
| DSA/ECDSA nonce 偏置 | Hidden Number Problem → CVP | 已知偏置位数 |
| Knapsack 密码 | 低密度格攻击 | 密度 < 0.9408 |
| LCG 截断输出 | CVP on recurrence lattice | 每输出未知位数 |
| Subset sum | LLL on knapsack lattice | 元素大小 vs 数量 |
| NTRU 密钥恢复 | NTRU 格归约 | 维度和密钥大小 |
- **工具**: SageMath `LLL()` / `BKZ()`、Python `fpylll`、Coppersmith 用 `small_roots(X=..., beta=..., m=..., d=...)`

---

## 哈希攻击

### 快速攻击选择
| 场景 | 攻击 | 工具 |
|------|------|------|
| `H(secret\|\|msg)` 已知可扩展 | 长度扩展 | HashPump / hash_extender |
| 需两个同 MD5 文件 | 同前缀碰撞 | fastcoll |
| 需特定 MD5 前缀匹配 | 选择前缀碰撞 | hashclash |
| 字节级 HMAC 比较 | 时序攻击 | 自写脚本 |
| 找任意碰撞 | 生日攻击 | O(2^(n/2)) |
| PoW 前导零 | 暴力 | hashcat |
- **长度扩展**: 已知 `H(secret+data)` 和 secret 长度 → 算 `H(secret+data+padding+append)`（见 web.md Web 密码学误用）
- **MD5 碰撞**: 同前缀 `fastcoll` / 选择前缀 `hashclash` / UniColl 单块近碰撞 → CTF 常用 `md5($a)==md5($b)` 绕过（PHP 弱比较见 web.md Type Juggling）
- **Web 场景弱哈希**: 见 web.md 密码学误用 + cracking.md 哈希识别

---

## 古典密码（CTF）

### 识别方法论（先识别再破解）
- **频率分布平坦 + 全大写** → 多表替换（Vigenere）→ IC + Kasiski
- **IC ≈ 0.065** → 英文/单表替换（Caesar/简单替换/Playfair）
- **IC ≈ 0.038** → 随机/多表（Vigenere key < 10 时 IC ≈ 0.045-0.055）
- **IC > 0.060** → 单表；**IC ≈ 0.045-0.055** → 短 key 多表
- **工具**: Kasiski 测试找 key 长 → 词频分析 → Vigenere 破解；XOR 密文用单字节 XOR 暴力 + 频率评分
- **注意**: base64/hex 编码的密文需先解码再分析——模型常跳过识别直接猜错密码类型
