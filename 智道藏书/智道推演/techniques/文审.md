---
name: code-audit
description: 大爱仙尊·假设作业长文（code-audit）。先专卡后读本文。
---

> 作业先走 MAP.md 对应专卡。本文件是长文速查，不替代专卡。

# Code Audit（源码审计）

> 仅在根路由选择本目录后读取。拿到源码（泄露/.git还原/反编译/开源）后转入白盒审计时使用。

**触发**: 已获取目标源码（.git/.svn 泄露还原、备份文件、开源框架、APK/IPA 反编译、Java class/.NET dll 反编译），需定位可利用漏洞。

## 领域决策直觉

1. **没有 Source→Sink 完整证据链，不得标"已确认可利用"**——看到危险函数 ≠ 漏洞，用户输入必须实际到达危险调用
2. **三层分析法**：面（全局 grep Sink 定位危险区域）→ 线（从 Sink 反向追踪变量流到 Source）→ 点（验证利用条件：过滤可绕？参数可控？路径可达？）
3. 审计产出要对接可利用性：标注是否需要认证、过滤链、可达路由，接不上就不能算漏洞
4. 高危 Sink 集中区：SQL 拼接、命令执行、文件读写、反序列化、模板/表达式求值——按这个顺序扫
5. 框架默认不安全配置（debug/路由/上传）优先看——未强制路由时所有 public 方法可被 URL 直达
6. **组件版本命中 ≠ 漏洞**：有 CVE 还必须三步验证（①版本在受影响区间含传递依赖 ②漏洞代码路径被调用 ③攻击面对外可达）
7. **覆盖率自检**：审计结束对照 D1-D10 矩阵，未覆盖维度补审（见下文矩阵）——防漏报是白盒审计最常翻车处

---

## 证据契约（漏洞有效性判定标准）

只有**同时满足全部 6 条**，才能把发现写成"确认漏洞"：

| # | 标准 | 含义 |
|---|------|------|
| 1 可达 | 存在真实外部入口：HTTP 路由 / Servlet / Filter / Controller / RPC / WebService / 上传处理入口等 | |
| 2 可控 | 能指出用户可控参数、参数来源、绑定方式、进入代码的准确位置 | |
| 3 可传播 | 存在清晰 source-to-sink 文件/方法级调用链，中途无有效鉴权/校验/编码/白名单/类型约束阻断 | |
| 4 可利用 | sink 的语义在当前项目上下文中能造成真实安全影响 | |
| 5 可复现 | 必须有构造的安全 Payload，并提供可直接放入 BurpSuite Repeater 的原始 HTTP 请求包 | |
| 6 影响成立 | 能说明触发后产生的越权、泄露、绕过、写入或其他安全影响 | |

任一标准缺失 → 只能写"**高风险线索 / 待人工验证**"，不得写入"确认漏洞"。

每个确认漏洞必须包含：标题 / 严重等级 / 受影响入口 / 鉴权要求 / 用户可控输入 / Source-to-sink 调用链 / Sink / 同源受影响路由 / 防护判断 / 安全 Payload / BurpSuite 原始请求包 / 影响 / 证据文件和行号 / 限制说明。

BurpSuite 请求包必须完整到可直接粘贴进 Repeater：

```http
POST /<授权测试路径> HTTP/1.1
Host: <授权测试主机>
Content-Type: application/x-www-form-urlencoded
Connection: close

<参数名>=<安全证明Payload>
```

**安全边界**：只用无害证明 Payload（只读验证 / 布尔差异 / 受控回显标记 / 针对非敏感测试记录的越权证明）。破坏性 payload（删库/写后门/反弹 shell/窃密/横向）一律不输出——若漏洞必须依赖破坏性操作才能证明，写为高风险线索并说明缺失的非破坏性验证路径。

---

## 覆盖率自检矩阵（审后必过）

Phase 2A（自由审计）完成后，对照此矩阵验证维度覆盖；未覆盖维度按语言清单加载对应 D{N} 段落补充审计。

| # | 维度 | 关键问题 | 已覆盖? | 发现数 |
|---|------|---------|--------|--------|
| D1 | 注入 | 用户输入是否能到达 SQL/Cmd/LDAP/SSTI/SpEL 执行点？ | [ ] | |
| D2 | 认证 | Token/Session 生成、验证、过期是否完整？密钥是否安全？ | [ ] | |
| D3 | 授权 | 每个敏感操作是否验证用户归属？CRUD 权限是否一致？ | [ ] | |
| D4 | 反序列化 | 是否存在不受信数据的反序列化？Gadget 链是否可达？ | [ ] | |
| D5 | 文件操作 | 上传/下载/读取路径是否可控？是否有路径遍历？ | [ ] | |
| D6 | SSRF | 服务端 HTTP 请求的 URL 是否用户可控？协议是否限制？ | [ ] | |
| D7 | 加密 | 硬编码密钥/IV？ECB/CBC-no-MAC？弱KDF？RSA-PKCS1v1.5？证书校验绕过？ | [ ] | |
| D8 | 配置 | 调试接口(Actuator/pprof)是否暴露？CORS 是否过宽？错误堆栈是否泄露？ | [ ] | |
| D9 | 业务逻辑 | 竞态条件？支付金额可篡改？流程可跳过？Mass Assignment？IDOR/水平越权？数据导出范围？ | [ ] | |
| D10 | 供应链 | 依赖是否有已知 CVE？版本是否在安全范围？SBOM/来源证明是否可信可消费？ | [ ] | |

规则：**Critical（D1-D6）必须全覆盖**；D7-D8 强烈建议；D9 有后台/多角色/多租户/支付逻辑必查；D10 有外部依赖必查。

---

## 通用管线（多语言共用）

### 五阶段流程
| 阶段 | 任务 | 产出 |
|------|------|------|
| P1 路由映射 | 解析全部入口点及参数绑定方式 | 路由清单 |
| P2 权限建模 | 认证/授权中间件覆盖范围，标记裸露路由 | 权限矩阵 |
| P3 数据流追踪 | Source→Sink 完整路径（记录每步传递/过滤） | 证据集 |
| P4 分类审计 | 按 Sink 类型深入 | 漏洞清单 |
| P5 报告组装 | 评分 + 利用链编排 + 覆盖率自检 | 审计报告 |

### 严重度评分
`Score = R*0.40 + I*0.35 + C*0.25`（各 0-3）
- R 可达性：3=未认证直达，2=未认证需条件，1=普通认证，0=管理员+配置
- I 影响：3=RCE/完全控制，2=数据篡改，1=敏感泄露，0=非敏感
- C 复杂度反向：3=直接拼 payload，0=多步+竞态
- CVSS ≈ Score/3.0*10；2.50-3.00 Critical / 2.00-2.49 High / 1.25-1.99 Medium

### 可控性矩阵（数据流追踪时同步输出）
对每个 sink 参数，判断进入危险拼接/执行前是否被硬化覆盖，标注三态：
- `✅完全可控`：直接来自 `$_GET/$_POST/$request/body/header/cookie`，无过滤
- `⚠️条件可控`：有白名单/类型转换/转义/长度约束，需找绕过
- `❌不可控`：被枚举/强校验阻断，写清拦截点代码位置

输出每行含：sink 参数变量名 → 来源（`request()->post()`/`php://input`/header）→ 硬化点代码证据位置 → 三态结论。无法证明"真实落点执行"的一律标 `⚠️待验证`，不写 `✅已确认可利用`。

---

## PHP 审计

### 入口点识别
- **Laravel**: `routes/web.php` + `routes/api.php`，关注 `Route::any` 与资源路由
- **ThinkPHP**: `config/route.php` + 控制器自动路由（TP5 默认开启，未强制路由时所有 public 方法可被 URL 直达）——最高产
- **原生 PHP**: 扫描全部 `.php` 文件，识别 `$_GET/$_POST/$_REQUEST` 入口

### 危险函数分类表（全局 grep 关键字）
| 漏洞 | 危险函数（Sink） |
|------|----------------|
| SQL 注入 | `mysqli_query`/`mysqli_multi_query`/`PDO::query`/`PDO::exec`/`pg_query`/`sqlite_query`/`$wpdb->query`；ORM Raw：`DB::raw`/`whereRaw`/`orderByRaw`/`selectRaw`/`havingRaw`/`groupByRaw` |
| 命令注入 | `exec`/`system`/`shell_exec`/`passthru`/`proc_open`/`popen`/`pcntl_exec`/反引号 |
| SSRF | `curl_exec`/`curl_multi_exec`/`file_get_contents`(远程)/`fopen`/`fsockopen`/`SoapClient`/`get_headers` |
| XSS | `echo`/`print`/`printf`/`sprintf`；Blade `{!! !!}`、Twig `{{ var\|raw }}`、Smarty `{$var nofilter}` |
| 文件读/包含 | `include`/`require`/`*_once`/`file_get_contents`/`readfile`/`file`/`fopen`/`fread`/`highlight_file`/`show_source` |
| 文件上传 | `move_uploaded_file`/`rename`/`copy`（配合 `$_FILES`） |
| 文件写入 | `file_put_contents`/`fwrite`/`fputs`/`fopen('w'/'a')` |
| 归档提取 | `ZipArchive::extractTo`/`PharData::extractTo`（Zip Slip） |
| 反序列化 | `unserialize`；`phar://` 触发：`file_exists`/`is_dir`/`is_file`/`fopen`/`filesize`/`stat`/`md5_file`/`getimagesize` 等 40+ 文件函数 |
| XXE | `DOMDocument::loadXML`/`load`/`simplexml_load_string`/`SimpleXMLElement`/`XMLReader::open`/`xml` |
| 表达式 | `eval`/`assert`(PHP<8)/`preg_replace('/e')`(PHP<7)/`create_function`(PHP<8)；可控回调 `call_user_func`/`array_map`/`usort` |
| 模板注入 | Twig `createTemplate`/Smarty `fetch("string:...")`/Blade 动态编译 |
| LDAP | `ldap_search`/`ldap_list`/`ldap_read`/`ldap_bind`(认证绕过) |
| 重定向 | `header("Location:")`/`header("Refresh:")`/`redirect()`/`Redirect::to()` |
| CRLF | `header()`/`setcookie()` |

### 框架特有审计点
- **Laravel**: Mass Assignment（`$fillable`/`$guarded`、`create($request->all())`、`forceFill` 绕过）；Blade XSS（`{!! !!}`、`@php echo`、自定义 Directive 未包 `e()`、`Js::from()`）；CSRF 例外（`VerifyCsrfToken::$except`、`routes/api.php` 默认无 CSRF）；Gate/Policy 覆盖率；Eloquent Raw（`whereRaw`/`DB::raw` 变量拼接）；`APP_DEBUG=true`、`.env` 可下载、Telescope/Debugbar 暴露；队列 Job 反序列化 + `SerializesModels`；上传 `store()` 未验 MIME
- **ThinkPHP**: 历史 RCE（TP5.0 `_method=__construct` 覆盖 `filter` 属性任意函数调用；TP5.1 `invokefunction`）；路由注入（`s=` 兼容模式直映射、`app_map`/`deny_app_list` 绕过）；where 数组注入（`exp`/`like`/`between` 表达式，`Db::raw`）；`field/order/group` 直接拼接；缓存文件写入（`Cache::set` 内容可控 → `runtime/cache/` 可预测路径写马）；模板注入（`{php}` 标签、`{:system($cmd)}` 变量函数）；`.env`/`runtime/` 目录泄露
- **WordPress**: `wp_ajax_nopriv_*` 未认证钩子；Shortcode 回调 `$atts` 注入；`$wpdb` 未 `prepare()`（需 `%s/%d/%f` 占位符 + `esc_like`）；nonce 缺失（`wp_verify_nonce`/`check_ajax_referer`）；REST `permission_callback => '__return_true'`；`is_admin()` 误用（判路径非权限）；上传 `upload_mimes` 添加 `.php/.svg`；SVG 嵌入 JS 存储型 XSS
- **Symfony**: `APP_DEBUG` 泄露 Profiler/SQL；低版本 `Yaml::parse` `!!php/object` 对象注入
- **Yii2**: RBAC `DbManager` 分配不当、`checkAccess` 缺失；ActiveRecord `findBySql`/`where` 字符串拼接
- **CodeIgniter**: CI3 `global_xss_filtering` 已弃用；CI4 需手动 `esc()`；`CSRFProtection` 排除路由过多

---

## Java 审计

### 反编译策略（目标是 .class/JAR/WAR）
- **CFR 首选** `java -jar cfr.jar app.jar --outputdir ./decompiled --extraclasspath "lib/*"`；Procyon（泛型还原好）补充；FernFlower（IDE 内置）快速浏览
- Spring Boot fat JAR：`BOOT-INF/classes` 业务代码、`BOOT-INF/lib` 依赖；WAR：`WEB-INF/classes` + `WEB-INF/lib`；先解包 `jar -xf`
- 路由还原：RUNTIME 注解（`@RequestMapping`/`@WebServlet`/`@Path`/`@PreAuthorize`）CFR 可还原；`-parameters` 编译才有真实参数名；Spring 注解字符串不被混淆

### 入口点识别
- **Spring MVC**: `@RequestMapping`/`@GetMapping`/`@PostMapping`，扫全部 `@Controller`/`@RestController`
- **Servlet**: `web.xml` `<servlet-mapping>` + `@WebServlet`
- **JAX-RS**: `@Path`/`@GET`/`@POST`；**Struts2**: `struts.xml` `<action>` + ActionMapping 通配符；**WebService**: CXF/Axis `@WebService` + WSDL 路径
- 记录参数绑定方式：`@RequestParam`/`@PathVariable`/`@RequestBody`

### 危险 Sink 速查（全局 grep 关键字）
| 漏洞 | 危险函数（Sink） |
|------|----------------|
| SQL 注入 | `Statement.executeQuery/executeUpdate/execute`、`prepareStatement`(拼接时)、MyBatis `${}`、Hibernate `createQuery`/`createSQLQuery`、JPA `@Query(nativeQuery=true)`、`EntityManager.createNativeQuery`、Spring `JdbcTemplate.query/update` |
| 命令注入 | `Runtime.getRuntime().exec()`/`ProcessBuilder.start()`/commons-exec `CommandLine`/`DefaultExecutor` |
| SSRF | `URL.openConnection`/`HttpURLConnection.connect`/OkHttp `newCall().execute`/Apache HttpClient/`RestTemplate.getForObject/exchange`/`WebClient`/`java.net.Socket` |
| 文件读取 | `FileInputStream`/`Files.readAllBytes`/`RandomAccessFile`/`ClassLoader.getResource(AsStream)`/`Path`+`Files.read*`/`ServletContext.getResourceAsStream` |
| 文件上传 | `MultipartFile.transferTo()`/`Part.write()`/`Files.copy`/`FileOutputStream` |
| XSS | JSP `<%= %>`/`out.print()`、Thymeleaf `th:utext`、FreeMarker `${var?no_esc}`/`<#noescape>`、Velocity `$!{var}` |
| XXE | `DocumentBuilderFactory`/`SAXParserFactory`/`XMLInputFactory`/`TransformerFactory`/`SchemaFactory`/`XMLReader`/JAXB `Unmarshaller.unmarshal` |
| 反序列化 | `ObjectInputStream.readObject/readUnshared`、`XMLDecoder.readObject`、FastJSON `JSON.parseObject`(含 `Feature.SupportNonPublicField`)、Jackson `enableDefaultTyping`/`@JsonTypeInfo`、Hessian `HessianInput.readObject`、Kryo、XStream `fromXML` |
| 表达式 | SpEL `ExpressionParser.parseExpression`、OGNL `Ognl.getValue`(Struts2)、MVEL `MVEL.eval`、`ELProcessor.eval`、Groovy `GroovyShell.evaluate` |
| 重定向 | `sendRedirect()`/`"redirect:"+url`/`RedirectView`/`ResponseEntity.status(302).header("Location",url)` |
| LDAP | `InitialDirContext.search`/`SpringLdapTemplate.search` |

### 框架特有审计点
- **Spring 全家桶**: Mass Assignment（`@ModelAttribute` 绑定 `role/isAdmin/price` 敏感字段，缺 `@InitBinder.setAllowedFields` 或 DTO 隔离）；SpEL 5 注入点（`@Value("#{...}")`、`@Cacheable(key=...)` 拼接、`@PreAuthorize` 动态表达式、`ExpressionParser.parseExpression(input)`、Spring Data `@Query` SpEL `?#{...}`；安全用 `SimpleEvaluationContext`）；Actuator（`/env`+`/restart`→改数据源 JNDI、`/jolokia`→JMX reloadByURL、`/heapdump`→提取密钥）；高频 CVE（CVE-2022-22947 Gateway SpEL、22963 Cloud Function、22965 JDK9+ 参数绑定 RCE、2018-1270 STOMP、2017-8046 Data REST、2020-5421 RFD）；`antMatchers` 顺序/`permitAll` 过大
- **Struts2**: OGNL 注入路径（参数→`ParametersInterceptor`→`ValueStack`→绕过 `SecurityMemberAccess`→`Runtime.exec`）；S2-045/046（Content-Type/Content-Disposition filename 异常解析触发 OGNL）；参数前缀 `method:`（调用任意方法）/`redirect:`/`redirectAction:`；危险配置 `struts.ognl.allowStaticMethodAccess=true` + `struts.enable.DynamicMethodInvocation=true`；高危 CVE 区间 S2-001~S2-062
- **Shiro**: RememberMe 反序列化（硬编码 AES 密钥 `kPH+bIxk5D2deZiIxcaaaA==`，CVE-2016-4437；升级后仍查 `setCipherKey` 弱密钥）；URI 绕过（分号截断 `/admin/;.js`、路径穿越 `/toLogin;/../admin`、双重编码、尾部斜杠、大小写——根因 Shiro 与 Spring/Servlet 解析规则不一致）；CVE-2020-1957/11989/13933/2021-41303
- **FastJSON/Jackson**: FastJSON `@type` autotype 绕过史（1.2.24→1.2.41 `L/;` 前缀→1.2.47 `java.lang.Class` 缓存→1.2.68 `AutoCloseable` expectClass→1.2.83+ `safeMode`）；Jackson `enableDefaultTyping`/`activateDefaultTyping` 全局多态 = RCE 入口；常用 Gadget：`JdbcRowSetImpl`(JNDI)、`TemplatesImpl`(恶意字节码)、BCEL `ClassLoader`、Spring `PropertyPathFactoryBean`
- **MyBatis**: `${}` 直接拼接 vs `#{}` 预编译（核心！）；ORDER BY 无法参数化须白名单、LIKE 用 `CONCAT('%', #{kw}, '%')`、IN 用 `<foreach item="#{id}">`；`<if>`/`<foreach>` 动态 SQL 内 `${}` 高危；注解 `@Select("...${tableName}...")` 与 XML 双入口都审；`@SelectProvider` 动态 SQL 拼接

### Java 组件漏洞扫描（自动化）
```bash
python3 scripts/tools/run_component_vulnerability_scan.py --workspace <审计工作目录>
python3 scripts/tools/run_component_vulnerability_scan.py --source <源码目录|依赖目录|app.jar|app.war|WEB-INF/lib>
python3 scripts/tools/run_component_vulnerability_scan.py --validate-rules   # 校验规则正则
```
- 规则库：`scripts/references/java-vulnerability.yaml`（Log4j/Fastjson/Struts2/Spring/Shiro/XStream/SnakeYAML/ActiveMQ/RocketMQ/Kafka/Nacos/Tomcat 等 100+ 条，按 severity 分级正则）
- 解析来源：pom.xml / gradle / JAR/WAR/EAR 嵌套 / pom.properties / MANIFEST.MF
- 输出：`<workspace>/evidence/component-hits/{critical,high,medium,low}.md` + `index.md` + `manifest.json`
- **组件命中只能作为线索**，必须回到证据契约（可达/可控/传播链/可利用/影响）确认，不能直接转漏洞

---

## .NET 审计

### 反编译 / 反混淆（目标是 bin/*.dll、*.exe、.aspx 后端类）
流程：① `strings <dll> | grep -iE 'confuser|eazfuscator|smartassembly|dotfuscator|reactor'` 判混淆 → ② `de4dot` 整目录反混淆（多 DLL 必须整目录一次性处理）→ ③ `ilspycmd` 导出可检索 C# 项目（跳过 `System.*|Microsoft.*|Mono.*|netstandard|mscorlib`）。命令级细节见 `scripts/references/dotnet-decompile.md`；反混淆产物仅用于静态审计，不要替换生产程序集运行。

### .NET 危险 Sink 速查
| 漏洞 | 危险函数（Sink） |
|------|----------------|
| SQL 注入 | `SqlCommand` 字符串拼接 `CommandText`、`ExecuteNonQuery/ExecuteReader/ExecuteScalar`、Dapper `Query<T>(sql, ...)` 拼接、EF Core `FromSqlRaw`/`ExecuteSqlRaw` |
| 命令注入 | `Process.Start` + `"/c " + cmd`、`System.Diagnostics.Process` 拼接 |
| 反序列化 | `BinaryFormatter.Deserialize`、`LosFormatter`、`ObjectStateFormatter`、`Json.NET TypeNameHandling=All/Auto`、`XmlSerializer`、`DataContractSerializer`、`fastjson.NET`、ViewState `__VIEWSTATE`（含 machineKey 弱密钥时） |
| XXE | `XmlDocument.Load/LoadXml`（未禁用 DTD）、`XmlReader` + `DtdProcessing=Parse`、`XDocument.Parse` |
| 表达式 | `Microsoft.CSharp.CSharpCodeProvider` 动态编译、`DataTable.Compute` 表达式 |
| SSRF | `WebRequest.Create`/`HttpClient`/`RestClient`/`TcpClient`（URL 可控） |
| 文件 | `File.ReadAllText/WriteAllText`、`Path.Combine(userInput, ...)`（遍历）、`ZipFile.ExtractToDirectory`（Zip Slip） |
| 不安全反序列化入口 | `__VIEWSTATE`/`__EVENTVALIDATION`/`__EVENTTARGET`（ASP.NET 特有，见 WooYun 参数频率） |
| 认证 | ViewState MAC 校验缺失（`EnableViewStateMac=false` → 伪造 ViewState 反序列化）、machineKey 硬编码/泄露 |

---

## 按漏洞类型审计要点

### 注入 / SQLi
- **PHP**: `PDO::query/exec`、`mysqli_query` 直接执行，拼接即危险；`prepare()+execute()` 占位符绑定完整才安全；ORM Raw 方法（`whereRaw/orderByRaw/selectRaw`）绕开参数化；动态 ORDER BY/LIMIT 唯一安全方案 `intval()` 或白名单；过滤陷阱：`addslashes` 对 GBK 宽字节无效、`sprintf("...'%s'", $input)` 本质仍拼接、二次注入需跨存储边界追踪
- **Java**: MyBatis `${}` 常被误当 `#{}`；Hibernate HQL 字符串拼接 `"from User where name='" + input + "'"` 可注入；JPA `@Query(nativeQuery=true)` 中 SpEL `#{#param}` 或字符串拼接均危险；Spring Data `Sort` 对象安全；NoSQL：字符串拼接构造 `BasicDBObject`/`Document` 危险、`Criteria.where().is()` 安全
- **Go**: `fmt.Sprintf`/字符串拼接构造查询、GORM `db.Raw()`/`db.Where()` 拼接、`db.Order(userInput)`（见 D1 Go 清单）
- **Python**: f-string/`%`/`.format()` 拼接、Django `raw()`/`extra()`/`RawSQL()`、SQLAlchemy `text()` 拼接、Django `filter(**request.GET.dict())` 字段名注入（见 D1 Python 清单）
- **Node.js**: `sequelize.query`/`knex.raw`/`pool.query` 模板字符串 `${}`、MongoDB `find({$where: user_input})`/`find(JSON.parse(req.body))` 操作符注入 `$gt/$regex/$where`（见 D1 JS 清单）

### 命令注入
- **PHP**: `escapeshellarg` 单独有效，但与 `escapeshellcmd` 同用反而引入绕过（引号配对被破坏）；注意反引号与 `exec("convert ".$src." ".$dest)` 未转义参数段；`proc_open([$cmd,$arg...])` 数组模式安全
- **Java**: `Runtime.exec(String)` 经 `StringTokenizer` 拆分 vs `exec(String[])` 行为不同；`ProcessBuilder` 数组形式较安全，但 `sh -c "cmd"` 包装退化为 shell；反射调 `Runtime.getRuntime()` 绕过静态扫描
- **WooYun 高频命令执行入口**（按频率）：文件操作 68%、系统命令函数 62%、Struts2 OGNL 50%、压缩解压 30%、SSRF 30%、ping 网络诊断 26%、图片处理（ImageMagick/GraphicsMagick）24%、curl/wget 调用 24%、Java 反序列化 20%、DNS 查询 16%——审计优先扫这些功能点

### 反序列化
- **PHP**: `unserialize` 入口来源：Cookie/Session/缓存/数据库/HTTP body 逐一点查；Phar 反序列化（40+ 文件函数处理 `phar://` 路径自动触发，查路径参数可控性）；POP 链 `__destruct`/`__wakeup`/`__toString` 入口 → 链到危险 Sink；`allowed_classes`：空数组 `[]`/`false` 才完全安全
- **Java**: 入口 `readObject/readUnshared`、`XMLDecoder.readObject`；数据来源 HTTP body（魔术字节 `AC ED 00 05`/Base64 `rO0AB`）、RMI/JMX、Redis/Memcached、ViewState；Gadget 可用性检查 classpath：commons-collections 3.x/4.x、commons-beanutils、spring-core、Groovy、hibernate-core；防御验证 JEP 290 `ObjectInputFilter`；Shiro RememberMe 链 = Base64→AES-CBC→`readObject`
- **.NET**: `BinaryFormatter`/`LosFormatter`/`ObjectStateFormatter`/Json.NET `TypeNameHandling`、ViewState（machineKey 弱密钥）

### XXE
- **PHP**: `libxml_disable_entity_loader(true)`（PHP<8 必显式）；`LIBXML_NOENT` 标志重新展开实体（危险）；盲 XXE 用带外 HTTP/FTP/DNS
- **Java 5 种解析器安全配置**: DBF `setFeature("http://apache.org/xml/features/disallow-doctype-decl", true)`；SAX 加 external-general/parameter-entities=false；StAX `SUPPORT_DTD=false`+`IS_SUPPORTING_EXTERNAL_ENTITIES=false`；Transformer `ACCESS_EXTERNAL_DTD=""`+`ACCESS_EXTERNAL_STYLESHEET=""`；Schema 同；Java 特有 `jar:`/`netdoc:` 协议
- **.NET**: `XmlDocument` 未禁 DTD、`XmlReaderSettings.DtdProcessing=Parse`

### 认证配置
- **PHP**: 弱比较 `"0e123"=="0e456"` 类型混淆；JWT algorithm confusion（RS256→HS256、`alg:none`、`kid` 注入）；Remember Me/密码重置 token 可预测、未绑定用户/无过期；IDOR 直查无归属检查；`display_errors=On`/`APP_DEBUG=true` 泄露；`md5($pass)`/`sha1` 存储；`mt_rand/rand/uniqid` 弱随机
- **Java**: Spring Security 匹配器顺序（宽松 `permitAll()` 覆盖后续）；Shiro URI 解析差异绕过；JWT 算法混淆、`exp/iss/aud` 校验缺失；DAO `WHERE id=?` 缺 `AND user_id=?` 水平越权；MD5/SHA1 哈希、ECB、`new Random()`；Actuator `/env`/`/heapdump`/`/jolokia` 泄露

### 密码重置 4 模式（WooYun 22 案例）
| 模式 | 根因 | 检测 |
|------|------|------|
| A 验证码回显 | 响应包直接含验证码明文 | `POST /sendSmsCode` 检查 `{"verifyCode":"123456"}` |
| B 验证码解绑 | 验证码仅校验有效性，未校验归属用户 | 自己手机收到验证码 A → 对目标账号重置 → 用 A 完成 |
| C 步骤可跳过 | 多步流程无状态校验 | 直接访问第 3 步 URL / 前端 DOM 覆盖法 |
| D 凭证参数可控 | username 等来自客户端可篡改 | `POST /resetPassword username=victim&newPassword=...` |

---

## WooYun 实战统计（88,636 案例提炼，2010-2016）

> 数据来源：wooyun_vulnerabilities.json 88,636 条。用法：按频率优先测——统计上最常命中的参数名、入口点、绕过法，比从任意点开始扫产出高。

| 漏洞类型 | 案例数 | 占比 | 核心价值 |
|---------|-------|------|----------|
| SQL 注入 | 27,732 | 31% | 高危参数名频率、数据库指纹、绕过技巧 |
| 未授权访问 | 14,377 | 16% | 后台/API/服务未授权、IDOR、中间件默认路径 |
| 逻辑漏洞 | 8,292 | 9% | 密码重置 4 模式、越权矩阵、验证码绕过 |
| 信息泄露 | 7,337 | 8% | 泄露类型、敏感路径、利用链 |
| XSS | 7,532 | 8% | 输出点分类矩阵、上下文绕过、DOM XSS |
| 命令执行 | 6,826 | 8% | 入口点分类、拼接符、编码绕过 |
| 文件遍历 | 2,854 | 3% | 参数名、Payload 编码、敏感文件 |
| 文件上传 | 2,711 | 3% | 检测绕过、解析漏洞、高危 CMS |

### SQLi 高危参数名（按出现频率）
```text
数字型ID: id(56) sort_id(37) stid(32) fid(8) hotelid(11) areainfoid(8)
认证类:   username(33) password(30) userpwd(11)
业务类:   type(18) action(7) page(4) name(30)
ASP.NET:  __viewstate(58) __eventvalidation(56) __eventargument(52) __eventtarget(41)
```
（括号内为 27,732 案例中的出现频率排序值）→ 审到这些参数名的拼接点优先追。

### 命令执行入口频率
文件操作 68% > 系统命令函数 62% > Struts2 OGNL 50% > 压缩解压 30% > SSRF 30% > ping 26% > 图片处理 24% > 网络请求 24% > Java 反序列化 20% > DNS 16%

### 文件上传绕过检测矩阵
| 检测类型 | 位置 | 绕过 | 成功率 |
|---------|------|------|--------|
| JavaScript 验证 | 客户端 | 禁 JS/Burp 拦截改包 | 95% |
| Content-Type | HTTP 头 | 改 `image/jpeg` | 85% |
| 文件头检测 | 内容 | 加 `GIF89a` 头 | 75% |
| 扩展名黑名单 | 服务端 | 大小写/双写/特殊扩展名 | 70% |
| 内容检测 | 内容 | 图片马/编码绕过 | 60% |
| 扩展名白名单 | 服务端 | %00 截断/解析漏洞 | 40% |

### 上传点频率矩阵
富文本编辑器 42%（FCKeditor 48%/eWebEditor 28%/UEditor 12%/KindEditor 8%）> 头像上传 18% > 附件/文档 15% > 后台功能上传 12% > 业务功能 8% > 导入功能 5%。编辑器上传接口测试路径：FCKeditor `/FCKeditor/editor/filemanager/browser/default/connectors/test.html`、eWebEditor `/ewebeditor/admin/default.jsp`。

### 中间件未授权默认路径（未授权访问 14,377 案例高频）
Tomcat `/manager/html`、WebLogic `/console/` + `/wls-wsat/`、JBoss `/jmx-console/` + `/invoker/JMXInvokerServlet`、Spring Boot Actuator `/actuator/env|health|heapdump|mappings`。

---

## 多语言审计要点索引（D1 注入面，按语言）

> 完整 10 维（D1-D10）每语言语义提示见各语言清单。以下为 D1 注入面速查，其余维度按覆盖率矩阵按需补。

- **Go**: `fmt.Sprintf` 拼 SQL；GORM `db.Raw()`/`db.Where()`/`db.Order(userInput)`；`exec.Command` + `sh -c`；`text/template` 渲染 HTML（无转义）；`template.HTML()`/`template.JS()` 类型转换绕过转义；LDAP `ldap.SearchRequest` filter 拼接
- **Python**: f-string/`%`/`.format()` 拼 SQL；Django `raw()`/`extra()`/`RawSQL()`；SQLAlchemy `text()`；`os.system`/`subprocess` + `shell=True`；`eval()`/`exec()`；Django `filter(**request.GET.dict())` 字段名注入；Flask `render_template_string(user_input)`（SSTI）；`pickle.loads`（不可信）
- **Node.js/JS**: `sequelize.query`/`knex.raw`/`pool.query` 模板字符串；MongoDB `find({$where})`/`find(JSON.parse(req.body))`；`eval()`/`new Function()`/`vm.runInNewContext`；`child_process.exec` + `shell:true`；模板引擎把用户输入当模板编译（SSTI）；`RegExp(user_input)` ReDoS
- **.NET**: `SqlCommand.CommandText` 拼接、Dapper/EF `FromSqlRaw`、`BinaryFormatter`、ViewState、`CSharpCodeProvider` 动态编译、`DataTable.Compute`
- **Ruby**: `eval()`/`send()` 动态调用、Rails `render inline:`、`Kernel.system`/反引号、`YAML.load`（非 `safe_load`）、`Marshal.load`
- **Rust**: `sqlx::query(&format!(...))`/`rusqlite` 拼接、`Command::new("sh").arg("-c")`、`unsafe` 块内危险操作、反序列化 `serde` + `serde_yaml` 旧版
- **C/C++**: `system()`/`popen()`/`execl*`、`sprintf`/`strcpy`/`strcat`/`gets`（缓冲区溢出）、`mysql_real_query` 拼接、`setenv` 注入

---

## 审计工具链

### SAST 工具选型
| 场景 | 工具 | 命令 |
|------|------|------|
| 多语言初筛 | Semgrep / Opengrep | `semgrep --config auto .` / `--config p/security-audit .` / `--config rules/ --json --output out.json` |
| PHP taint | Psalm | `./vendor/bin/psalm --taint-analysis --report=results.sarif` |
| Java 深度查询 | CodeQL | `codeql database create db --language=java --source-root .` → `codeql database analyze db github/security-queries --format=sarif-latest` |
| 依赖/供应链 | Snyk / OSV / OWASP Dependency-Check | `snyk test` / `snyk code test` |

**关键坑**: 官方 CodeQL 不支持 PHP，勿把 Java 建库命令套到 PHP 项目；PHP 用 Semgrep/Psalm/人工追踪替代。工具输出必须回填人工证据链，不能直接转漏洞。

### 手工 grep 模式（候选 Sink 定位）
- 第 3/4/5 节危险函数表关键字直接 `rg` 全项目
- 框架特定：`$request->all()`+`create/update`（Mass Assignment）、`{!!`、`whereRaw|DB::raw`、`unserialize|phar://`、`eval|assert|create_function`、`${`（MyBatis/TP）、`setCipherKey|enableDefaultTyping|parseObject`、`sendRedirect|redirect:`、`Runtime.exec|ProcessBuilder`、`HttpURLConnection|RestTemplate`、`SqlCommand|BinaryFormatter|FromSqlRaw`、`TypeNameHandling`、`fmt.Sprintf|db.Raw|child_process.exec|shell=True`

### 结果分级
候选（命中未确认入口）→ 待验证（过滤链/条件未确认）→ 已确认（完整证据链）→ 误报。复核五问：Source 是否不可信？Sink 是否真执行（非死代码）？Sanitizer 是否覆盖全部路径？权限检查是否在 Sink 前生效？业务上下文是否放大/限制影响？复核完跑覆盖率矩阵 D1-D10 补漏。

### 依赖 CVE 审计
- **PHP**: `composer audit`（2.4+）/ `local-php-security-checker`，解析 `composer.lock` 精确版本比对
- **Java**: `mvn dependency:tree` / `gradle dependencies --configuration runtimeClasspath` / OWASP Dependency-Check / `scripts/tools/run_component_vulnerability_scan.py`（正则规则库 `scripts/references/java-vulnerability.yaml`）
- **有 CVE ≠ 可利用**：三步验证（①版本确实在受影响区间含传递依赖 ②漏洞代码路径实际被调用 ③攻击面对外可达）
- 高频危险依赖：commons-collections 3.x、fastjson <1.2.83、log4j-core 2.0-2.17.0、shiro <1.2.5、struts2 2.0-2.5.30、spring-cloud-gateway <3.1.1、jackson-databind <2.13.4

---

## 供应链来源验证（SBOM / SLSA / 来源证明）

> 拿到构建产物 + 流水线/仓库访问权后，把审计面从"代码里有什么洞"扩到"这份产物是不是可信来源、依赖清单能不能被消费"。这是 D10 供应链维度的深水区：**SBOM 只是清单，值不值得信取决于它的质量与消费闭环**。SBOM 命中同样遵守证据契约——清单里有 CVE ≠ 可利用，仍需回三步验证。

### SBOM 质量模型（8 维自检，缺一维即"不可消费清单"）

清单存在 ≠ 清单有用。逐维核对，任一维不合格标注为供应链盲区：

| # | 维度 | 关键问题 | 不合格信号（审计红旗） |
|---|------|---------|----------------------|
| Q1 | 对象绑定 | SBOM 是否绑定到**具体产物+版本+摘要**，而非泛泛"某项目" | 无 `component.hashes` / 无版本 / 一份 SBOM 复用多产物 |
| Q2 | 组件身份 | 每个组件是否有无歧义标识（PURL / CPE / SWID） | 只有名字无 PURL、同名不同源无法区分、私有组件裸名 |
| Q3 | 关系完整性 | 依赖图是否含**传递依赖**，非仅顶层直接依赖 | 只列直接依赖、`dependsOn` 为空、扁平列表无层级 |
| Q4 | 内容准确性 | 清单与实际构建产物是否一致（无漏报/幻报） | 构建引入但清单缺失、清单有但产物无、版本对不上 |
| Q5 | 新鲜度 | 是否**每次构建重新生成**、时间戳贴合构建时刻 | 手工维护、陈旧时间戳、发版后未更新 |
| Q6 | 可机读 | 是否标准格式（SPDX 3.0.1 / CycloneDX 1.7）可自动消费 | 纯 PDF/Excel、私有格式、字段缺失无法解析 |
| Q7 | 完整性认证 | SBOM 自身是否被**签名**、可校验未被篡改 | 无签名、签名不可验、与产物摘要无绑定 |
| Q8 | 可行动性 | 是否能驱动下游（漏洞匹配 / VEX / 策略门禁） | 生成即归档、无 VEX 联动、无消费闭环 |

- **标准锚点**：NTIA 2021 最小要素（供应者/组件名/版本/唯一标识/依赖关系/作者/时间戳）为地板线；NIST SSDF **PS.3.2** 要求留存来源数据；格式取 SPDX 3.0.1 或 CycloneDX 1.7。
- **审计定式**：先问"这份 SBOM 绑定到哪个产物摘要"（Q1），绑不上就整份存疑；再验 Q3 传递依赖完整性——**多数真实供应链漏洞藏在传递依赖**，只列直接依赖的 SBOM 会系统性漏报。

### 制品摘要绑定 + 签名验证

供应链信任链的根：**产物摘要 → SBOM/来源证明 → 签名**三者互绑，断一环即不可信。

- **摘要绑定**：SBOM 的 `component.hashes`（SHA-256）必须等于实际产物摘要；证明（attestation）的 `subject.digest` 必须指向同一摘要。三者摘要不一致 = 清单/证明张冠李戴。
- **签名校验**：Sigstore（cosign）无密钥签名 → 校验 Rekor 透明日志条目 + Fulcio 短时证书的身份（OIDC identity，如 CI 的 workload identity）。校验时**必须锚定预期签名者身份**（`--certificate-identity` + `--certificate-oidc-issuer`），否则"有签名"只证明"有人签过"，不证明"可信方签的"。
- **审计要点**：`cosign verify-attestation` 通过 ≠ 内容可信——还要看证明的 predicateType 与 builder.id 是否是你信任的构建者（见下）。

### in-toto + SLSA 来源证明（Provenance）

来源证明回答"**这个产物由谁、用什么源码、经什么流水线构建**"。in-toto 四层封装 + SLSA 分级：

| 层 | 名称 | 内容 |
|----|------|------|
| L1 | Predicate | 具体断言体（如 SLSA Provenance v1：`buildDefinition` + `runDetails`） |
| L2 | Statement | `subject`（产物+摘要）+ `predicateType` + `predicate` |
| L3 | Envelope | DSSE 签名信封（`payload` + `signatures`），防篡改 |
| L4 | Bundle | Statement/Envelope + 验证材料（证书/透明日志）打包分发 |

- **SLSA Build 等级**（防篡改强度递增）：
  - **L0** 无保证；**L1** 有来源证明（provenance 存在，可能不防篡改）；**L2** 托管构建 + 签名来源（防事后篡改）；**L3** 强隔离构建 + 不可伪造来源（源+构建过程隔离，防构建期投毒）。
  - 审计映射：产物声称 L3 但 provenance 里 `builder.id` 指向共享/可写的 runner = 名不副实，降级处置。
- **Provenance v1 关键字段**：`buildDefinition.buildType`（构建类型 URI）、`externalParameters`（触发源：repo/ref/commit）、`internalParameters`、`resolvedDependencies`（构建期拉取的依赖及摘要）、`runDetails.builder.id`（构建者身份 = **信任锚**）、`runDetails.metadata`（invocationId/时间戳）。
- **验证三阶段**（缺一不可）：
  1. **信封完整性**：DSSE 签名有效 + 签名者身份锚定预期（Sigstore 路径校验 Rekor+Fulcio）。
  2. **主体匹配**：`statement.subject.digest` == 手上产物摘要（证明确实描述这个产物）。
  3. **策略符合**：`builder.id` ∈ 信任的构建者集合、`buildType`/`externalParameters` 符合预期（源仓库/分支正确、无旁路触发）、SLSA 等级达门槛。
- **审计红旗**：provenance 缺失（L0）；`builder.id` 是本地/开发者机器而非受控 CI；`externalParameters` 里的源 repo/commit 与发布 tag 对不上；`resolvedDependencies` 为空（无法追溯构建期依赖）。

### SBOM × VEX 联动（把"清单里有 CVE"降噪成"是否真受影响"）

SBOM 列出组件，VEX（Vulnerability Exploitability eXchange）声明每个 CVE 对**本产物**是否可利用——两者联动才是可行动的供应链结论。CISA 四状态：

| VEX 状态 | 含义 | 审计动作 |
|---------|------|---------|
| **NOT AFFECTED** | 组件含该 CVE 但本产物不受影响 | **必须核 justification**（见下），justification 站不住 = 当 AFFECTED 处理 |
| **AFFECTED** | 确认受影响 | 进证据契约三步验证 → 可达/可控/可利用才升级为确认 |
| **FIXED** | 已修复 | 核对修复版本确实进了产物（回 Q4 内容准确性） |
| **UNDER INVESTIGATION** | 调查中 | 当作未决风险，不得静默忽略 |

- **NOT AFFECTED 的 justification 必须合法**（CISA 五类）：`component_not_present`（组件实际不在产物里）/ `vulnerable_code_not_present`（有组件无脆弱代码）/ `vulnerable_code_not_in_execute_path`（脆弱代码不在执行路径）/ `vulnerable_code_cannot_be_controlled_by_adversary`（攻击者不可控）/ `inline_mitigations_already_exist`（已有内联缓解）。**理由类型与代码事实不符 = 供应链层的"假不受影响"，重点审计对象**——这正好对接白盒的数据流追踪（脆弱代码是否真在执行路径、是否真不可控）。
- **联动流程**：SBOM 匹配漏洞库产出"潜在受影响"全集 → VEX 逐条裁决收敛到"真实受影响"子集 → AFFECTED 项回本文件证据契约做可利用性验证。
- **冲突处理**：SBOM 说组件在、VEX 说 `component_not_present` → 以产物实测为准（回 Q4）；多来源 VEX 状态冲突 → 取最保守（AFFECTED > UNDER INVESTIGATION > NOT AFFECTED）并记录分歧。

### 供应链审计红旗速查（检测信号）

| 信号 | 含义 | 处置 |
|------|------|------|
| 悬空依赖 | 清单列了但产物无 / 产物有但清单无（Q4） | 逐一核实，可能是投毒或漏报 |
| 身份歧义 | 组件无 PURL/CPE、同名不同源（Q2） | 无法精确匹配漏洞库 → 手工确认来源 |
| 摘要不匹配 | SBOM/attestation 摘要 ≠ 产物摘要 | 清单/证明张冠李戴 → 整份存疑 |
| 过期清单 | 时间戳早于最近构建（Q5） | 清单未反映现产物 → 重新生成 |
| 来源缺失 | 无 provenance 或 SLSA L0 | 无法追溯构建源 → 供应链盲区 |
| builder 不可信 | `builder.id` 非受控 CI（本地/共享 runner） | SLSA 等级名不副实 → 降级 |
| 无消费闭环 | SBOM 生成即归档、无 VEX/门禁（Q8） | 清单不可行动 → 治理缺陷 |
| 假不受影响 | VEX NOT AFFECTED 但 justification 与代码事实矛盾 | 当 AFFECTED，进证据契约验证 |

> **与前文衔接**：供应链发现同样受"证据契约 6 条"约束——SBOM/VEX 提供的是**候选与降噪**，可利用性仍要落到 source-to-sink 可达链。"假不受影响"的核验直接复用 D1-D6 的数据流追踪能力。工具侧：`syft`/`trivy` 生成 SBOM，`grype`/`osv-scanner` 匹配漏洞，`cosign`+`slsa-verifier` 验签与来源，`vexctl` 处理 VEX——工具输出一律回填人工证据链，不直接转漏洞。
