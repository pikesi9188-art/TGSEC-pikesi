---
name: social-engineering
description: 大爱仙尊·假设作业长文（social-engineering）。先专卡后读本文。
---

> 作业先走 Skill `乐土·人情`。未进 scope 不社工。

# Social Engineering（社工 / 钓鱼）

> 仅在根路由选择本目录后读取，且仅限明确授权的社工/钓鱼测试任务。每个条目都是待验证的检查假设。

**触发**: 目标含登录页、第三方 OAuth（Google/TG/GitHub 登录）、支付页、客服系统，且授权范围含钓鱼/社工验证。

## 领域决策直觉

1. 钓鱼核心不是"伪造页面"，是**可信上下文**——克隆真实站点 + 合法域名/HTTPS + 时序对齐（用户刚收到邮件就收到钓鱼）
2. 你的实际场景多是**仿冒目标的后台/客服/充值页**收集运营者或用户的凭据/卡密
3. 优先测**登录态拦截**（evilginx 类反代）而非静态克隆——实时转发保持可信
4. 先评估被钓鱼对象的价值（运营者 vs 普通用户）——运营者凭据 → 后台接管

---

## 凭证钓鱼 / MITM

### Evilginx / AiTM 反代钓鱼
- **信号**: 目标有 OAuth 单点登录（Google/Microsoft/TG 登录），需实时会话
- **验证**: evilginx2 配置目标域 phishlet → 反代转发登录 → 捕获 session cookie（AiTM 绕过 MFA）→ 受害者看到的 URL/证书完全合法
- **证实**: 捕获到有效 session cookie 可重放
- **升级**: 会话重放 → 账户接管（绕过 MFA）
- **注意**: 需要可控域名 + TLS 证书

### 静态页面克隆 + 凭证收集
- **信号**: 目标登录页/客服系统/充值页可克隆
- **验证**: 抓取完整页面（含 JS/CSS/logo）→ 替换表单 action 为收集端 → 部署到高相似域名（同形字/加前缀）→ 诱骗输入 → 后端记录凭据
- **证实**: 收集到目标凭据/卡密
- **升级**: 凭据撞库 → 真站登录

### URL 同形字检测（防守视角，识别别人钓你）
- **信号**: 收到疑似钓鱼 URL
- **验证**: 同形字映射表（0→O、1→l、а→a、е→e、о→o 等）+ 20 种品牌仿冒识别（Google/Facebook/Microsoft/PayPal/淘宝/微信）+ 短链接 HEAD 展开 + IP 直连/非标准端口/过长域名/登录页路径检测
- **证实**: 判定 URL 为仿冒
- **升级**: 反制/标记钓鱼基础设施

---

## 平台生态钓鱼（你的高频场景）

### Telegram / WhatsApp 仿冒
- **信号**: 目标用户群活跃在 TG/WhatsApp，有官方 bot/客服
- **验证**: 伪造同名 bot/客服（加空格/同形字，如 `@official_bot` vs `@off1cial_bot`）→ 诱导授权/付款/发验证码 → 或克隆客服对话界面
- **证实**: 用户向伪造 bot 提交凭据/资金/OTP
- **升级**: OTP 劫持 → 真账户接管

### OAuth 钓鱼
- **信号**: 目标支持第三方登录
- **验证**: 伪造 OAuth 授权页（克隆真实授权界面）→ 诱导用户授权 → 捕获 authorization code/token → 或用已泄露 client_id 的 redirect_uri 缺陷（见 web.md JWT/SAML）
- **证实**: 拿到第三方 token 访问目标数据
- **升级**: 账户接管 → 数据窃取

### 客服/工单钓鱼
- **信号**: 目标有客服系统（Chatwoot/Salesmartly/自研），受害用户已与客服有信任
- **验证**: 冒充客服发钓鱼链接/索要验证码 → 利用已建立的信任关系
- **证实**: 用户在信任上下文内提交敏感信息
- **升级**: 账户接管

---

## 邮件钓鱼

### 鱼叉式 / 批量邮件
- **信号**: 已知目标员工/运营者邮箱 + 命名规则（first.last@ / first@）
- **验证**: 个性化 pretext（最近项目/品牌事件/系统通知）→ 3 种 subject 变体 → 附件/链接（克隆登录页）→ 发信域名用 SPF/DKIM 对齐（或子域接管后的发信权限，见 recon.md）
- **证实**: 目标点击/提交凭据
- **升级**: 凭据 → 真站登录

### 二维码钓鱼（Quishing）
- **信号**: 需要绕过邮件安全网关/URL 过滤/沙箱
- **验证**: 生成含 Logo 的二维码（ERROR_CORRECT_H 容错允许 Logo 嵌入）→ 场景化文案（会议签到/WiFi/扫码领奖）→ 二维码天然绕过邮件网关 URL 检测
- **证实**: 扫码访问钓鱼页并提交信息
- **升级**: 凭据收集

---

## 邮件基础设施（社工前置）

| 需求 | 手法 |
|------|------|
| 发信域名信誉 | 子域接管获得 SPF 子域授权（改 TXT 授权攻击者邮件服务器发伪造邮件） |
| SPF/DKIM 对齐 | 接管 `include:sub.target.com` 的 SPF 子域 → 发信 SPF 通过 |
| 钓鱼域名 | 同形字域名 + 可信 TLS（免费 CA 对同形域签发 DV 证书） |
| 沙箱规避 | 二维码/密码保护 ZIP/JS 重定向链 |

---

## 社工方法论速查（OSINT 前置）

1. **目标画像**: LinkedIn/社工库/公开泄露 → 姓名、邮箱、手机、习惯密码、最近动态
2. **信任锚点**: 冒充对象选受害者信任的人/系统（HR/IT 支持/客服/官方 bot）
3. **时间对齐**: 系统通知类 pretext 卡在真实事件后（刚下单/刚改密/刚注册）
4. **紧迫感 + 合法路径**: "您的账户异常，点击验证" + 链接先指向真实登录页再重定向到伪造页
5. **双因子绕路**: 诱导提供 OTP/备份码，而非强攻密码
6. **验收标准**: 凭据/OTP/session cookie 任一落袋即成功，记录受害者交互链作为证据
