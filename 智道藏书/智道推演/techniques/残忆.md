---
name: forensic
description: 大爱仙尊·假设作业长文（forensic）。先专卡后读本文。
---

> 作业先走 Skill `残忆`；排后门仍走 `host-ir-check`。本文件是长文速查，不替代专卡。

# Forensic / Memory & Traffic & Stego

> 仅在根路由选择本目录后读取，且仅限明确授权的取证/CTF 任务。每个条目都是待验证的检查假设。

**触发**: 目标含内存转储（.raw/.lime）、PCAP 网络流量、隐写图片/音频/文件。

---

## 内存取证（Volatility）

### Volatility 2 vs 3 命令速查

| 需求 | Vol2 命令 | Vol3 命令 |
|------|-----------|-----------|
| 进程列表 | `pslist` | `windows.pslist` |
| 隐藏进程 | `psscan`（pool tag 扫描，找未链接进程） | `windows.psscan` |
| 进程树 | `pstree` | `windows.pstree` |
| 网络连接 | `netscan` | `windows.netscan` |
| 代码注入 | `malfind`（找 RWX 无文件映射区） | `windows.malfind` |
| DLL 分析 | `ldrmodules -p PID`（三列全 False = 反射注入） | `windows.ldrmodules` |
| 哈希提取 | `hashdump` | `windows.hashdump` |
| 文件扫描 | `filescan` | `windows.filescan` |
| Linux | `linux_bash`/`linux_check_syscall`/`linux_check_afinfo` | `linux.bash`/`linux.check_syscall` |

### 关键检测模式
- **隐藏进程**: 进程在 `psscan` 中出现但 `pslist` 没有 = DKOM 隐藏
- **DLL 反射注入**: `ldrmodules` 三列全 False
- **代码注入**: `malfind` 找到 RWX 内存区域且无文件映射
- **正常进程参考**: System → smss → csrss/wininit → services → svchost
- **Linux rootkit**: `linux_check_syscall`（syscall 表被篡改）/ `linux_check_afinfo`（netfilter 钩子）/ `tty_check`（tty 写入器 rootkit）
- **CTF 流程**: `filescan | grep -E "flag|secret|passwd"` → `cmdline` → `memdump` → `clipboard` → `screenshot`
- **Linux profile（Vol2）**: `tools/linux make` + System.map 打包 zip；Vol3 自动

---

## 流量分析（PCAP）

### 修复 / 基本分析
- `pcapfix corrupted.pcap -o fixed.pcap`（修损坏 pcap；magic: d4c3b2a1 LE / a1b2c3d4 BE / 0a0d0d0a pcapng）
- `tshark -q -z io,stat,1`（流量统计）/ `-z conv,tcp`（会话统计）
- `--export-objects http,/tmp/`（导出 HTTP 对象）

### 协议分析速查
- **HTTP 解密**: `SSLKEYLOGFILE` 环境变量 + Wireshark TLS 偏好设置
- **USB HID 键盘**: `tshark -r usb.pcap -Y "usb.capdata && usb.data_len==8" -T fields -e usb.capdata` → keycode 0x04-0x1d=a-z 映射解码
- **NTLM 提取**: `user::domain:challenge:NTProofStr:blob` → hashcat `-m 5600`
- **WPA 握手**: `hcxpcapngtool` 转化 → `-m 22000`
- **DNS 隧道检测**: 子域名 >30 字符 / `dns.qry.name.len > 50`
- **FTP 凭据**: `tshark -r capture.pcap -Y "ftp.request.command==USER || ftp.request.command==PASS" -T fields -e ftp.request.arg`

---

## 隐写术

### 工具链（按文件类型）
- **全自动**: `stegoveritas image.png`（一次跑 exiftool/binwalk/zsteg/foremost/color plane）
- **PNG/BMP LSB**: `zsteg image.png`（自动检测 LSB 模式）/ `-a` 全部方法 / `-b 1` 提取位平面 / `-E "b1,rgb,lsb,xy"` 指定提取模式
- **JPEG**: `steghide extract -sf image.jpg`（需要密码）
- **文件隐写**: `binwalk -Me file`（提取嵌入文件）/ `foremost file`（文件雕复）
- **音频**: Sonic Visualiser 频谱分析（Window=4096, Overlap=87.5%）/ 音频 LSB 提取
- **文本**: 零宽字符（U+200B/C/D/FEFF → 二进制 → ASCII）/ `stegsnow` 空白字符隐写
- **NTFS**: `type file > :hidden.txt`（备用数据流 ADS）
- **PNG CRC 爆破**: 暴力遍历宽高算 IHDR CRC 匹配真实宽高
- **WAV 大小异常**: `frames*channels*samplewidth+44` 对比实际文件大小
