---
name: blockchain
description: 大爱仙尊·假设作业长文（blockchain）。先专卡后读本文。
---

> 作业先走 Skill `链上`。本文件是长文速查，不替代专卡。

# Blockchain / Smart Contract

> 仅在根路由选择本目录后读取，且仅限明确授权的智能合约审计/Web3 任务。每个条目都是待验证的检查假设。

**触发**: 目标含智能合约（EVM/Solidity）、DeFi 协议、代币/桥/治理，或需 Web3 安全审计。

## 领域决策直觉

1. 审计入口按决策树走：代理模式→存储布局冲突；外部调用→重入；代币处理→整数溢出；签名→重放；链上随机→可预测
2. DeFi 攻击核心是"操纵价格/借闪贷/抢跑"——先看协议依赖什么价格来源（现货 vs TWAP vs Chainlink）
3. 工具链：Slither / Echidna（模糊测试）/ Foundry forge / Mythril

---

## 智能合约漏洞（Solidity）

### 重入（Reentrancy）
- **信号**: 合约做外部调用（`call`/`transfer`/`send`），且先调用后更新状态（违反 CEI 模式）
- **验证**: 经典单函数重入（fallback 递归调用提款）→ 跨函数重入（共享状态）→ 跨合约重入 → 只读重入（回调期间 view 函数读到旧状态）
- **证实**: 递归提款耗尽合约余额
- **升级**: 资金盗取 → 协议清空
- **决策**: 状态在调用前更新？→ CEI 模式 OK；ReentrancyGuard 在所有入口点都有？→ 检查跨函数

### 整数溢出/下溢
- **验证**: Solidity <0.8 无内置检查 → 全部算术可疑；`unchecked{}` 块内用户可控值；uint 尺寸转换截断；SafeMath 绕过场景
- **证实**: 余额/总量被操纵
- **升级**: 铸币/烧币异常 → 价格失真

### 访问控制 / tx.origin
- **验证**: `tx.origin == owner` 模式（钓鱼可绕过）vs `msg.sender`；初始化函数可二次调用；管理员函数缺权限检查
- **升级**: 权限提升 → 合约控制权

### 链上随机可预测
- **验证**: `blockhash`/`block.timestamp`/`block.difficulty` 作随机源 → 攻击者可在同一区块内预先计算并抢跑
- **建议**: Chainlink VRF 或 commit-reveal + 质押

### delegatecall / 存储布局冲突
- **信号**: 代理升级模式（UUPS/Transparent）
- **验证**: proxy 与 implementation 的 slot 分配冲突 → 写坏状态；函数选择器碰撞；initializer 可二次调用；selfdestruct 强制转账
- **升级**: 代理存储被篡改 → 完全控制

### 签名重放
- **验证**: 签名缺 nonce（可重复提交）→ 缺 chainId（跨链重放）→ `ecrecover` 结果未检查 `address(0)`
- **升级**: 跨链/重复执行签名操作

### 审计决策树（完整）
```
代理模式? → 存储布局/选择器碰撞/initializer 二次调用
外部调用? → 重入: 调用前更新状态? ReentrancyGuard 全覆盖? 跨函数/只读重入
代币/ETH? → 溢出(<0.8 全部算术/unchecked/截断) + selfdestruct 强制转账
签名? → nonce? chainId? ecrecover 检查 address(0)?
链上随机? → 可预测 → 建议 VRF
```

---

## DeFi 攻击模式

### 闪贷攻击（Flash Loan）
- **信号**: Aave/Uniswap 等闪贷源，无抵押大额借贷
- **验证**: 闪贷借治理代币 → 投票 → 归还（协议不拍快照则可行）→ 闪贷操纵价格（借币砸池 → 价格失真 → 套利）→ 流动性池重入抽干 → 治理闪贷
- **升级**: 资金盗取 / 协议操控

### 价格预言机操纵
- **验证**: AMM 现货价（单池可操纵）→ 大额 swap 改变 spot price → 触发清算/套利；TWAP 抗短期操纵但窗口可被攻击；Chainlink 陈旧价（stale）→ 用旧价套利
- **决策**: 协议用现货价？→ 单笔大 swap 可操纵；TWAP → 检查窗口；Chainlink → 查 `latestRoundData` 的 updatedAt 陈旧性

### MEV（抢跑/三明治）
- **验证**: 三明治攻击（大买单前买入 → 抬高 → 卖出）→ JIT 流动性（临时的 LP 入场狙击）→ 清算 MEV（抢先清算获利）
- **升级**: 普通用户损失 / 套利者收益

### 精度损失利用
- **验证**: 舍入错误（`amount / total * x` 顺序）→ 首存款人/金库通胀攻击（先存 1 wei 抬高份额价值）→ dust 攻击（精度截断积累）
- **升级**: 份额价值操纵 → 资金盗取

### 治理攻击
- **验证**: 闪贷治理投票（协议不拍快照）→ Timelock 绕过（设 0 / `emergencyExecute` / 守卫多签单点）→ 法定人数操纵（闪借 10% 代币 → 提案设 admin=攻击者 → 无 timelock 立即执行 → 归还）
- **升级**: 协议治理权夺取 → 完全控制

---

## 工具链

| 工具 | 用途 |
|------|------|
| Slither | 静态分析（重入/溢出/权限） |
| Echidna | 基于属性的模糊测试 |
| Foundry (forge) | 快速测试 + PoC |
| Mythril | 符号执行漏洞检测 |
| Hardhat + 主网 fork | 真实环境复现 DeFi 攻击 |
| web3.py / cast | 链上交互 |
