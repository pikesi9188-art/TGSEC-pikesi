---
name: recon
description: 大爱仙尊·假设作业长文（recon）。先专卡后读本文。
---

> 作业先走 Skill `云帷` / `origin_recon.py`。本文件是长文速查，不替代专卡。

# Recon（信息收集）

> 仅在根路由选择本目录后读取。每个条目都是待验证的检查假设；执行前用当前一手证据确认适用性。

**触发**: 目标是 Web/API/域名/企业，需最大化发现资产、域名、IP、子域、APP、小程序、云存储，尤其在初始范围只有一个公司名或一个主站时。

---

## 通用信息收集

### CDN 绕过 → 真实源站 IP + 内部主机名泄露
- **信号**: CNAME 指向 Cloudflare/Akamai/阿里云CDN/腾讯云CDN
- **假设**: 源站 IP 隐藏但可通过证书/DNS 历史/特征反查；内部主机名（jenkins/staging/admin-portal）也可能通过 CT 日志泄露
- **验证**: `curl -H "Host: target.com" https://<候选IP>` 返回目标内容
- **找 IP 管线**: SecurityTrails DNS 历史 A 记录时间线 → FOFA `cert.serial_number="<hex>"` + Shodan `ssl.cert.serial:<dec>` + Censys 三引擎 SSL 证书反查 → Favicon MurmurHash3 跨引擎反查(shodan `http.favicon.hash:<mmh3>` / fofa `icon_hash=<mmh3>`) → DNS Cache Snooping massdns `--norecurse` 非递归查询权威 NS → **CT 日志全量采样**：监控 CA 全量签发流，企业经常为内部主机（jenkins.internal.corp.com/admin-portal.staging.int）签发公网证书，这些出现在 SAN 字段但不属于目标查询范围
- **证实**: 两个以上引擎指向同一 IP + HTTP 直接访问响应内容匹配；或发现未公开的内部主机名
- **证伪**: 所有候选 IP 返回 CDN 错误页或超时 → 源站可能有 IP 白名单
- **升级**: 拿到源站 IP 后跳过 WAF，直接端口扫描 + 漏洞探测；内部主机名 → ASL 追加 → 定向测试

### CDN 溯源方法矩阵（26 种组合拳）
- **信号**: 目标有 CDN（`www.target.com` 多地 ping 返回不同 IP，CNAME 指向 CDN 厂商）
- **假设**: 单方法成功率有限，3 种以上组合可达 95%+。推荐顺序：**证书日志 → 子域枚举 → 历史 DNS → 大爱仙尊 → Host 头验证**
- **验证方法按成功率排序**:
  1. **SSL 证书透明度日志** (85%): crt.sh/Censys/CertSpotter 查 SAN 字段 → 发现内部主机名（jenkins/staging）和未挂 CDN 的源站
  2. **子域名枚举** (80%): 非主要域名（api/dev/cdn-origin）常不挂 CDN，直接解析到源站 IP
  3. **历史 DNS 记录** (75%): SecurityTrails/HackerTarget/VirusTotal 查目标上 CDN 前的真实 A 记录
  4. **网络空间搜索引擎** (70%): Shodan `ssl.cert.serial:<dec>` / FOFA `cert="<hex>"` / Censys 三引擎 SSL 证书反查 → Favicon MurmurHash3 跨引擎 `icon_hash=<mmh3>` → body 唯一特征串全网搜 `body="<唯一串>"`（见下方 body 特征串条目）
  5. **CNAME 解析链追踪** (70%): `dig CNAME target.com +short` 追踪完整链，链中间跳有时暴露源站
  6. **CDN IP 范围反查** (65%): 已知 CDN IP 段（kaeferjaeger.gay/ASN 前缀），排除 CDN IP 后剩候选
  7. **SPF 邮件记录** (60%): `dig txt target.com` 展开 `include:`/`ip4:` → 企业自持 IP 段常含源站
  8. **MX 邮件服务器关联** (55%): `dig MX target.com` → 邮件服务器 IP 往往不挂 CDN，同段可推测源站
  9. **Host 头验证** (55%): 候选 IP 发 `curl -H "Host: target.com" https://<IP>` → 返回目标内容即命中
  10. **第三方服务 ID 关联** (50%): GA UA ID/百度统计/腾讯分析 ID 跨域反查 → 发现同 ID 的未挂 CDN 站点
  11. **ICP 备案查询** (45%): 国内站 `beian.miit.gov.cn` 查备案号 → 关联域名 IP 可能未挂 CDN
  12. **AltDNS 排列 + 泛解析过滤** (45%): `altdns`/`dnsgen` 基于已知子域名模式生成变体 → 过滤泛解析干扰
  13. **XML-RPC Pingback 泄露** (40%): WordPress pingback 返回源站 IP（`curl -d '<methodCall>...pingback.ping...' http://target.com/xmlrpc.php`）
  14. **邮件头分析** (40%): 触发目标发信 → 读 `Received:` 链最底部一跳 + `X-Originating-IP`
  15. **Favicon 哈希匹配** (35%): Shodan `http.favicon.hash:<mmh3>` / FOFA `icon_hash=<mmh3>` 找同 favicon 的未挂 CDN 站点
  16. **CSP/CORS 头泄露** (40%): `Content-Security-Policy` 的 `connect-src`/`report-uri` 可能含源站域名；`Access-Control-Allow-Origin` 可能回显源站
  17. **CDN 回源 IP 段利用** (55%): 查 CDN 官方文档的回源段（Cloudflare/Akamai 等），排除后剩候选
  18. **IPv6 解析** (25%): 部分站点 IPv6 不挂 CDN → `nslookup -type=AAAA target.com` 拿到源站
  19. **DNS 区域传送** (15%): `dig AXFR target.com @ns1.target.com` → 全量 DNS 记录泄露
  20. **被动 DNS 数据聚合** (80%): 多源（SecurityTrails + PassiveDNS.cn + CIRCL + DNSDB）交叉验证
  21. **证书颁发机构 (CA) 分析** (50%): 同一 CA 签发的不同域名合集 → 关联发现
  22. **HTTP 重定向链分析** (35%): `curl -L -v target.com` 追踪重定向链，中间跳可能暴露源站 IP
  23. **多 DNS 解析器交叉验证** (60%): `dnsx`/`massdns` 用多个公众 DNS 解析同一域名 → 对比差异判断 CDN 节点
  24. **网站功能泄露** (50%): RSS 订阅源、`robots.txt`、`sitemap.xml` 中的 IP 或内部域名
  25. **JS/API 端点泄露** (45%): 前端 JS 中硬编码的 API host 或内部域名
  26. **DNS Cache Snooping** (补充): `dig +norecurse @resolver` 查缓存命中 → 非权威回答（被缓存过）表示该域名真实使用
- **置信度评分**: 每命中 1 种方法 +5~15%，3 种交叉即 95%+；候选验证首选 `httpx -probe -status-code -title -tech-detect -H "Host: target.com" https://<候选IP>`
- **证实**: 候选 IP 返回目标内容 + 至少两个独立引擎指向同一 IP
- **升级**: 拿到源站 IP → 跳过 CDN/WAF → 直接端口扫描 + 漏洞探测

### JS Source Map → 源码还原 + 端点提取
- **信号**: 目标使用 Webpack/Vite 打包且 `.map` 文件可访问
- **假设**: Source map 包含原始目录结构、变量名、注释和隐藏端点
- **验证管线**: `unwebpack-sourcemap` 还原完整目录结构 → `LinkFinder` 提取 API 路由/WebSocket endpoint → `SecretFinder` 提取 20+ 种凭证类型 → headless browser 渲染 SPA 用 paramFuzzer 提取动态参数 → SubDomainizer 提取云存储 URL + 隐藏子域名
- **证实**: 发现未文档化的 API 端点或硬编码凭证
- **升级**: 新端点 → ASL 追加 → 进入 API Fuzzing 流程

### 企业拓线 → 关联资产发现
- **信号**: 目标为企业，需扩大攻击面到子公司/供应商
- **假设**: GA/Facebook Pixel/Hotjar 追踪 ID 可跨域关联同一组织
- **验证管线**: GA/Hotjar 追踪 ID 反向查询关联域名 → ICP 备案反查（ENScan_GO 同一主体/手机号/邮箱）→ WHOIS 历史 + 注册邮箱 + Name Server + SPF 交叉关联 → Crunchbase/OpenCorporates 母子关系 + 高管关联 → CrossLinked 抓 LinkedIn 员工邮箱反推命名规则
- **证实**: 发现未在初始范围内的关联域名或资产
- **升级**: 新域名 → RECON 状态重启（独立 ASL 条目）

### 云存储资产发现
- **信号**: 目标使用 AWS/Azure/GCP/阿里云/腾讯云
- **假设**: 存在公开或可枚举的对象存储桶
- **验证**: `cloud_enum` 多云关键词变异爆破（企业名+缩写+产品名+环境名组合）→ GCP `TestIamPermissions` API 未认证检测 → s3recon 逐位爆破利用 IAM 条件键 s3:ResourceAccount 确定 12 位 Account ID → SubDomainizer 从 JS 自动提取云存储 URL
- **证实**: Bucket 可以未认证读取/写入/列目录
- **升级**: Bucket 内容分析 → 敏感信息提取 → 云身份横向

### 版本控制 / 备份文件泄露 → 源码还原
- **信号**: 目标可能残留 `.git`/`.svn`/`.DS_Store` 目录或备份/临时文件
- **假设**: 泄露目录可还原服务端源码、配置、DB 口令、目录结构
- **验证**: `.git`→GitHack/GitHacker（GitHacker 能挖已删 commit 与分支/stash）；`.svn`→dvcs-ripper `rip-svn.pl`；`.DS_Store`→ds_store_exp 递归下载引用文件；备份/临时文件 fuzz `www.zip`/`网站名.zip`/`.bak`/`.swp`/`.orig`；Java 站测 `WEB-INF/web.xml`、`WEB-INF/classes`（class 反编译）
- **证实**: 拿到整站源码/配置或接口映射
- **升级**: 源码转白盒审计（配置/硬编码凭据/后门）→ 新端点 ASL 追加

### 配置与调试信息泄露
- **信号**: Spring Boot 应用（`/actuator/` 响应）、Flask 应用（`/console` 可访问）、PHP 应用（`phpinfo()` 页面）、`.env` 文件可读取
- **假设**: 配置信息/调试接口未经保护暴露敏感参数
- **验证**:
  - 敏感路径 fuzz：`/.env`、`/application.yml`、`/WEB-INF/web.xml`、`/actuator/env`、`/actuator/heapdump`（堆 dump 翻出口令/AKSK）→ `/swagger-ui.html`/`/v2/api-docs`/`/v3/api-docs` 接口文档泄露 → GraphQL 内省 `__schema` 查询泄露全部类型 → `/console`（Flask/Werkzeug 交互式执行，结合 PIN 计算可 RCE）→ Django `DEBUG=True` 配置/SQL 全展示 → 报错堆栈泄露绝对路径/框架版本/SQL 语句
  - 前端源映射：`/static/js/app.js.map` → `unwebpack-sourcemap` 还原源码，`LinkFinder` 提取 API 路由，`SecretFinder` 提取 20+ 种凭证
  - 状态码语义：200/403/404 分别代表"可读/存在但禁读/不存在"，403 也值得换姿势再试
- **证实**: 获取到敏感配置、凭据、接口文档或源码
- **升级**: 凭据/密钥 → 直接登录/API 调用；源码转白盒审计 → 新漏洞发现
- **工具**: dirsearch/ffuf 扫敏感路径；GitHack/GitHacker 还原 git；heapdump 分析工具（Eclipse MAT/`heapdump_tool`）

### 云密钥 / GitHub 泄露搜索
- **信号**: 目标企业名/开发者名/项目名
- **验证**: GitHub/Gitee 搜索 `"target.com" AKID`/`AccessKeyId`/`SecretAccessKey`/`LTAI`（阿里云 AK 前缀）→ APK/小程序反编译翻 AKSK → JS bundle 硬编码密钥 → 验证：`aws sts get-caller-identity`（临时凭证有效即可用）

### 被动归档 → 历史 URL + 隐藏子域

### 被动归档 → 历史 URL + 隐藏子域
- **信号**: 目标有历史资产、旧端点、下线接口
- **假设**: Wayback/Common Crawl 归档了历史 URL 与参数，含已下线端点和带参 URL（`redirect=`/`file=`/`url=` → SSRF/开放重定向线索）
- **验证**: `gau target.com`（一次拉 Wayback+Common Crawl+OTX+URLScan）/ `echo target.com | waybackurls`；Common Crawl CDX `https://index.commoncrawl.org/` 用 `*.target.com` 通配挖子域，全程不触达目标
- **证实**: 发现现役枚举漏掉的旧端点/子域/敏感参数
- **升级**: 旧端点 → 参数 Fuzzing；子域 → ASL 追加

### 被动 DNS → IP↔域名双向反查
- **信号**: 需从一个已知 IP 反查其历史绑过的全部域名，或补充非 SecurityTrails 的解析源
- **假设**: 被动 DNS 记录了 IP 上过 CDN 之前的真实解析和历史绑定域名（带 first/last-seen）
- **验证**: 微步在线 x.threatbook.com / PassiveDNS.cn / DNSDB rdata 反向搜索（IP→域名）/ CIRCL / `amass enum -passive -d target.com`
- **证实**: 一个 IP 反查出多个关联域名，或发现旧解析暴露的源站
- **升级**: 新域名/源站 IP → RECON 重启 / 直连探测

### 子域接管（CNAME/NS/MX 悬空）
- **信号**: 子域 CNAME/NS/MX 指向已注销的云资源或第三方 SaaS 租户
- **假设**: CNAME 后的资源无人认领且可注册 → 攻击者在目标域名下控制内容
- **验证**:
  1. `subjack`/`nuclei -t takeovers/`/`dnsreaper`/`subzy` 批量扫 CNAME 悬空
  2. 关键指纹: S3 `NoSuchBucket`(404) / GitHub Pages `There isn't a GitHub Pages site here` / Heroku `No such app` / Fastly `unknown domain` / Cloudflare Workers `Worker not found`(1101) / Azure `Web Site Not Found` / Shopify `currently unavailable` / 2026 新增 `*.fly.dev` `*.render.com` `*.workers.dev` `*.pages.dev` `*.replit.app` `*.vercel.app`
  3. 重注册同名资源（S3 建同名桶 / GH 建同名 repo+CNAME 文件 / Heroku 同名 app）→ 绑定目标子域
  4. **NS 接管**（最高危）: `dig NS target.com` → NS 域名过期可注册 → 控制整个 zone 的 DNS（可发 DV 证书、改 SPF/DKIM 发伪造邮件）
  5. **MX 接管**: MX 指向已停用邮件服务 → 认领租户 → 拦截密码重置邮件
  6. **CNAME 链**: 追 2-3 跳（`docs.target.com → readthedocs.io(已NXDOMAIN)`），中间跳常被遗忘 → 最高价值
  7. 通配 DNS: `dig A random123.target.com` 若解析 → `*.target.com` 全部子域可接管
- **证实**: 在目标子域下托管内容 / 收到目标域的邮件
- **升级**: cookie 窃取（父域 `.target.com` 作用域）→ OAuth redirect_uri 劫持 → CSP `*.target.com` 白名单绕过 → 2026 AI 时代: 接管 Edge Function/AI API 端点截获 prompt+API key
- **AI 盲区**: 模型常混淆"CNAME 存在"与"可接管"——关键是 CNAME 后资源是否**无人认领且可注册**（S3 返回 403 已私有 ≠ 可接管，只有 NoSuchBucket 才是）

### DNS 协议层攻击面（DNSSEC 错配 / 重绑定 / TSIG / 缓存伪造）
> DNS 协议层 runbook。子域接管打的是「资源无人认领」，这里打的是「DNS 协议/服务配置本身」。均属低影响验证——只做被动观测+受控 lab 复现，禁对生产递归投毒。
- **DNSSEC 验证与错配**（信号：域启用 DNSSEC，返回 RRSIG/DNSKEY/DS）
  - 失败条件：DS 与父区不匹配、RRSIG 过期、算法回退、`SERVFAIL` 被解析器忽略当明文放行、NSEC/NSEC3 泄露全量记录（NSEC 遍历枚举 zone）
  - 验证：`dig +dnssec DNSKEY target.com`、`delv @解析器 target.com`、`dig NSEC3PARAM`；检查签名有效期窗口与 DS 一致性
- **DNS 重绑定**（信号：Web 应用/IoT/云控台按 hostname 而非 IP 做信任判断）
  - 手法：控制权威域，首次解析返回真实公网 IP 过 SOP，短 TTL 后重解析到 `127.0.0.1`/内网 → 浏览器/服务端后续请求打内网。与 SSRF 互补（SSRF 打服务端，重绑定打「受害者浏览器为跳板」）
  - 验证：自建低 TTL A 记录 + 双 IP 轮换（如 rebind.network/自搭 dnschef），隔离 lab 观测第二次请求落点。防御看是否校验 Host 头+DNS pinning
- **TSIG / 动态更新**（信号：`dig` 允许 `UPDATE`，或 AXFR 需 TSIG）
  - 失败条件：TSIG 密钥弱/泄露/算法用 MD5、动态更新未鉴权 → 可写 zone 记录（加自己的 A/TXT，等价 zone 内接管）
  - 验证：`nsupdate` 试无鉴权 UPDATE；找配置泄露的 TSIG key（常在 named.conf/备份/git）
- **公开递归 + 缓存伪造前置**（信号：目标 DNS 对外开放递归）
  - 失败条件：开放递归可被滥用做放大 DDoS 反射源、源端口/TXID 熵不足给缓存投毒留窗口
  - 验证：`dig @目标DNS google.com`（非本域仍应答=开放递归）；观测响应源端口是否随机化。仅记录前置条件，不实施投毒
- **悬空记录再证据**：与上面子域接管联动，DNS 层额外看 `dig ANY` 残留的历史指向（旧 TXT/SPF include 已失效域也是拓线面）

### 邮件认证记录 → 关联域名 + 自有 IP 段
- **信号**: 目标有企业邮箱（几乎所有企业域都配 SPF/DMARC/MX）
- **假设**: SPF include 链、DMARC rua、MX/DKIM 泄露第三方 SaaS、兄弟/子公司域名，`ip4/ip6` 机制直接列自有 IP 段（SPF flattening 会拍平成裸 IP 列表）
- **验证**: `dig txt target.com`（递归展开 `include:`/`redirect=`/`ip4:`）；`dig txt _dmarc.target.com`（读 `rua=`/`ruf=` 收件域）；`dig mx`/`dig txt selector._domainkey.target.com`
- **证实**: 发现关联域名或企业自持 IP 段
- **升级**: IP 段 → ASN/C 段扩面；关联域 → ASL 追加

### ASN / BGP → 企业整段 IP → C 段反查
- **信号**: 大目标需界定地址空间（高校/集团常自持 B/C 段）
- **假设**: 企业 BGP 宣告的 CIDR 内有未上 CDN 的真实资产与旁站
- **验证**: `asnmap -org "COMPANY"` / `asnmap -d target.com`（输出全部 CIDR）→ C 段落地：DomainClassCIPScan（C 段域名反查）+ `nmap -sn` 探活 + PTR 反向解析
- **证实**: 同段内发现子域枚举覆盖不到的旁站/兄弟系统
- **升级**: 新资产 → 端口扫描 + 指纹 → ASL 追加

### 邮件头 Received 链 → 真实源站 IP（绕 CDN）
- **信号**: 目标接 CDN 隐藏源站，但会主动发信（注册激活/找回密码/订阅确认/退订）
- **假设**: CDN 只加速 Web，邮件多从源站直发；邮件头暴露源站真实 IP 与内网信息
- **验证**: 触发目标发信 → 看邮件"原始/源代码" → 读 `Received:` 链最底部（最早）一跳 + `X-Originating-IP`/`X-Sender-IP`（HELO/EHLO 握手常带客户端 LAN IP）
- **证伪**: 目标用 SendGrid/Mailgun/阿里云邮件推送 → 拿到的是服务商 IP
- **证实**: `curl -H "Host: target.com"` 验证候选 IP 返回目标内容
- **升级**: 拿到源站 IP → 跳过 WAF 直接探测

### GoogleHack 以点拓面（同 LAN 易控守目标）
- **信号**: 主目标难攻破，需迂回找同服务器/同 C 段的易控守目标
- **假设**: 子域应用与终极目标相关性高，同 C 段 IP 有较高相关性；找到易控守的旁站/同服务器应用即可突破
- **验证**（快速模式）: Google/ZoomEye 利用技巧找相关应用（struts2/Jboss/FCKeditor 等易控守漏洞）→ 四类思路——①易获取权限的漏洞（struts s2-016、editor 系列）②被 Hacked 的网站/webshell ③配置错误（可列目录/报错）④Admin 后台
- **经典搜索规则**: DNN `inurl:Portals/0/` + FCKeditor（`fcklinkgallery.aspx` 上传 + asp 解析 `Dz4aLL.asp;me.jpg`）/ CuteEditor `inurl:CuteSoft_Client/` + `Load.ashx?type=image&file=../../../web.config` 穿越 / HttpFileServer 2.3 `intext:ServerTime: HttpFileServer 2.3` + `%00{.exec|cmd}` / JBoss `JMXInvokerServlet` / 列目录 `intitle:index of /`
- **证实**: 找到同网段/同服务器的可控守漏洞
- **升级**: 旁站突破 → 同服务器其他应用/内网

---

## 国内特有手法

### 大爱仙尊：进阶语法

> FOFA/Quake/Hunter/ZoomEye 数据互补：Quake 非 Web/工控/未授权指纹最细；Hunter/SUMAP 备案关联最强；FOFA 亚洲 Web 覆盖广且有 fid/is_honeypot 独有字段。精确 pivot 用 `==`，跨平台换字段名。

#### ICP 备案号反查全主体资产
- **信号**: 已拿到目标一个资产，需从企业主体维度反向扩面（国内引擎独有能力，Shodan/ZoomEye 没有）
- **假设**: 同一 ICP 主体名下常备案大量域名/资产，通过备案号可一键捞全
- **验证**: 从已知站提取备案号 → Hunter `icp.number="京ICP备XXX号"`，可叠加 `&& ip.port="8080"` 收敛暴露服务；Quake/SUMAP 有同类字段
- **证实**: 返回该主体名下未在初始范围的域名/IP
- **升级**: 新域名 → RECON 重启（独立 ASL 条目）→ 二次子域名收集

#### fid 站点结构聚类
- **信号**: 目标把同一套 Web 应用部署在多个分散 IP/域名（镜像、负载节点、SaaS 多租户）
- **假设**: `fid` 基于站点整体结构生成，比 `icon_hash` 更精细，能聚出整个集群
- **验证**: 先查一个已知站拿 `fid` → FOFA `fid="<值>"` 全网 pivot
- **证实**: 捞出跨 IP/域名的同源站集群
- **升级**: 集群内逐个探测，找防护最弱节点打点

#### 蜜罐/欺诈数据净化（HVV 必备）
- **信号**: 批量拉取某框架资产准备打点，需避免踩蜜罐触发告警
- **验证**: FOFA 查询追加 `&& is_honeypot=false && is_fraud=false`（国内引擎独有字段）
- **证实**: 结果集剔除蜜罐节点与仿冒数据后更干净
- **升级**: 净化后的资产进入指纹叠加打点流程

#### 降噪三件套（精确 pivot 基础）
- **信号**: 做证书/标题/指纹 pivot 时误报多（多数引擎默认分词模糊匹配）
- **验证**:
  - `==` 强制精确大小写敏感匹配（FOFA `domain=="qq.com"` / ZoomEye `title=="XXX后台"`），把"包含"收敛为"等于"
  - `!=` 排跳转空壳（`body!="Object moved" && title!="302"`），剔除 CDN/WAF 的 302 空壳页
  - `ip_ports="80,161"` 多端口共开特征，精确定位特定设备/源站（如同时开 Web+SNMP 的网络设备）
- **证实**: 结果噪声显著下降，pivot 命中率提升

#### body 特征串全网找裸奔源站（绕 CDN 补充）
- **信号**: 源站接 CDN，前端 IP 隐藏，但源站在 80/8080/8443 等端口可能仍原样返回同一 body
- **假设**: 页面里独一无二的字符串（内联 JS 版本、构建 hash、后台标识）在源站未接 CDN 时全网可搜到；比 favicon 更抗改（有些站改了图标但没改 body）
- **验证**: 提取页面唯一特征串 → FOFA `body="<唯一串>"`（去掉 host/domain 限制全网搜）→ 候选 IP 用 `curl -H "Host: target.com"` 验证
- **证实**: 命中未接 CDN 的真实源站 IP
- **升级**: 拿到源站 IP → 跳过 WAF 直接端口扫描 + 漏洞探测

#### 测试/预发布/内部系统定向发现
- **信号**: 目标存在测试/预发布环境（防护薄弱、鉴权缺失、带真实数据，且常不接 CDN 直暴源站）
- **假设**: 环境命名习惯 + 内部系统 body 标识可批量捞出
- **验证**: FOFA `domain="target.com" && (host="uat"||host="dev"||host="pre"||host="staging"||title="测试"||body="内部系统"||body="仅供内部使用")`；Hunter `domain.suffix="target.com" && web.title="测试环境"`
- **证实**: 发现打点突破口——弱防护的非生产环境
- **升级**: 定向测试鉴权缺失/默认口令 → ASL 追加

---

### 企业拓线：小程序 / APP / 股权穿透

> 国内目标核心增量。推荐主链路：企业名 → 股权穿透枚举全部关联主体 → 每主体分别做 ICP/APP/公众号/小程序反查 → 拿到的 APP/小程序做静态提取挖后端接口与 AK/SK → 后端域名/IP 回大爱仙尊 icon_hash+cert+C段横向扩面。

#### 微信小程序 wxapkg 反编译提取接口/域名/密钥
- **信号**: 目标有微信小程序（后端 API 往往比主站防护弱、暴露隐藏域名）
- **假设**: 小程序包内硬编码后端 API host、隐藏域名、OSS/云密钥、未授权接口
- **验证**:
  1. 微信打开目标小程序使其缓存到本地。新版微信缓存约在 `AppData\Roaming\Tencent\xwechat\radium\Applet\packages`；旧版在 `WeChat Files\Applet\wx{appid}\`
  2. 判断结构：只有 `__APP__.wxapkg`=主包；多个 `.wxapkg`=分包，需全部反编译
  3. 反编译：`unveilr <文件夹>`（自动处理分包，最推荐）或 wedecode（活跃维护）或 wxappUnpacker
  4. 导入结果搜索：域名、API 接口、`ossAccessKeyId`/AK-SK、电话、邮箱
- **证实**: 提取到未公开的后端域名/接口或可用 AK/SK
- **升级**: 新域名 → 测绘扩面；AK/SK → aksk_tool 反拉云资产（见 cloud.md）

#### APP 资产反查 + 静态提取
- **信号**: 目标企业有移动 APP（含被遗忘的老旧/子品牌 APP）
- **假设**: 同主体名下多个 APP 各自有独立后端资产；APK/IPA 内硬编码域名/IP/AK-SK
- **验证**:
  - 反查全部 APP：七麦(qimai.cn)/点点(app.diandian.com)按开发者反查该主体名下所有 iOS/安卓 APP；爱企查从软著维度补充
  - 静态提取：`AppInfoScanner`（apk/ipa 批量提域名/IP/URL/AK-SK）、`apkleaks`（正则扫 URI/endpoints/secrets）、MobSF（综合）
- **证实**: 发现独立后端接口、第三方服务域名、云密钥
- **升级**: 后端域名/IP → 测绘扩面；接口 → API Fuzzing（web.md）

#### 股权穿透 + 多维聚合拓线
- **信号**: 根域名/主体都找不全，需从组织架构维度破局（国内 SRC 起手式）
- **假设**: 资产常藏在子公司/关联公司名下；沿股权关系下钻可拉全域名+APP+小程序+公众号
- **验证**:
  - 股权底座：`EquityInfoer` 拉天眼查股权穿透（母子公司/实控人链路，关注 100% 控股）
  - 一键聚合：`ENScan_GO`（狼组）聚合爱企查/天眼查/快查 API，一次输出 ICP备案+APP+小程序+公众号+控股关系
  - 官方去噪：`ICP_Query_Batch` 直连工信部对一批主体批量查备案（含 APP/小程序备案）
- **证实**: 发现关联主体名下未在初始范围的资产
- **升级**: 每个新主体/域名 → RECON 重启，喂给 OneForAll/水泽做二次子域名收集

---

## 轻量工具速查（受限环境 fallback）

> 目标主机/攻击机无完整工具链时，以下纯 Python 单文件工具可用（asyncio/标准库实现，不依赖 nmap/masscan）。

### 标准扫描流程（回答三问）
> 扫描不是把所有工具跑一遍，而是尽快回答：①开放哪些端口/服务 ②协议/产品/版本 ③哪些入口值得深挖。
- **主机发现**: `fping -asg 192.168.1.0/24` / `nmap -sn 192.168.1.0/24`
- **端口发现**: `masscan 192.168.1.0/24 -p1-65535 --rate 5000`（大网段粗扫快）→ `naabu -host IP -p top-1000`（配合 Web 指纹）
- **服务识别**: `nmap -sV -sC -O -Pn IP`（-sV 版本 / -sC 默认脚本 / -O 系统）
- **Web 指纹**: `httpx -tech-detect -title -status-code` → 识别组件 → 查 1day 速查表（见 web.md）
- **内容发现**: `ffuf`/`dirsearch`/`dirmap` 目录爆破 → 敏感路径（.git/.env/swagger 见信息泄露条目）
- **漏洞验证**: `nuclei` 模板批量 + 手动确认（dnslog 出网验证）
- **内网域环境重点端口**: 445/135/139/88(Kerberos)/389(LDAP)/3389(RDP)/5985(WinRM)

### 子域枚举（crt.sh + 字典双源）
- **验证**: `subdomain_enum.py target.com` → 证书透明日志 API + 65 个默认前缀字典爆破 + DNS 解析验证，20 线程并发；双源融合（CT 日志 + 字典），默认含 admin/api/dev/staging/grafana/jenkins/k8s 等高价值前缀
- **升级**: 命中子域 → 测绘扩面 / 直连源站

### 端口扫描（asyncio 异步）
- **验证**: `port_scanner.py target` → 200 并发 connect 扫描 + banner 抓取（FTP/SSH/SMTP/POP3/IMAP/MySQL/PG/Redis/Memcached/MongoDB）+ 37 个常见端口默认库，纯 asyncio 无需 nmap
- **升级**: 开放端口 → 指纹 → 定向攻击

### 目录爆破（扩展名自动附加）
- **验证**: `dir_brute.py -u target` → 多线程字典 + 12 种扩展名自动附加（.php/.html/.bak/.zip/.sql/.conf 等）+ 状态码过滤 + 最小大小过滤 → 找备份文件特别有效（`www.zip`/`config.bak`/`.swp`）
- **升级**: 备份/源码泄露 → 白盒审计（见版本控制条目）

### WAF 检测（指纹匹配）
- **验证**: `waf_detect.py -u target` → 正常请求 + 攻击请求（SQLi payload）双请求对比 + 15 种 WAF 指纹库（Cloudflare/ModSecurity/AWS WAF/Akamai/F5/Imperva/Sucuri/FortiWeb/Barracuda/Citrix/WordFence/Nginx/Radware/Kona）→ 含攻击请求触发状态码分析
- **升级**: 识别 WAF 类型 → 选对应绕过策略（见 web.md WAF 绕过条目）

### CMS 指纹
- **验证**: `cms_fingerprint.py -u target` → 特征路径检测（wp-admin/Joomla-administrator/Drupal-sites/default/Laravel-vendor/Spring-actuator）+ 关键词 + Server 头分析，覆盖 10 种 CMS/WebServer
- **升级**: 识别框架 → 框架专项漏洞探测

### JS 端点提取
- **验证**: `js_endpoints.py -u https://target/app.js` → 6 种正则提取 API 路由、fetch/axios 调用、baseURL、硬编码 URL
- **升级**: 端点 → API Fuzzing / 鉴权测试（见 web.md API 参数 Fuzzing 条目）
