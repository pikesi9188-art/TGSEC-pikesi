# 库存分闸

> 不要按 493 张一起刷。full=挂了 Playbook+工具，只是计数，不是质量。  
> 闸：`python3 炼蛊房/skill_lane.py`  
> 蒸馏会带 `lanes`。`attack_paper` 才是还该修的打站洞。

## 1. 四闸

| 闸 | 算什么 | 怎么动 |
|----|--------|--------|
| **打站过关** | 有自己的探针/步骤 | 出假阳性再修，不重贴真源 |
| **打站纸面** | 业务站卡，工具还是 `auto_campaign` / `nday_route --help` / 只挂 nuclei | **真该修**：补本族路径和 L2 |
| **分流** | 路由卡，本来就指向别人 | 挂菜单即可，不算洞 |
| **百科** | iOS 内核、无线、培训、CI 百科 | 资料。不要按打站 L2 收 |
| **收编** | 96 张 `pack: survey-skill` | 库存。开源不带，本库不当打站成绩 |

## 2. 现况（以脚本为准）

```bash
python3 炼蛊房/skill_lane.py
python3 炼蛊房/engine_distill.py snapshot
```

打站过关一大片；打站纸面应收到 **0**。  
百科 / 分流 / 收编再多，也不进「bug 修不完」那一堆。

## 3. 什么算过关

打站卡的 full 只认这三样：

1. 本族路径或签名（不是 `--help`）
2. L2 口径写死（禁止只靠体长 / HTML 200）
3. 有单测或 `--list` 能跑

挂 `auto_campaign --help` 只算分流或纸面。

## 4. 还在造洞的做法（停）

- `skill_upgrade_thin.py` 再批量贴通用工具
- 整包改名导出却不跑两边 `--list` / pytest
- 把百科卡和打站卡写进同一张「未完成」清单
