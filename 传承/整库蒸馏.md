# 大爱仙尊整库蒸馏

> 快照：2026-09-06 · 刷新 `python3 炼蛊房/engine_distill.py snapshot`  
> 数字真源：`skill_catalog.py` + `engine_distill.py`（禁止手改 AGENTS 里的张数）  
> 不扫：`作业入口/`、`站点详情/`、`案卷/`  
> 不读：`态度蛊.json` 正文

**不是**百科。蒸馏只回答四件事：这是什么、怎么走、现在有多少、哪断了。

---

## 0. 何时启用

| 用户说法 | 走 |
|----------|----|
| 蒸馏整个仓库 / 整库蒸馏 / 盘点技能 / 三件套缺口 | **本卡** · `engine_distill.py` |
| 某张卡怎么写 / 商店收编 / 交货完整性 | `工卡·规范.md` · `skill_catalog.py` |
| 某类技能有没有 / 39 模块 | `cybersecurity-catalog` · `css_query.py` |
| 百科检索 / 39 模块 | `三十九门·检索.md`（禁止 sync） |
| 真假分离 / 进站 / 组件情报 | `幻道辨真.md` |
| 技能索引 / sqlmap 阶梯 | `沈伤·索引.md` |
| 只丢了工具名/手法名 | `凤金煌·分音.md` |
| 公开仓对照 / PAT / HackTricks / nuclei 缺口 | `公开仓对照补强手法.md` |

```bash
python3 炼蛊房/engine_distill.py snapshot
python3 炼蛊房/engine_distill.py orphans
python3 炼蛊房/engine_distill.py dirty
python3 炼蛊房/skill_catalog.py doctor
```

---

## 1. 一句话

授权范围内的资产测绘 + 渗透自检 + 丢失资产找回工作区。  
四层同时工作：**引擎编排** · **授权闸** · **案卷作战室** · **Skill/Playbook/探针三件套**。

人类走 `作业入口/`，专项走 `杀招/`，程序认 `杀招/` · `传承/` · `炼蛊房/`。

---

## 2. 作业操作系统（压缩）

```
URL / 域名
  → 不在 scope？  scope_expand.py --grant <域> --case <案卷>
  → 用户说暂停？  停进攻，只写文档
  → case-triage 定级（A/B/C）
  → 有身份？      先填 案卷/object_matrix.md（专卡阴性必须回表）
  → 认族          AGENTS 决策树 → keyword-router → 家族路由
  → 打到 L2       读 Playbook → 跑探针 → 落 STATUS
  → 先问只剩      耗余额下单 / 删站清数据 / 改原超管密
```

路由分层（**细则不进 AGENTS**）：

| 层 | 管什么 | 真源 |
|----|--------|------|
| 粗路由 | 指纹 → 专链入口 | `AGENTS.md` 决策树 |
| 关键词 | 用户只丢工具名 | `keyword-router` · `凤金煌·分音.md` |
| 家族 | 白标 / 芋道 / TMA / 盘口细则 | `gambling-family-router` · `extended-skill-router` |
| 漏洞类 | 已确认 XSS/SQLi/… | `web-vuln-router` |
| 域/WP | AD / WordPress 场景 | `ad-windows-router` · `wordpress-attack-router` |
| 百科 | 39 模块有没有 | `cybersecurity-catalog` |
| 假设账本 | 长时多假设 / ledger | `hypothesis-ledger` · `东方长凡·推演.md` |

---

## 3. 2026-09-06 快照

以当天 `python3 炼蛊房/engine_distill.py snapshot` 为准。自写工具箱在 `tools/`；nuclei / JDK / jadx 二进制不进仓，装法见 `炼蛊房/外门依赖.md`。

| 层 | 数量 | 含义 |
|----|------|------|
| 杀招 | **408** | `杀招/`（fail=0） |
| 传承 | **243**（手法 232 + CVE 日报 11） | `传承/`；日报文件名保持 `CVE日报-` |
| 炼蛊房脚本 | **196** | `炼蛊房/*.py` |
| 工具箱 | **13** 柜 / **154** 文件 | `tools/`（1day-kit 自写模板 71；无 nuclei 本体） |
| 九转技能包 | **112** | `智道藏书/九转/`，无专卡才读 |
| 三十九门 | **40 模 / 195 条** | `智道藏书/三十九门/` |
| 智道推演 | **22** | `智道藏书/智道推演/` |
| 授权闸 | 态度蛊 | `态度蛊.json`（不进 git） |
| 案卷 | 本地 | `案卷/`，gitignore |

三件套：`skill_catalog.py doctor` **fail=0**，orphan_biz=0。

---

## 4. 目录层（程序认的）

| 路径 | 角色 |
|------|------|
| `态度蛊.json` | 开打闸门；扩权走 `炼蛊房/scope_expand.py --grant` |
| `传承/` | 手法真源 |
| `杀招/` | 触发词 + 交接 |
| `炼蛊房/` | 可跑探针 |
| `案卷/` | STATUS / 对象矩阵 |
| `智道藏书/` | 知识层（无专卡才读） |
| `NAMING.json` | 英文机名 ↔ 挂号名 |

开源包没有 `engine/` / `cli/`。`tools/` 只带自写柜，不带 arsenal / deepaudit / vendor JDK。换出口、单站授权文件不进公开仓。

---

## 5. 断点（本轮蒸馏）

### 5.1 数字漂了

`AGENTS.md` / `README.md` 已对齐 **493**（395+2+96）。以本卡快照 + `engine_distill.py` 为准。假设账本已融进作业链（`hypothesis-ledger`），15 份领域长文不覆盖专卡。同族重复卡并进专卡，旧名留 stub。stub 判定只认「本卡仅重定向」，主卡写「旧名已并入」不算 stub。不收 50 张通用百科。全文仓目录名已收成 slug。

### 5.2 打站卡未挂任何入口（0）

原 20 张工作流/百科已挂进 `凤金煌·分音.md` §2.10，**没有**进 AGENTS。  
作业仍走专卡；这些名字只负责认词。禁止再往 AGENTS 决策树堆行。

### 5.3 thin / fail

无。fail=0，thin=0，kit=0。

### 5.4 探针未被 Skill 正文点名（0）

`engine_distill.py orphans` → `ops_not_in_skill=0`。  
redis / fastjson / dirbrute / bucket 已写进对应 Skill 的 `炼蛊房/*.py` 路径。  
元工具（`probe_http` 库、`skill_*`、`site_detail`、`scope_stale_candidates` 等）进 `META_OPS`，不算漏挂。

### 5.7 同族重复卡（本轮已并）

融合百科 / 旧入口并进专卡，Cursor 目录改 stub，长文留 `智道藏书/`。  
主卡：`hypothesis-ledger` · `keyword-router` · `idor-bola-chain` · `auth-brute` · `crypto-toolkit` · `linux-post-exploit` · `binary-pwn` · `yudao-appapi-pentest` · `encrypted-api-spa` · `web-vuln-router` · `middleware-unauth` · `cdn-origin-tracing` · `spa-protocol-reverse` · `attack-router` · `case-triage`。  
本轮并：`cdn-origin-bypass` · `spa-frontend-api-recon` · `pentest-workflow` · `target-triage`。  
PHP debug 两张专卡互补，只交叉引用，不 stub。不并家族专卡（芋道 TMA / 博彩族 / 逆向分流）。

### 5.6 文档命令对不上真实 CLI（本轮已修）

`stdlib_fallback.PREFER` 与 `attack-chain` 等把子命令/参数写错（`--host`/`--base`/`fill`/`fingerprint`/`generate`）。  
对照 `add_argument` 回写。新探针改 CLI 时同步改 PREFER 和入口卡。

### 5.5 融合短规则误伤（本轮已修）

`shizhan_pack_fuse.py` 曾用子串匹配：`iam`⊂`authentication`、`aws`⊂`flaws`、`el-`⊂`panel/laravel`、`ad-`⊂`upload-`。  
登录旁路卡被挂到云元数据 SSRF。现按 hyphen token 匹配；`python3 炼蛊房/shizhan_pack_fuse.py --rekit` 只回写融合卡手法/工具。  
登录注入走 `authbypass-authentication-flaws`，不进 AGENTS。

---

## 6. 蒸馏规矩（以后每张新卡）

1. **三件套**：Playbook + Skill + 可跑探针。缺一 = `kit`/`thin`，不能当交货。  
2. **先挂入口再写细则**：粗路由只留家族入口；细则进家族/关键词表。  
3. **数字跟脚本**：改完跑 `engine_distill.py snapshot`，同步 AGENTS / README 张数。  
4. **授权外不扫**：本脚本禁止走进工作台/案卷/报告树。  
5. **Isolated 卡阴性不算结案**：有身份必须回 `object_matrix.md`。

---

## 7. 复工勾选

- [x] `engine_distill.py snapshot` 与本卡快照日期一致，或已重刷日期+表  
- [x] AGENTS / README 张数 = snapshot 的 `skills.total`  
- [x] `orphans` 新增名字已分流（挂入口 / stub / 降权）  
- [x] fail=0 或已标明「百科短卡、不交货」
- [x] `python3 炼蛊房/engine_distill.py snapshot` 与本卡快照日期一致
- [x] `skill_catalog.py doctor` fail=0、orphan_biz=0
- [x] CVE 日报文件名仍是 `CVE日报-YYYY-MM-DD.md`（蒸馏才数得着）
- [x] 别名打印挂号名：`python3 炼蛊房/se_skill_alias.py --signal 天盾卡密` → `skill: 卡密·破禁`
