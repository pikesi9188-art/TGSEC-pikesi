> **盗天**
> 自身本是轮回客，踏遍万里寻归途！
> 盗亦有道留一线，手到偷来不问途。

# 暗渡陈仓·2

> 杀招：`暗渡陈仓` · 工具：`炼蛊房/open_redirect_surface_probe.py`  
> HITCON：returnUrl / 控制字元绕过检查。

```bash
python3 炼蛊房/open_redirect_surface_probe.py --base https://授权站 --case <案卷>
```

| 档 | 成功 |
|----|------|
| L1 | 加参数后 Location **相对基线变化** |
| L2 | 跳到 `example.com/se-open-redirect` |

不跟随外域、不发钓鱼。ATO 要 token 进查询串时并行 `reset-token-surface`。

**微信 OAuth：** `Location` 或随机 302 把 `code=` 带到异 host（含恶意落地页）算 L2。并行 `tp3_fenxiao_probe` 的 `user_weixin_login`（`state=STATE` / callback 零校验）。剪贴板劫持 JS 记 IOC，不把恶意域自动扩进 scope。
