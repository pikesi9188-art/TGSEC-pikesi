# N-central 认证绕过手法（CVE-2026-18556 / CVE-2026-18577）

> 情报：`od_kit.py cvebase lookup --cve CVE-2026-18577`  
> 指纹：`tools/1day-kit/custom-templates/ncentral-auth-bypass-cve-2026-18577.yaml`  
> 总览：`新伤·在野.md`  
> 红线：仅 `授权范围`；RMM 接管后对受管终端操作先问

---

## 1. 一句话

N-able **N-central**（≤ **2026.3.1**）存在认证绕过；**CVE-2026-18577** 是对 **CVE-2026-18556** 的**不完整补丁再绕**。  
无凭据接近管理 API / 控制台能力 → 再经 Take Control 等通道打向受管终端。  
KEV · 在野。修复关注：**2026.3.1.10（Hotfix 2）** 及厂商后续。

---

## 2. 战场价值

RMM = 全网管工具箱：远端桌面、脚本、补丁、凭据保险库。  
打穿 N-central 控制台 ≈ 一次性拿到客户机群跳板，优先级高于普通业务站。

---

## 3. 指纹触发

| 信号 | 说明 |
|------|------|
| 登录页 | `/dms2/login.do` · `/login.do` · 体含 N-central / N-able |
| API | `/api/account-management/accounts` · `/api/v1/users` · `/dms2/rest/api/auth/token` |
| 旁路探测头 | 部分环境对 `X-Forwarded-For` / `X-Real-IP: 127.0.0.1` 响应差分（模板已带；命中≠已接管） |

```bash
python3 tools/1day-kit/od_kit.py cvebase lookup --cve CVE-2026-18577 --docs
python3 tools/1day-kit/od_kit.py nuclei --url https://授权站 --case <案卷> \
  --template-id ncentral-auth-bypass-cve-2026-18577
```

---

## 4. 档位与成功口径

| 档 | 动作 | 成功证据 |
|----|------|----------|
| **L1 指纹** | 登录页 / 版号提取 | 确认 N-central |
| **L2 绕过** | 未授权 API 200 或会话/角色提升 | 管理数据可读或等价会话 |
| **L3 横向** | Take Control / 脚本下发（授权内） | 受管主机可验证控制；**先问** |

本库 yaml **默认只探面**，不自动完成账号接管。L2/L3 按公开细节与授权范围人工推进。

---

## 5. STATUS 复工条件示例

- [ ] 授权目标确认 N-central 且版本 ≤ 未修线  
- [ ] L2：未授权管理 API 或会话绕过可复现  
- [ ] L3：受管终端控制须单独确认授权范围
