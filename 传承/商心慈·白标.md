> **商心慈**
> 心慈不是心手软，账清才能走得远。
> 千门白标同一宗，认族先问商家钱。

# 白标博彩家族与加密 API 分流

> 先认族，再打开对应 `智道藏书/旁支传承/skills/<name>/SKILL.md`。  
> **不替代** `凤九歌·认族.md` 的资金/代理主线。  
> 红线：仅 `授权范围`；耗余额下单 / 批量改金先问。  
> 管理端 `/admin-api` + `mock-enable` / 代付 `df-user` 切 **`芋府·假面开天.md`**，不要套 TMA 信封。

## 1. 30 秒

```text
有盘口/白标/TMA/加密请求
  → 业务专链（假支付 / USDT 归属 / Actuator / GVA）仍优先
  → 跑 yudao_appapi_probe 认协议
  → 按下表切家族卡，立刻按卡上默认探针做
  → 认不出族 → gambling-saas-pentest-workflow + JS 密钥
```

```bash
python3 炼蛊房/yudao_appapi_probe.py -u https://授权站 --case <案卷>
python3 炼蛊房/gambling_family_probe.py --family 1z --base https://授权站 --case <案卷>
python3 炼蛊房/gambling_family_probe.py --family m8 --base https://授权站 --case <案卷>
python3 炼蛊房/gambling_family_probe.py --family mm8 --base https://授权站 --case <案卷>
python3 炼蛊房/gambling_family_probe.py --family daanrox --base https://授权站 --case <案卷>
```

## 2. 指纹 → 卡

| 看到 | 走 |
|------|-----|
| `/runtime/app-config.js` + `PLATFORM_ID` + `/app-api/` | **先** `yudao-appapi-pentest`（探针）；TMA 细则 `yudao-tma-pentest`；创世/世博补 `telegram-gambling-yudao-pentest`；Web 变体 `yudao-web-appapi-pentest`。叠打不并卡 |
| `/app-api/encrypt` 或只见 `sk_encrypt.json` | **`encrypted-api-spa`**（同一探针，不是另一套协议） |
| `<title>Qzino</title>` / `POST /api.html` / `vue/config.js` | **`telegram-tma-gambling`**（勿套芋道信封） |
| 瓦力 / `trial.do` / GCS+XOR | `wali-tg-tma-pentest` |
| 666Bet / `suid` / `PID=10` | `666bet-tma-pentest` |
| Laravel TMA / `X-Device-Id` | `laravel-gambling-tma-pentest` |
| kk8 / `GET /gc?c=` | `kk8-tma-platform-pentest` |
| `lsmview` / `mclsm` / `cdnrc` | **`lsm-mclsm-gambling-pentest`**（不要停在通用白标卡） |
| 1Z / qpuserapi / 点选 SVG | `1z-gambling-family-pentest` |
| 鼎艺 / dingyi / verifyKey | `dingyi-whitelabel-gambling-pentest` + `gambling-deposit-chain-pentest` |
| 曼巴 m8 / m5ba / HSBox m9 | `m8-manba-*` / `m9-hsbox-*` |
| MM8 / GEN 白标 | `mm8bet-gen-whitelabel-pentest` |
| GoFun / 66U / `VITE_HTTP_SINGKETY` | `aks-gofun-gambling-pentest` + `gambling-api-crypto-reversal` |
| 巴西 tRPC SPA | 有 Daanrox/PHP 后台 → `daanrox-br-gambling-pentest`；只有 tRPC 壳 → `br-gambling-trpc-spa-pentest` |
| 绑卡+凭证上传才充值 | `gambling-deposit-chain-pentest` |
| `wallet_id` / 展示层充值地址 / 双路径 / 幽灵单 / `fundsn` / `turnWaterInit` | **`fund-edge-ops`** · `血神子·账.md` |
| 请求带密文 `?c=` / webpackChunk 加密模块 | `gambling-saas-pentest-workflow` T1–T2 |
| PG Soft `keys=` 启动器 | `pg-soft-launcher-pentest`；`ot`+`ops` 会话另开 `pg-operator-session-chain`（两段不并） |
| 「支付管理中心」/ `admin_login.lock` | `epay-admin-pentest`（假支付仍走 `pay_matrix`） |
| FastAdmin 代付 `/sh.php` `/ks.php` / `part/index` | **`fastadmin-daifu-pentest`** · `快府·代付.md` |
| FastAdmin Shop `/shop_hq` `filter/op` GT | **`fastadmin-shop-tenant-bola`** · `快府·横夺.md` |
| API key 死绑用户 token、IDOR 全灭 | **`account-takeover-chain`** |
| 网狐 / `QPAccountsDB` / `protocal=167` / `Machine` 注入 | **`whgame-tp5-login-sqli`** · `网狐·吞库.md`（探表 `SELECT 1`） |

全文在 `智道藏书/旁支传承/INDEX.md`。

## 3. 强制顺序

1. 目标在 scope。  
2. 探针落 `案卷/yudao_appapi/probe.json`。  
3. 命中密钥/协议 → 按家族卡做 **未授权泄露 + 登录/充提链**，不要只记指纹。  
4. 支付回调仍交 `payment-callback-forgery` / `pay_matrix`。  
5. 凭证伪造进人工审核 ≠ 自动入账；STATUS 写清「待审」还是「已到账」。

## 4. 复工

- [ ] 族已钉（或已写「未知白标 + SaaS 工作流」）  
- [ ] 探针 JSON 在案卷  
- [ ] 资金面阴性后才转运维面（宝塔/Actuator）
