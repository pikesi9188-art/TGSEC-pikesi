# 三十九门·检索

> 大爱仙尊作业卡。检索本库 39 模块、payload、字典；**不是**第二套打站武器。  
> 业务专卡永远优先。禁止把外部百科 sync 进 `杀招/`。

---

## 1. 本库四层

```
知识卡 → 检索命令 → 专卡探针 → 案卷
```

| 层 | 本库入口 |
|----|----------|
| Skill | `杀招/` + AGENTS 决策树 |
| 分类知识 | `智道藏书/三十九门/`（39 模块） |
| 字典 | `dict/` + `weakpass` + `auth_brute_probe` |
| Payload | `payload_lookup.py` · 72stack-sec · evasion-kit |
| 1day | `nday_route.py` · 1day-kit · CVE 日报 · nuclei |
| 检索 | `css_query.py` · `engine_distill.py` · `h1_search.py` |

本库强在授权闸、家族杀伤链、可跑探针、对象矩阵、案卷。百科只补「这类手法有没有」。

---

## 2. 怎么查

```bash
python3 炼蛊房/css_query.py search --keyword <词>
python3 炼蛊房/payload_lookup.py --type <sqli|xss|ssrf|rce>
python3 炼蛊房/nday_route.py -u https://授权站 --case <案卷>
```

| 主题 | 走 |
|------|----|
| 侦察 / OSINT | 模块 01 · `space-search` · `session_pipeline` |
| Web 注入族 | `web-vuln-router` · `core_web_surface_probe`；业务洞仍走假支付等 00 |
| 认证 / 越权 | `rbac-bypass-authz` · `auth-brute` · `jwt_gql_probe` |
| 云 | 模块 17 · 阿里云 AK Playbook · `cognito-unauth-s3-chain` |
| 横向 | `ad-windows-router` · `internal_scan` |
| 后渗透 | `linux-post-exploit` · `host-ir-check` |
| 白盒 | `deepaudit-code-audit` |
| LLM | `llm-security` · `大灵.md` |
| Nuclei | `1day-nuclei-kit` |
| 免杀 / 马 / C2 | **只开本库 CSS 08 原理卡** |
| Nacos / Grafana 等中间件 | 先 `nday_route`，再专卡 |
| 假支付 / 芋道 / FastAdmin Shop / TG | 只走本库 00，百科没有对等物 |

冲突时：**业务专卡 > 本库 Playbook > 39 模块**。

---

## 3. 禁止

- 把外部百科或 sync 脚本灌进 `杀招/`
- 收编免杀 / 马 / C2 实现
- 用外部 PoC 替代本库「只指纹」1day 卡
- 只聊天检索、不跑本库探针
