# 实战认族

> 长文仓：`智道藏书/旁支传承/`  
> 路由：`python3 炼蛊房/shizhan_pack_route.py --name <卡名>`  
> Cursor：Skill `实战认族`

## 何时用

- 用户提到实战包卡名 / Ghost Bits / 401-403 / type juggling / Host 头
- `attack-router` 点到 `401-403-bypass-techniques`、`clickjacking` 等
- XSS/SQLi 仍先走本库专卡

## 强制

1. `杀招/` 已有打站卡，禁止用同名目录覆盖。
2. 长文在 `智道藏书/旁支传承/`。
3. 免杀 / 马 / 证件伪造脚本不收成作业卡。
4. 案例域名（如已修的 `tg.507.mx`）不自动扩进 scope。
5. 新卡必须有 Playbook + 可跑入口。

## 入口

```bash
python3 炼蛊房/shizhan_pack_route.py --name 401-403-bypass-techniques
python3 炼蛊房/shizhan_pack_fuse.py --rekit   # 只回写融合卡手法/工具，不覆盖专卡
python3 炼蛊房/engine_distill.py snapshot
```

## 分流

| 内容 | 走 |
|------|----|
| 同名专卡 | 本库专卡 |
| XSS/SQLi/SSRF/上传/JWT/假付 | 本库对应专卡 |
| 登录注入 / admin'-- / 认证绕过 | `authbypass-authentication-flaws`（网狐走 `whgame-tp5-login-sqli`） |
| FastAdmin `filter/op` GT | `fastadmin-shop-tenant-bola`（507 案例已修，换站） |
| 独角 Next / 易支付回放 | `dujiao-next-1yuan-pay` / `epay-admin-pentest` |
| 异次元 ACG | `acg-faka` |
| 401/403、Host、Ghost Bits、PHP `==` | 对应作业卡 |
| AMSI / webshell 免杀 | `edr-bypass-re` + CSS 08；授权内落地，证件伪造 stub 不落地 |

## 不要做

- 把 219 张一次性全加载进上下文
- 把开户/证件脚本当 L2 命令跑
