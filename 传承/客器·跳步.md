# 客器·跳步

> 杀招：`客器`  
> 工具：`炼蛊房/client_state_skip_probe.py`  
> 情报：HITCON ZD-2026-00974（验证状态可窜改 → 无限免费骑乘）、购物逻辑漏洞  
> 红线：不耗余额下单、不改超管密。

前端 `verified=true` / `paid=1` 被后端当已检。HITCON 上这类仍在修。本卡先验后端是否吃客户端旗标。

```bash
python3 炼蛊房/client_state_skip_probe.py --base https://授权站 --case <案卷>
```

| 档 | 动作 | 成功 |
|----|------|------|
| L1 | 首页/JS 扫 isPaid、isVerified、canRide | 仅文案 |
| L2 | POST 翻旗标后状态/体相对基线变 | 前端自己变 |
| L3 | 免付/免检业务 | 先问 |

命中后填对象矩阵（自己 vs 他人 × 读/写/加款）。加款面并行 `business-logic-payment`。
